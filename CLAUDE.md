# CLAUDE.md

This file provides guidance to Claude Code when working with this repository.

## Project Overview

MSFS Blind Assist - C# Windows Forms accessibility application for Microsoft Flight Simulator. Multi-aircraft support (FlyByWire A320, Fenix A320, extensible). SimConnect integration, screen reader optimized (NVDA/JAWS). .NET 9, Windows Forms, SQLite.

## Build Commands

```bash
dotnet build MSFSBlindAssist.sln -c Debug
dotnet build MSFSBlindAssist.sln -c Release
```

**Output:** `MSFSBlindAssist\bin\x64\{Debug|Release}\net9.0-windows\win-x64\`

**⚠️ ALWAYS build the SOLUTION (or pass `-p:Platform=x64`), NEVER `dotnet build MSFSBlindAssist\MSFSBlindAssist.csproj` alone.** The csproj declares `<Platforms>x64</Platforms>` only, but a bare `dotnet build` on the .csproj still defaults to **Platform=AnyCPU** and writes the exe to **`bin\Debug\net9.0-windows\`** — a DIFFERENT folder from the one the app runs from (**`bin\x64\Debug\net9.0-windows\`**). The build will say "Build succeeded" while the running x64 exe stays frozen at its old timestamp, so changes silently never reach the user (burned a whole session on this — every "compile-check" went to bin\Debug and the user's x64 exe never updated). Correct commands: `dotnet build MSFSBlindAssist.sln -c Debug` (the .sln maps to Debug|x64) or `dotnet build MSFSBlindAssist\MSFSBlindAssist.csproj -c Debug -p:Platform=x64`. To verify a build actually landed, check the LastWriteTime of `bin\x64\Debug\net9.0-windows\MSFSBlindAssist.exe` is "now". The exe is file-locked while MSFSBA runs (MSB3021) — close the app before building a fresh exe the user will run.

**Prerequisites:** MSFS_SDK environment variable, .NET 9 SDK

The solution contains three projects: `MSFSBlindAssist` (main app), `MSFSBlindAssistUpdater` (small WinForms auto-update helper), and `tools/PMDGDispatchTester` (a console diagnostic REPL for probing which PMDG NG3 dispatch shape a switch accepts against a live sim — e.g. used to confirm the 737 fire-handle UNLOCK→TOP sequence). The tester compiles the main app's `SimConnect/PMDGNG3DataStruct.cs` via a **linked** `<Compile>` (not a copy) so its CDA layout can never drift. `dotnet build MSFSBlindAssist.sln` builds all three. A second standalone probe, `tools/CDUTest`, fires a single CDA-write or TransmitClientEvent at one chosen PMDG event (used to prove the NG3 CDU keys need TransmitClientEvent, not the CDA write); it builds on its own (`dotnet build tools/CDUTest`), not as part of the solution.

## Testing

No automated test project exists. Verification is done by running the app against a live sim (MSFS 2020 or 2024). When making changes, build, then describe an in-sim test plan in the PR — the human owner of the repo runs it. Don't add unit tests speculatively; this is a SimConnect-driven UI app where most code paths only execute against the real simulator.

## Git Workflow

The `main` branch is protected. Always create a new branch for changes and open a pull request — never commit directly to main.

## CRITICAL Rules (Always Follow)

### Screen Reader Announcements

**CRITICAL:** Screen readers automatically announce ALL UI control interactions.

**NEVER announce:**
- Button presses in panel controls
- Combo box/dropdown value changes
- Any direct user interaction with UI elements

**ONLY announce:**
- Numeric input confirmations (user needs exact value feedback)
- Error conditions (validation failures)
- Background state changes (not directly triggered by user)

**Why:** Screen readers already announce UI interactions. Redundant announcements = poor UX.

### SimConnect Connection Timing

**CRITICAL:** In SimConnectManager.cs, set `IsConnected = true` BEFORE calling `SetupDataDefinitions()`. Required for `StartContinuousMonitoring()` to execute properly (has guard clause requiring `IsConnected == true`). See SimConnectManager.cs:251

### Accessible TreeView Controls

**CRITICAL:** Never use `TreeView` directly in forms. Use `NativeAccessibleTreeView` (`Controls/NativeAccessibleTreeView.cs`) instead. .NET 9's `TreeViewAccessibleObject` (UIA-based) produces incorrect navigation order in NVDA — items appear out of sequence, focus jumps between unrelated nodes. `NativeAccessibleTreeView` bypasses the .NET 9 UIA implementation and falls back to the native Win32 SysTreeView32 MSAA proxy, which works reliably.

**Pattern for tree views with detail data:**
- Parent nodes show summary text only — no child nodes pre-populated
- Add a dummy child `new TreeNode("Loading...") { Tag = "placeholder" }` so the expand indicator (+) appears
- Handle `BeforeExpand` to lazily populate real child nodes on demand, checking for the placeholder first
- Store the data index in `parent.Tag` so the expand handler can look up the data
- Leaf nodes (e.g. airport endpoints with no detail) get no placeholder and no expand indicator

This lazy-loading pattern keeps the tree lightweight (fewer total nodes) and avoids accessibility edge cases.

### Database Paths

**CRITICAL:** Never hardcode `Path.Combine(..., "FBWBA", "databases", ...)` or `Path.Combine(..., "MSFSBlindAssist", "databases", ...)`. The user's DB may live at *either* location for historical reasons (the app was renamed). All code must go through `Database/DatabasePathResolver.cs`:

- `ResolveExistingDatabasePath(simVer)` — for **reads** (canonical first, legacy fallback). Used by `DatabaseSelector`, `MainForm`, `ElectronicFlightBagForm` lookups, etc.
- `GetCanonicalDatabasePath(simVer)` — for **writes** (always `MSFSBlindAssist\databases\`). Used only by the build target in `DatabaseBuildProgressForm`.

`NavdataReaderBuilder.GetDefaultDatabasePath` delegates to the resolver and is safe for reads. The MS Store package name for FS2024 is `Microsoft.Limitless_8wekyb3d8bbwe` (not "FlightSimulator2024"); FS2020 is `Microsoft.FlightSimulator_8wekyb3d8bbwe`. Both are referenced in `NavdataReaderBuilder.GetMSFSBasePath` when resolving `UserCfg.opt` for the scenery base path.

### Taxi Guidance

**Feature:** Turn-by-turn taxi assistance — stereo-panned steering tone + spoken announcements, driven by a graph built from the navdatareader `taxi_path` / `start` / `parking` tables. Works on any airport the user's DB exposes.

**Key rules when touching this code:**
- **No airport-specific hardcoding.** Everything comes from the user's DB. Taxiway names (`A`, `K2`, `LINK 53`, `HAWKER`), parking abbreviations (`G`, `GA–GZ`, `P`, `NP`, `EP`), and runway IDs flow through unchanged.
- **Do not break the teleport → takeoff-assist flow.** The MainForm runway-reference seeding from taxi lineup remains guarded by `!takeoffAssistManager.IsActive && !takeoffAssistManager.HasRunwayReference` so the existing teleport dialog path wins for the current activation. **Takeoff Assist's `Toggle(off)` now unconditionally clears the runway reference** — within-session preservation was unsafe because turnaround flights silently reused flight 1's runway threshold and heading on flight 2's CTRL+T (the `HasRunwayReference` guard rejected the fresh taxi-lineup reference). Across-session preservation isn't needed: process restart resets everything. The teleport dialog path (`OnTakeoffRunwayReferenceSet`) still calls `SetRunwayReference` unconditionally so teleport always wins when used.
- **Where-Am-I runway-detection fallback.** When neither taxi-lineup nor teleport has provided a runway reference, the MainForm `POSITION_FOR_TAKEOFF_ASSIST` handler probes `TaxiGuidanceManager.TryDetectRunwayUnderAircraft` (which wraps `TaxiGraph.TryGetRunwayAtPosition`) using the aircraft's current lat/lon and heading. Gated on `_lastOnGround` — airborne CTRL+T still falls through to the synthetic-centerline path in `TakeoffAssistManager.Toggle()`. Uses a strict half-width tolerance (no +5 m fudge, unlike `DescribeLocation`) so a high-speed exit adjacent to a runway doesn't false-positive. Falls through to synthetic centerline if the airport has no `RunwayCenterlines` (sparse navdata).
- **Auto-activate Takeoff Assist on lineup.** `TaxiGuidanceManager` fires `RequestTakeoffAssistAutoActivate` (one-shot per route, gated by `_autoActivateFired` which resets on `LoadRoute` / `StopGuidance`) when the aircraft enters the lineup-aligned hysteresis on a runway target (`_isRunwayLineup == true`). MainForm subscribes and, if `SettingsManager.Current.TakeoffAssistAutoActivateOnLineup` is true and Takeoff Assist isn't already active, fires the standard `RequestPositionForTakeoffAssist` flow after announcing *"Lined up. Activating takeoff assist."* The latch is intentionally NOT reset by lineup drift-out — if the pilot manually deactivates Takeoff Assist after auto-activation, drifts off, and re-aligns, Takeoff Assist does NOT re-engage. This prevents surprise after a deliberate manual decision.
- **Do not announce runway info** (length, surface, ILS) from taxi guidance. Out of scope.
- **WAYPOINT_CAPTURE_RADIUS_M (25 m) must skip the last segment.** Otherwise it preempts the gate arrival radius (6 m) and the 50/20/10 ft parking countdown. Runways are unaffected (30 m > 25 m), but gates break without this guard.
- Steering tone uses **stereo pan only** — no frequency or volume modulation. Hysteresis (3°/6°) + 400 ms min sustain + low-pass filter on heading error kill flapping. Thresholds are **width-aware**: scaled per call by `sqrt(pathWidthFeet / 60)` clamped to `[0.65, 1.40]`. For taxi / gate lineup, call the `UpdateHeadingError(error, pathWidthFeet)` overload with the real segment width (gate lineup uses the no-arg baseline). The takeoff *roll* (`TakeoffAssistManager`) handles the wide-runway case itself.
- **Runway lineup uses explicit thresholds, NOT width scaling.** Call `_steeringTone.UpdateHeadingErrorWithThresholds(error, silent=0.5°, activation=1°, maxPan=15°)`. Width scaling — even at the 25 ft / `MIN_SCALE = 0.65` clamp — gave silent ≈1.95° / activation ≈3.9°, which left pilots sitting 3° off heading with no audio cue (between silent and activation thresholds). The new precision values keep the tone panning until heading is centered within ½° and re-resume immediately past 1°. Do NOT call the width-scaled overload from the runway-lineup branch.
- **Lineup-aligned hysteresis** (governs the "Lined up" announcement): enter at heading <1° AND cross-track <10 ft; exit at >2° OR >20 ft. These are literals in `UpdateLineup` runway branch — tightened from 2°/5° / 15ft/30ft after the same too-loose-deadband bug.
- **Lineup pulse mode.** When stopped (≤3 kt) AND (heading error ≥5° OR cross-track ≥10 ft), the runway-lineup branch calls `_steeringTone.SetPulse(true)` — same pan direction, but tone toggles on/off at `PULSE_HZ = 3.0` (volume modulation, phase from `DateTime.UtcNow.Ticks`). Pure audio cue ("you've stopped but you're not done yet") with no speech — pilot's hands are on rudder + throttle and can't field verbal callouts. Forced off in the gate-lineup branch. **Cross-track condition is critical**: intercept-angle saturates at ±30° when cross-track is large, so a pilot who matches the saturated desired heading then stops would get heading error ≈ 0 (tone silent) even though cross-track is still huge. Without the cross-track branch here, that pilot has zero audio cue that they need to move forward to let the intercept controller close on centerline. Pulse keeps firing until BOTH dimensions are within the lineup-aligned hysteresis.
- **Runway lineup math is intercept-angle, not bearing-to-threshold.** `desiredHeading = runwayHeading + intercept · sign(crossTrack)`, with `intercept` rising on a sqrt curve from 0° (at `LINEUP_NOISE_DEADBAND_FEET = 8`) to 30° (at `LINEUP_INTERCEPT_SAT_FEET = 100`). Don't reintroduce a bearing-to-threshold blend — once the aircraft crosses the threshold (which happens during every "line up and wait"), the threshold is behind the aircraft and bearing-to-threshold sits on the ±180° wrap, producing chaotic sign flips on GPS jitter.
- **Lineup state entry must reset the heading-error smoother.** `_smoothedHeadingError = 0; _headingErrorInitialized = false;` at every `LiningUp` entry path (both Continue-past-hold-short and gate `HandleArrival`). Without the reset, the taxi-phase low-pass residual (often 50–80°) leaks into the lineup tone for ~300 ms and steers the pilot off the runway at low speed.
- **No feet-quantity verbal cues for blind-pilot guidance.** "42 feet left of centerline" has no spatial reference for a blind pilot — the tone is the instrument for cross-track. Heading numbers are fine (every pilot has a heading instrument).
- **Threading.** `TaxiGuidanceManager._stateLock` serializes the SimConnect-thread `UpdatePosition` against UI-thread mutators (`LoadRoute`, `StartGuidance`, `StopGuidance`, `ContinuePastHoldShort`, `GetStatusAnnouncement`). Any new public method that touches `_route` / `_state` / `_currentSegmentIndex` MUST acquire `_stateLock`. `TaxiSteeringTone` has its own `_lock` covering `UpdateHeadingError` / `UpdateHeadingErrorWithThresholds` / `SetPulse` / `Pause` / `Resume` / `Start` / `Stop` so audio buffer ops can't race with disposal.
- Magnetic → true heading conversion uses `magVariation` (east positive) before comparing to graph bearings.
- Off-route detection uses **perpendicular cross-track distance** (equirectangular projection, clamped to segment endpoints). Do not switch to endpoint-distance comparisons — that breaks on long segments.
- Hold-short node naming picks **connector-style** names (letter+digit like `A5`) over plain parallel names (`A`) when both are available on the same hold-short node. Preserve this ranking in `TaxiGraph` hold-short resolution.
- **"Where Am I" (Output > `Alt+Y`)** — `TaxiGraph.DescribeLocation(lat, lon)` returns `Taxiway X` / `Gate X` / `Runway X` for the nearest airport. It does NOT depend on guidance being active; the manager caches a query-only graph in `_whereAmICachedGraph`. **Ground-only by design** — gated on `MainForm._lastOnGround` (cached from `SIM_ON_GROUND`); announces `"In flight."` when airborne. Airborne queries belong to the separate LocationInfo hotkey (city/terrain). **Runway detection** uses `TaxiGraph.RunwayCenterlines` — paired runway-start positions from the navdatareader `start` table — not `taxi_path.type='R'` edges (the DB has none). The pair is found by matching reciprocal headings within ±15° and a threshold separation of 200–6000 m. Without this, a pilot standing mid-runway only got a "Runway X" callout within 50 m of a threshold node. Note: hotkey must NOT collide with output `Shift+Y` (`HOTKEY_STATUS_DISPLAY`) — Win32 silently rejects duplicate-chord registrations.
- **Landing Exit Planner (Input > `Shift+X`)** — pre-touchdown exit picker. `LandingExitPlanner` edge-detects airborne→on-ground with GS ≥ 40 kt and auto-activates `TaxiGuidanceManager.LoadRoute(...)` using the pre-built graph. Reuses the existing ILS destination runway/airport (via `simConnectManager.GetDestinationRunway()`/`GetDestinationAirport()`) when set — do not duplicate runway-selection UI. **MainForm's SIM_ON_GROUND handler always uses `RequestAircraftPositionAsync` to feed `ProcessGroundState`** — do NOT trust `LastKnownPosition` here. The cached position is only updated by VISUAL_GUIDANCE / TAKEOFF_ASSIST / TAXI_GUIDANCE paths, and during a hand-flown approach with visual guidance off, none of those fire — the cache stays at whatever the last active path left there (typically the departure-airport taxi-out at GS ~10 kt). Feeding that stale GS to `ProcessGroundState` fails the planner's `GS ≥ 40 kt` "real landing" gate and the activation is silently skipped. The async request adds one SimConnect roundtrip (~33 ms at 30 Hz) — negligible inside the rollout window — and guarantees fresh GS / lat / lon at the moment of the SIM_ON_GROUND change. `_activatedThisLanding` inside ActivateGuidance + a HasPendingExit recheck inside the async callback together prevent double-fire if SIM_ON_GROUND bounces (oleo flicker on hard landings). The `lastKnownPosition` mirror in cases 505/506/507 of SimConnectManager remains because other consumers (TCAS altitude diff, WeatherRadarForm altitude readout, Where-Am-I) still benefit from a fresher cache — but the landing-exit gate cannot rely on it. **`SetExit(..., bool currentlyAirborne)`** arms `_wasAirborne` from the actual air/ground state, NOT unconditionally true. Source: `simConnectManager.LastKnownOnGround` (mirrored from MainForm's SIM_ON_GROUND handler). Wrong-side fix: setting unconditionally to `true` while ON THE GROUND would meet the activation condition on the next ground-state event with GS≥40, false-triggering during a high-speed taxi or rejected takeoff. Honoring actual state means an on-ground plan correctly waits for the next takeoff+land cycle. Form's runway combo items each carry their own wind suffix in display text (`RunwayChoice` wrapper, refreshed via `RefreshRunwayItemsWithWind` when the async `RequestWindInfo` callback resolves — marshal back to UI thread via `BeginInvoke`); the screen reader reads "30R, 12 knot headwind" on focus during dropdown navigation, no separate post-selection announcement needed. Suffix suppressed when `|headwind| < 3 kt`. Don't auto-recommend a specific exit — that needs aircraft-perf data we don't have; let the pilot judge from the wind number.

  **Rollout-phase tone gate and overshoot retarget (`TaxiGuidanceManager.UpdateLandingRollout`).** The handoff from `LandingRollout` to `Taxiing` requires `turnBegun || (atTaxiSpeed && nearExit)`, where `nearExit = distToExitFeet < ROLLOUT_NEAR_EXIT_FT = 500`. The pure `atTaxiSpeed` (GS < 30 kt) condition was wrong — on a long runway the aircraft routinely drops below 30 kt thousands of feet upfield of the planned exit, and resuming the tone there suggested "turn now" while the pilot was still on the runway centerline. **Do not relax the `nearExit` gate back to a speed-only condition.** `turnBegun` is `hdgDeltaAbs >= ROLLOUT_TURN_BEGAN_HDG_DEG (15°) && groundSpeedKts < ROLLOUT_TURN_MAX_GS_KTS (90 kt)`. The speed cap is critical: above 90 kt a heading deviation from runway centerline is touchdown yaw, crosswind crab alignment, or sim physics at wheel contact — not a real runway exit maneuver. Category E rapid-exit taxiways top out at ~90 kt, so legitimate high-speed exit turns are still detected. **Do not remove the `ROLLOUT_TURN_MAX_GS_KTS` guard** — a crabbed approach at KJFK 22L caused the heading to drift 16° from runway heading within 2 seconds of touchdown at 112 kt, falsely triggering the handoff 5,077 ft before the planned exit. On overshoot — aircraft along-runway projection past the chosen exit by ≥ `ROLLOUT_OVERSHOOT_FT = 100` ft AND heading still within `ROLLOUT_TURN_BEGAN_HDG_DEG = 15°` of runway heading — the manager scans `_rolloutAllExits` (cached at `BeginLandingRollout` time from `_graph.GetLandingExits(runway)`, sorted by `DistanceFromThresholdFeet` ascending) for the first exit further downfield and re-`LoadRoute`s in place via `RetargetLandingExit`. If no downfield exit remains, `EnterRunwayEndCountdown` clears `_route` / `_destinationNodeId` and switches into runway-end countdown mode — the route nulling is what prevents `TryRecalculateRoute` from routing back across the runway to the now-passed exit (the original bug). `BeginLandingRollout` now takes `Runway runway` and `List<LandingExit> allExits` parameters; the planner computes the exit list once at touchdown. **`TryEarlyExitHandoff` (at ≤50 kt within 300 ft) only fires for High-speed exits (`ExitType == "High-speed"`, angle < 50°).** For Normal and End exits (angle ≥ 50°) the extension node is too far off the runway heading to give useful tone steering 300 ft before the junction — a 90° exit immediately pans the tone to maximum and the rollout's own 150 ft "turn now" callout is silently lost because state has already moved to Taxiing. Normal exits (50–110°) rely on the 150 ft verbal callout from `UpdateLandingRollout`; at the same moment the verbal fires, the rollout tone switches its desired heading from "bearing to the junction" to `ExitBearingTrue`, giving an immediate hard-pan toward the exit direction. The bearing-to-junction heading fights the turn at this range (junction is still ahead, so heading error flips to the wrong sign as the pilot turns off the runway), whereas `ExitBearingTrue` correctly decreases as the pilot aligns, conveying both direction and "how much more to turn." The heading-error smoother is reset at this transition so the pan is sharp rather than ramping from the near-zero approach residual. The tone continues until `turnBegun` fires (15° heading change), at which point the Taxiing handoff re-routes from the live position to the extension node. End exits are excluded from the ExitBearingTrue switch — a backtaxi requires a ~180° turn whose direction is ambiguous in the heading-error sign; the verbal is sufficient. **Do not restore `TryEarlyExitHandoff` for Normal/End exits** — it caused the EGNX runway 27 / taxiway M (90°) miss: tone went max-left at 300 ft before M junction with no verbal cue, pilot couldn't respond in time. **At every handoff to Taxiing (turnBegun / exitedLaterally / alignedWithExit / atTaxiSpeed&&nearExit), always re-route from the live aircraft position** using the extension-node logic (ApronNodeId if set, else FindExitExtensionNode, else NodeId). This replaces the initial touchdown route — which goes through the taxiway network and gets a false "hold short of runway X" tag from `InsertRunwayCrossingHoldShorts` because the route's destination sits on the runway — with a clean 1–2 segment route. **Do not revert to the ApronNodeId-only re-route** — Normal/End exits have ApronNodeId == NodeId and would keep the bogus initial route.

  **Implicit-exit `ExitBearingTrue` shallow-angle override (TaxiGraph.cs).** For airports whose navdata contains no `HS`/`IHS`/`HSND`/`IHSND` markers (every vanilla MSFS 2024 implicit-exit airport — EDDB, LGAV, and most regional fields), `GetLandingExits` falls through to the implicit-exit path. The runway-edge node's only outgoing edge is often a 30-50 m connector stub that runs nearly parallel to the runway before the taxiway curves off, giving a misleading first-edge `ExitBearingTrue` (e.g. EDDB 24L → M3 stub bearing 255.7° / 6.9° off runway, vs. real M3 end-to-end direction ~280° / 31° off). The shallow-angle BFS override block widens the gate from `exitAngle < 5°` to `exitAngle < 20°` to mirror the parallel HS-style override at the next block. **Both override guards (apron forward-direction AND apronAngle > currentAngleFwd) are required** — without the `> currentAngleFwd` guard, an exit whose stub points further off-runway than its eventual apron node would have its bearing *narrowed* by the override, a regression vs. the first-edge value. `ExitAngleDegrees` is intentionally NOT updated here — matches the pre-existing HS-branch pattern; changing the angle would touch `ExitType` classification, the angle-proportional overshoot margin formula at `:1462`, and the `alignedWithExit` heading-delta requirement, each with separate regression risk. EIDW S5 / N4 are unaffected (HS-type, use the parallel branch). EGNX 27/M and other ≥20° normal exits are unaffected (above the gate). LGAV 03R D8/D9 (the airport the post-handoff pastExit guard at `:1448` was added for) is benign — a wider `ExitBearingTrue` makes `alignedWithExit` MORE restrictive, not less, so A/P jitter is even less likely to false-fire alignment than before.

  **`exitedLaterally` combined gate (TaxiGuidanceManager.cs).** The primary lateral-exit handoff trigger in `UpdateLandingRollout` is `lateral >= halfWidth + 30 ft AND (distToExitFeet <= 250 OR hdgDeltaAbs >= 8° OR pastExit)`. The bare-lateral version fired too eagerly when the pilot drifted laterally during the rollout silent-tone phase (gs > 50 kt OR dist > 300 ft from exit — the tone is silent here by design to avoid pan-from-crosswind-crab confusion). EDDB 24L → M3 reproduction: 129 ft lateral at distToExit=445 ft / hdgDelta=6.6° triggered handoff BEFORE the 150 ft "turn now" verbal cue, leaving the pilot off the runway with no directional cue. The dist gate catches "close enough that verbal cues have fired or are about to"; the hdgDelta gate (8° = half of turnBegun) catches "pilot has clearly committed to a turn"; the pastExit gate preserves the overshoot-detector path. True shallow RETs (< 8° real exit angle) still trigger via the dist gate as they close to ≤250 ft. **The passive-handoff `exitedLaterallyPH` check in the `_rolloutHandoffActive` block is intentionally NOT gated** — different semantics ("has the pilot committed?" post-check, not a handoff trigger); applying the same gate would delay clearing the overshoot monitor and could cause spurious retarget cascades. Commit b69a03d's pastExit guard handles the analogous A/P-jitter concern for the sibling `alignedWithExitPH` check.

  **Runway-end countdown after a missed-last-exit (`UpdateRunwayEndCountdown`).** When `EnterRunwayEndCountdown` fires (overshoot with no downfield exit, or a retarget LoadRoute failure), state stays in `LandingRollout` and a per-frame loop drives three voice callouts based on signed along-runway projection from `_rolloutRunway.StartLat/Lon` plus `Length`: *"Runway end in 1500 feet."* / *"Runway end in 500 feet. Slow down."* / *"Runway end in 100 feet. Stop."* — the 500 ft "Slow down" suffix is suppressed when GS ≤ 30 kt (still at taxi speed, the directive is noise); the 100 ft "Stop" suffix is **unconditional** (the pilot needs the action cue regardless of current speed). Hold-short and parking countdowns are also unconditional on their action suffixes. Tone stays silent — no steering target on rollout, pilot is on rudder/brakes. Hands off to `Taxiing` (with `_route = null`) when GS drops below `ROLLOUT_NO_EXIT_STOPPED_GS_KTS = 3 kt` or heading deviates ≥ 15° AND GS < 90 kt (backtaxi turn; same `ROLLOUT_TURN_MAX_GS_KTS` speed gate applies here too). **Keep `_rolloutRunway` cached through `EnterRunwayEndCountdown` — the countdown needs it.** Full silence on a missed-last-exit is unsafe for a blind pilot rolling toward the end of an active runway; the countdown gives them real braking information.
- **Connected-component-aware start-node selection.** `TaxiGraph.Build` runs a single BFS pass at the end to assign every `TaxiNode` a `ComponentId`. `LoadRoute` (and `TryRecalculateRoute`) look up the destination node's component and pass it as `requiredComponentId` to `FindNearestNodeInDirection` / `FindNearestNodeOnTaxiway`; candidates in a different connected component are filtered out. Same filter is applied to `TaxiRouter`'s private `FindNearestNodeOnTaxiway*` helpers via the from/target node's component. `_nextNodeId` in `TaxiGraph` starts at 1 so node ID 0 is a permanent "not set" sentinel — `TaxiGuidanceManager` uses `_destinationNodeId = 0` as the cleared-route marker, and a zero-based node ID would collide with that. Motivating defect: fs2024 navdata at GCLP models taxiway S5 as an isolated 13-node island (no graph connection to any other taxiway at either terminus). A pilot touching down on 03L near S5 had the start-node picker snap to an S5 node, and A* couldn't reach R9R (in the main 1075-node component) — the pilot heard "Could not calculate a route to the destination." and got silence during rollout. With the component filter the picker skips S5 and selects an R3 node ~187 m ahead instead. Applies to every `LoadRoute` caller, not just landing-exit, so the gate-to-runway taxi path at the same airports is protected too.
- **Taxiway exit/intersection picking uses GRAPH distance, not Euclidean.** `TaxiRouter.FindNearestNodeOnTaxiwayToTarget` and `TaxiRouter.FindBestIntersection` both score candidate nodes via `ComputeGraphDistancesFrom(destinationId)` — a Dijkstra single-source shortest-paths run from the destination. Picking by straight-line distance silently fails when the closer-by-crow-flies node is a navdata graph dead-end: any path forward from a dead-end must round-trip back through its only neighbour, which is always worse than picking the other-end node. Original bug: KDEN gate A 60 recalc with `sequence=["M4"]` — M4's northern endpoint sits 1.56 km from the gate vs 2.13 km for the southern endpoint, so the Euclidean picker chose the northern endpoint, but the unconstrained final-leg A* then had to walk 540 m back south on M4 and loop 2.4 km around the airport to reach the gate. Graph distance correctly prices the round-trip into the dead-end's cost and picks the southern endpoint instead. The destination-side Dijkstra is hoisted to `FindConstrainedPath` (computed once per route build) and threaded through both helpers via an optional `precomputedDistFromTarget` / `distFromFinalDest` parameter — saves the Dijkstra-from-endNode runs on every taxiway-transition step. Euclidean is retained as a defensive last-resort fallback in `FindNearestNodeOnTaxiwayToTarget` only when Dijkstra finds no candidate (would only happen on a malformed graph where the ComponentId filter missed a split); `FindBestIntersection` does NOT have a Euclidean fallback — returning `-1` when every candidate is graph-unreachable is intentional (the old Euclidean code returned candidates A* couldn't traverse, producing silently wrong routes), and the callers' `-1` path (FindRunwayBridge / shortest-path) is the correct recovery.
- **Post-recalc sanity gate has TWO independent indicators.** `TaxiGuidanceManager.TryRecalculateRoute` rejects a recalculated route when EITHER (a) it's dramatically longer than what's left of the current route (`newRoute.TotalDistanceMeters > oldRemaining * RECALC_LENGTH_BLOWUP_RATIO + RECALC_LENGTH_BLOWUP_PAD_M`, i.e. >2× + 500 m) OR (b) its first segment heads >`RECALC_BACKWARDS_DELTA_DEG` (120°) away from the straight-line bearing from the aircraft to the destination. Either condition firing keeps the old route and announces *"Off route. Could not follow clearance. <reason>. Continuing on original route."* The previous gate only fired when the constrained search itself fell back to shortest path (`ConstrainedFallbackReason != null`); a constrained search that "succeeded" but produced a dead-end-backtrack route (KDEN: 48 segments, ~2960 m, when remaining was ~200 m) slipped through. Indicator (a) catches length-extreme cases; indicator (b) catches the dead-end backtrack signature regardless of total length. Both are needed: AND-ing them would let "long AND going forward" recoveries through, OR-ing them is the correct semantic. Thresholds are named constants near the top of the class so they're discoverable + tunable.
- **Landing-exit fallback when initial `LoadRoute` fails: full exit-geometry rollout, not runway-end countdown.** When `LandingExitPlanner.ActivateGuidance` cannot route from touchdown to the chosen exit (typically because the taxi graph is disconnected and the exit's connected component has no nodes near the touchdown zone), it calls `TaxiGuidanceManager.BeginLandingRolloutNoGraph(exit, runwayHeadingTrue, runway, allExits, lat, lon, settings)` to enter `LandingRollout` state with the exit set as the geometric target. The rollout's per-frame logic — distance callouts (1500 / 900 / 500 ft), steering tone, overshoot detection, undershoot retargeting — all use exit geometry directly, NOT the route, so they work without one. At handoff time (turnBegun / exitedLaterally / alignedWithExit), `UpdateLandingRollout` calls `LoadRoute` from the live aircraft position which by then IS in the exit's component, so the re-route succeeds and normal taxi guidance follows. **`BeginLandingRolloutNoGraph` is implicitly coupled to a prior `LoadRoute` call** — it reads `_graph`, `_dataProvider`, and `_icao` for the subsequent handoff re-route, and the precondition check at the top of the method **logs a `RolloutDiag` warning if any is null but does NOT bail** (geometry-driven callouts and tone still work without them, but the eventual handoff re-route will fail). Always call from a code path where a prior `LoadRoute` (even one that failed at route construction) has populated those fields. **`BeginLandingRolloutNoGraph` defensively nulls `_route` at entry.** The handoff-failure fallback in `UpdateLandingRollout` (`!handoffRerouted && _route == null` → *"Exit reached. Route unavailable. … use the taxi planner."* + `StopGuidance`) requires `_route` to be null on the NoGraph path. In the normal flow this holds because `OnTakeoffAssistActiveChanged` calls `taxiGuidanceManager.StopGuidance()` when TakeoffAssist activates at departure (`MainForm.cs:2785`). A pilot who hand-flies the departure without TakeoffAssist would otherwise carry the stale gate-to-runway `_route` across the flight — and on a NoGraph landing whose handoff re-route also fails, `FindNearestSegmentIndexFullRoute` would silently drive the steering tone against the stale departure-airport segments. `BeginLandingRolloutNoGraph` nulls `_route` defensively at entry to guarantee the invariant regardless of the takeoff path. **Runway-end countdown is the secondary fallback** — entered only when every downfield exit has failed to route (see `RetargetLandingExit` cascade below). `_activatedThisLanding` is set to `true` so the fallback path is final for this landing; the planner doesn't retry on subsequent oleo bounces.
- **`RetargetLandingExit` cascades through downfield exits on `LoadRoute` failure.** When the initial retarget target's route cannot be built, the method walks `NextDownfieldExit` (first downfield exit beyond `ROLLOUT_OVERSHOOT_FT` with `ExitAngleDegrees ≤ 90°`) and retries `LoadRoute` against each successive candidate. The override-announcement is dropped when falling forward to a later exit (it described the originally requested target); the generic *"Missed X. Retargeting Y, Z feet ahead."* message is used instead. Only when EVERY downfield exit has failed does the method announce *"Missed X. No reachable exit remaining."* and call `EnterRunwayEndCountdown()`. Motivating defect: YSSY 16R retarget where one bad LoadRoute (degenerate target near the runway end) used to drop straight into runway-end countdown despite good earlier exits still being routable. **Cascade is state-safe** — `LoadRoute` mutates fields at the top (e.g., `_dataProvider`, `_destinationName`, `_icao`) but those are idempotent overwrites, and `_route` is set only AFTER the `route == null` check, so failed iterations leave `_route` untouched. Each iteration is independent. **Cosmetic edge case**: `prevName` is captured once at function entry from the old `_rolloutExit`. When an *undershoot* call (target earlier than `_rolloutExit`) cascades forward through `NextDownfieldExit` and happens to settle on the original `_rolloutExit` (e.g. no intermediate exit qualifies), both `prevName` and `newName` name the same exit and the announcement reads *"Missed taxiway X. Retargeting taxiway X, … feet ahead."* The steering tone and routing target are correct; only the wording is awkward. **Undershoot scan now requires a speed-proportional minimum lead**: `Math.Max(ROLLOUT_UNDERSHOOT_MIN_LEAD_FT = 200, gs · ROLLOUT_UNDERSHOOT_LEAD_PER_KT_FT = 11)`. Previously the scan picked whatever exit was nearest within `ROLLOUT_UNDERSHOOT_RANGE_FT = 1000`, which at YSSY 16R retargeted to taxiway L just 79 ft ahead at 52 kt — impossible to make — and then cascaded to a false "no exit remaining". **Implicit coupling**: at `gs ≥ 91 kt`, the min-lead floor (`91 · 11 = 1001 ft`) exceeds the scan range (1000 ft), so undershoot retargeting effectively no-ops. This is intentional (90 kt is the high-speed-exit ceiling, beyond which any retarget is unsafe anyway) but it's coupled across two unrelated-looking constants — the inline comment near `ROLLOUT_UNDERSHOOT_LEAD_PER_KT_FT` documents the interaction; keep both constants in sync if either changes.
- **900 ft RETIL-analog callout (high-speed exits only).** `UpdateLandingRollout` fires *"`<exit name>`, 900 feet."* between 500 and 900 ft from a high-speed exit, gated by `_rolloutApproach900Announced` and `ExitType == "High-speed"`. Analogous to the first Runway Exit Taxiway Indicator Light (RETIL) flash at ~984 ft — sighted pilots see the lights here, blind pilots get the equivalent verbal cue before the 500 ft "prepare to turn" window. **The 1500 ft callout's lower bound was tightened from 500 → 900 ft** so the two callouts don't overlap. Normal/End exits still get only the 1500 ft and 500 ft callouts; the 900 ft cue is reserved for high-speed exits because that's where RETIL semantics apply and Normal exits' 500 ft "prepare to turn, taxiway X" is already sufficient. **`_rolloutApproach900Announced` must be reset in all four sites that reset the other rollout-announce latches**: `BeginLandingRollout`, `BeginLandingRolloutNoGraph`, `EnterRunwayEndCountdown`, `StopGuidance`. RetargetLandingExit's success-on-cascade path also resets it so the new target's 900 ft callout can re-fire.
- **`GroundTrafficMonitor.SuppressCheck` — pluggable alert silencer.** Public `Func<bool>? SuppressCheck` predicate on `Services/GroundTrafficMonitor.cs`; when non-null and returning `true`, the 3-second poll tick early-returns without alerting. `MainForm.InitializeManagers` wires `groundTrafficMonitor.SuppressCheck = () => takeoffAssistManager.IsActive || taxiGuidanceManager.State == TaxiGuidanceState.Inactive;` — traffic callouts are silenced during the takeoff roll (where the pilot's hands are full on rudder + throttle and a traffic alert can't be acted on) AND whenever Taxi Guidance has no route engaged (no actionable context for the alert; the pilot is parked at the gate, mid-config, or post-stop). Predicate is read via `SuppressCheck?.Invoke()` which is safe under property-read-then-invoke even under interleaving (C# captures the property into a local first). When adding other suppression contexts (e.g. flare phase, hand-fly), prefer chaining additional predicates over adding more boolean state to the monitor. **Hotkey summary (`Alt+G` / `GetNearestTrafficSummary`) is intentionally NOT gated** — it's a manual lookup outside the `OnTick` polling loop and remains available at all times so the pilot can query nearby traffic on demand even when not under guidance. **Forward-arc filter (`FORWARD_ARC_DEG = 120°`) gates Caution and Warning, not Awareness.** "Slow down" and "Stop, traffic very close" fire only for traffic within ±120° of the nose; behind-arc threats are not actionable by braking. Awareness pings ("Traffic, behind, X feet") fire in all directions so the pilot retains passive awareness — the hotkey summary covers behind-arc details on demand.
- **`TaxiAssistForm` aircraft-position freshness.** OnCalculateClicked refreshes `_aircraftLat/Lon/Heading` from `_simConnectManager.LastKnownPosition` immediately before route construction. Without this, the route starts from wherever the aircraft was when the FORM was opened — typically a pre-pushback gate position — and the post-pushback aircraft is already off-route from frame one, triggering the 3-second off-route detector and an immediate recalc. The form's `_simConnectManager` is optional (defaults null for callers that don't have one) but MainForm always passes it. `LastKnownPosition` is updated by every position-bearing SimConnect path (visual guidance, hand-fly, etc.) so it's nearly always within a frame of truth, even when the taxi-specific position monitor isn't active yet.
- **Weather Radar source selection (ActiveSky vs MSFS) — silent fallback.** `Services/ActiveSkyClient.cs` probes candidate HTTP ports (19285 default, 19286, 19287) **in parallel** with a 1.2-second per-port `CancellationToken` timeout against `/ActiveSky/API/GetMode`. First successful 2xx wins; cached port is tried alone on subsequent refreshes (instant). Worst-case detection time when AS is missing: ~1.2 s, not n×timeout. **Important: do NOT scan `%APPDATA%\HiFi\` for a settings-file port** — AS keeps gigabytes of weather logs/history in subdirectories and recursive `Directory.EnumerateFiles` there blocks the UI thread for many seconds (this WAS tried and pulled out). When AS is detected, `WeatherRadarForm.FetchAmbientAsync` pulls ambient/surface conditions from `/GetWeatherAreaJson?stations=` and the position-specific METAR from `/GetCurrentConditions`. **API doc gotcha**: despite the doc, `/GetCurrentConditions` returns a METAR-style ASCII string (`@POS 070835Z 14707KT 9999 -RA FEW311 29/23 Q1006 RMK ADVANCED INTERPOLATION`), NOT JSON. `GetWeatherAreaJson` with an empty stations list returns the documented JSON ambient/surface block; the empty-stations response also includes a `"NULL RMK NOT FOUND"` placeholder METAR which we filter out at parse time. Fallback is the existing SimConnect `AMBIENT_*` SimVar path. **Why prefer AS**: under ActiveSky, SimConnect's `AMBIENT_PRECIP_STATE` bitmask sticks on snow, `AMBIENT_IN_CLOUD` flickers, and wind values lag MSFS's interpolation. **In-cloud** is still read from SimConnect even under AS — MSFS knows where it renders clouds regardless of who set them. **Precipitation** is parsed first from the position METAR (`/GetCurrentConditions`) using a small WMO/ICAO weather-token decoder, then closest-station METAR from JSON, then SimConnect bitmask as last resort. **METAR-says-no-precip = "None", not fallthrough** — only an entirely missing METAR triggers the next source. **Turbulence**: AS reports a 1-100 scale that sits at ~25 in calm conditions (atmospheric baseline). We bucket into FAA AIM 7-1-23 categories (`light` / `moderate` / `severe` / `extreme`) and **hide the line entirely when ≤25** so users don't see "25/100" alarmingly when nothing is happening. **Visibility shown in both km and statute miles** in both source paths so it's useful regardless of US vs ICAO convention. **Silent fallback by design**: the status line shows only `Last updated: HH:mm:ss` — no "weather source: X" or "AS: detected on port Y" diagnostics. Users without AS see the same form they always saw; users with AS get the richer fields (surface wind+gust, ceiling, QNH, turbulence) automatically.
- **ActiveSky weather-update auto-announce.** `Services/ActiveSkyWeatherMonitor.cs` polls every 60 s on a `System.Windows.Forms.Timer` and announces only when AS has actually pulled fresh weather data. **Cadence signal: JSON `TimeStamp` field from `/GetWeatherAreaJson` (Unix epoch).** When AS refreshes at its configured interval (5 / 10 / 15 min), TimeStamp advances; when the user is sitting still and AS hasn't refreshed, it doesn't. Sanity guard: also require the normalized METAR content to differ — if AS turns out to update TimeStamp on every API call (request-time semantics rather than data-freshness), the content guard prevents per-poll announcements. Fallback when TimeStamp is 0/missing: pure normalized-METAR-content comparison (visibility / clouds / weather / CAVOK; strip wind / temp / pressure / timestamp because AS interpolates those continuously). **Announcement format** (full METAR decode, screen-reader friendly): `"Active sky weather updated. Decoded weather at <ICAO>. Wind: 123 at 4 knots[, gusting 15][, varying between 100 and 140]. Visibility: 10 kilometres or more. Clouds: Few at 1,500 feet, broken at 3,000 feet, overcast at 5,000 feet. Precipitation: None. Temperature: 20. Dew point: 10. Altimeter: 1013 (29.92 inches)."` Cloud groups, weather phenomena, temperature/dew, and altimeter are decoded directly from the METAR (the JSON only gives a single ceiling and surface fields). Closest-station METAR (`/GetClosestStationWeather`) supplies the airport ICAO label; if that endpoint returns nothing, fall back to the position METAR labelled "your position". Wind/surface vis/temp/QNH from JSON are used only when the METAR fails to parse those fields. **Silent for non-AS users**: each tick that fails to detect AS produces no announcement and no error. **First-poll silence**: baseline established without announcement; same on AS-came-back after a drop. Started unconditionally in `MainForm` constructor; disposed in cleanup. **Don't move the WMO/ICAO weather-token decoder out of `WeatherRadarForm.ParsePrecipFromMetar`** — the monitor has its own copy in `WeatherRadarFormPrecipShim` to avoid making the form helper public; if you change one, change both.
- **METAR Report form (Output Shift+M) — dual VATSIM + ActiveSky display, silent when AS absent.** `Forms/METARReportForm.cs` starts compact (500×400, original VATSIM-only layout). On `Load`, async-detects AS via `_activeSky.IsRunningAsync()`. If AS is detected, the AS METAR section becomes visible (TextBox below VATSIM one), the form grows to 500×580, and the close button shifts down. If AS isn't detected, the form stays compact and the user sees the original VATSIM-only experience — no "(ActiveSky not running)" status text or other indicator. On Enter in the ICAO field, both METARs are fetched in parallel (`VATSIMService.GetMETARAsync` + `_activeSky.GetMetarAsync`) when AS is up; only VATSIM otherwise. AS textbox `Visible = false` initially (so `TabStop` skips it automatically when hidden). Tab order is ICAO → VATSIM METAR → AS METAR (skipped if hidden) → Close.
- **Cold Temperature Altitude Correction (Output Alt+T) — offline calculator, aircraft-agnostic.** `Forms/ColdTemperatureCorrectionForm.cs` re-implements the FlyByWire EFB Performance-page `TemperatureCorrectionWidget` (which is a canvas/SimpleInput page a screen reader can't operate) as a plain accessible dialog. In cold air the pressure altimeter OVER-reads, so published minimum altitudes must be corrected UPWARD to keep obstacle clearance. Inputs: aerodrome field elevation (ft), reported temperature (°C), and a multiline list of published altitudes (one per line). Output: each "Published X ft → corrected Y ft (add Z)" in a read-only results box; a single altitude is also spoken via `AnnounceImmediate`. **Math is the EUROCONTROL Doc-2940 formula transcribed VERBATIM from the FBW source** (`ColdTemperatureCorrectionForm.CorrectedAltitude`, incl. the redundant `- fieldElevation + fieldElevation` term, and round-UP to nearest 10 ft) so the blind pilot gets the IDENTICAL number the sighted EFB gives — warm temps return the published altitude unchanged (never corrected downward). PURE offline arithmetic — no SimVars, no Coherent, no aircraft required, works in planning on the ground on EVERY aircraft. Wired globally as output-mode hotkey **Ctrl+Shift+T** (id 9250, in the offline-actions set; was Alt+T but rebound — Alt+letter global chords get swallowed by the sim/Windows menu handling, Ctrl+Shift is reliable) and listed in all 6 hotkey guides. Cross-checked: −30°C/3000 ft field-0 → +570 ft; +15°C → +0 ft; −40°C/5000 ft → +1210 ft.
- **Heading-independent start-node selection when a taxiway sequence is given.** `LoadRoute` first tries `_graph.FindNearestNodeOnTaxiway(lat, lon, taxiwaySequence[0])` and falls back to the heading-aware `FindNearestNodeInDirection` only if no node on the requested first taxiway exists nearby. Why: post-pushback the aircraft can be pointing 180° away from where the first taxiway is (e.g., ATC told them to face NE for pushback, but the cleared taxi route runs SW). The heading-aware fallback would pick an apron node "ahead" of the aircraft instead of the requested taxiway, the route's approach segments would diverge from the aircraft's heading, and the off-route detector would fire on the first frame of taxi. Snapping directly to the user's requested first taxiway makes the constrained route honor the clearance regardless of pushback orientation; the pilot will turn after pushback, and the lineup tone guides them onto the first segment naturally.
- **ILS spatial+heading fallback for orphaned fs2024 rows.** The fs2024 vanilla navdata extraction has 213 ILS rows where `loc_airport_ident`, `loc_runway_name`, AND `loc_runway_end_id` are all NULL/empty — the ILS row itself is correct (right ident, frequency, location, heading) but the join columns weren't populated by navdatareader. KPHX, KORD, and several other major airports are affected (KPHX has 5 such orphans including 07R). fs2020 has zero orphans. `LittleNavMapProvider.GetILSForRunway` uses the direct `loc_airport_ident = ICAO AND loc_runway_name = name` query as the fast path; on miss it falls through to `GetILSForRunwayFallback` which: (a) looks up the runway end's threshold lat/lon and heading, (b) searches unlinked ILS rows within a 0.1° (~11 km) bounding box of the airport whose `loc_heading` is within ±5° of the runway heading (with ±180° wrap handling), (c) picks the closest by squared-distance to the threshold. Localizer antennas sit on the runway centerline beyond the far end so closest-by-distance matching is unambiguous. Wired into both ILS code paths: `GetILSForRunway` (used by ILS-guidance lookup) and `CreateRunwayFromReader` (which sets `Runway.ILSFreq/ILSHeading` from `runway_end.ils_ident`, also empty for KPHX 07R in fs2024). For fs2020 users this is a no-op; for fs2024 users it re-links the orphans at query time without requiring a navdata rebuild. `ReadILSFromReader` is shared between fast and fallback paths so the projection stays consistent.
- **DB operational-flag filtering — broad scenery compatibility.** `Database/Models/Runway.cs` has `IsClosed`, `IsLanding`, `IsTakeoff` flags (defaults: open / can-land / can-takeoff — PERMISSIVE). Read from `runway_end.has_closed_markings` / `is_landing` / `is_takeoff` via `LittleNavMapProvider.SafeReadBool(reader, columnName, defaultValue)` — handles missing column / NULL / int-as-bool gracefully. **TaxiAssistForm filters its destination dropdown to `!IsClosed`; LandingExitForm filters to `!IsClosed && IsLanding`.** Note the asymmetry: TaxiAssistForm intentionally does NOT filter by `IsTakeoff` — some third-party sceneries incorrectly mark runways with `is_takeoff=false` (a Navigraph/scenery data quality issue, not a real-world status), which caused those runways to vanish from the taxi-destination dropdown despite being perfectly usable for departure. Trusting `IsLanding` for the landing-exit picker is safer because the cost of routing to a landing-prohibited runway is much higher (live arrival inbound). Sparse DBs (most navdatareader builds) populate every row permissively → no behavior change. Rich DBs (third-party scenery, some Navigraph merges) → automatic filtering by `IsClosed` only on the taxi side. When adding new DB-backed fields that may not exist on every build, ALWAYS use `SafeReadBool` (or a similar safe-read helper) with a permissive default — do NOT use `Convert.ToInt32(reader["col"])` directly.
- **Per-row "Hold short of runway" picker** in `TaxiAssistForm` (mnemonic `Alt+O` — cycles across the first row + every dynamic row). `TaxiGuidanceManager.LoadRoute` accepts `Dictionary<int, string>? userRunwayHoldShorts` mapping taxiway-sequence index → runway designator; `ApplyUserRunwayHoldShorts` resolves the picked runway to a `TaxiGraph.RunwayCenterline` (matching against EITHER reciprocal designator — "10R" and "28L" name the same pavement), then scans forward from the START of the matching run of segments tagged with that taxiway for the first endpoint within the runway's half-width tolerance, and tags the segment immediately before as a hold-short. Runs BEFORE auto-detection (`InsertRunwayCrossingHoldShorts`) so the user-set `HoldShortRunway` label wins where both fire on the same segment. If the route doesn't cross the requested runway anywhere from the named taxiway's first segment onward, the method returns a warning string that's appended to the route summary announcement — the route still loads. **Scan must start at the FIRST run of segments tagged with the named taxiway, not the LAST.** At airports where the same-named taxiway continues across a runway crossing (e.g. KSFO D crosses 10R/28L mid-way and keeps the name "D" on both sides), the LAST D segment is already past the runway and the forward scan finds nothing — the user's correct "hold short 10R" pick gets silently rejected as "route does not cross 10R" even though auto-detect tags the within-D crossing correctly. **Geometry test must be reciprocal-aware.** Comparing `WhichRunwayContains` (which returns the closer threshold's designator) against the user's typed designator misses crossings closer to the opposite end of the same runway. Resolving to the `RunwayCenterline` once and testing perpendicular distance directly against its endpoints sidesteps both name-pair issues. **Duplicate-taxiway sequences** ("via N, hold short 15R, N, hold short 22R, N") use `priorOccurrences` (count of the same name earlier in `taxiwaySequence`) to pick the correct run — each sequence entry binds to a distinct maximal contiguous block of route segments with that `TaxiwayName`. The auto-detector remains the primary mechanism (covers most ATC clearances since FAA mandates hold-short of every crossed runway); the explicit picker is for confirmation and rare clearance/route mismatches.
- **Constrained-router runway bridge.** ATC clearances commonly contain consecutive taxiways separated by a runway crossing — e.g., *"K14 hold short 30L M17"* at OMDB. K14 ends at the 30L hold-short on its side; M17 starts at the 30L hold-short on the opposite side. They share NO graph node, so the previous `FindBestIntersection` failure path bailed to whole-route shortest path, ditching the user's clearance entirely. `TaxiRouter.FindRunwayBridge(currentTaxiway, nextTaxiway)` finds the closest pair of nodes between them within `MAX_BRIDGE_METERS = 200`; on success the constrained search routes along the current taxiway to its exit, free-A*-bridges across the runway, and resumes the constrained sequence at the entry on the next taxiway. The 200 m cap prevents silent half-airport jumps when an ATC clearance is genuinely wrong (in those cases, fall back to shortest path with a clear log line). Applied at both the step-1 (first→second taxiway) and the inner-loop (i→i+1) intersection lookups.
- **TaxiSteeringTone pulse-state reset.** `_pulseActive` is reset to `false` in both `Start()` and `Stop()`. Without this, a previous lineup session that ended with `SetPulse(true)` would leak its pulse state into the next route — first Taxiing-phase `UpdateHeadingError` call uses the width-scaled overload, which doesn't touch `_pulseActive`, so the inherited true would pulse the taxiing tone at 3 Hz. Always reset audio-modulation state on start/stop boundaries — don't trust caller-side cleanup.
- **TaxiSteeringTone volume refresh every sounding frame.** `SetTone` always calls `_toneGenerator.UpdateVolume(EffectiveVolume())` while sounding, regardless of `_pulseActive`. The previous "only refresh in pulse mode" optimization left the tone stuck at zero volume during a pulse→continuous transition: when the user is stopped-misaligned (pulse fires) and then starts moving, `SetPulse(false)` is called, but if the last pulse cycle had set the volume to 0 (silent half), the next frame in continuous mode skipped UpdateVolume and the tone stayed silent until something else triggered a state change (oversteer / going silent / Pause). Always refreshing the volume on every sounding frame is cheap (one float assign per ~30 Hz tick) and removes the entire class of bug.
- **Verbal turn direction is computed from aircraft heading, NOT route's static `TurnDirection`.** `ComputeTurnVerbalFromHeading(targetBearing, aircraftHeadingTrue)` derives the spoken "left / slight right / continue" from the angular difference between the aircraft's current true heading and the next segment's bearing — same input the steering tone uses for its pan, so the two always agree. The route's pre-computed `TaxiRouteSegment.TurnDirection` is `nextSeg.bearing - currentSeg.bearing` and assumes the aircraft is exactly on-axis with the current segment. When the aircraft is off-axis (post-pushback rotation, after a wide turn, brief deviation, or starting at the gate before moving) the actual turn it must make to align with the next segment can be the OPPOSITE direction from the route's intent — and the static verbal cue contradicted the (correct) tone. All three spoken sites — advance notice, "now" callout, status query (`GetStatusAnnouncement`) — go through the helper. The `TurnDirection != "straight"` predicates stay on the static field (those just check whether there's *any* turn at the junction; that doesn't depend on aircraft heading).
- **Parking listing parity with the gate-teleport dialog.** `TaxiAssistForm` (parking destination) builds its dropdown from `IAirportDataProvider.GetParkingSpots(icao)` — the same data source `GateTeleportForm` uses — labelled with `ParkingSpot.ToString()` (e.g. `"P 21 - Ramp GA Large (Jetway)"`). Routing endpoint is the nearest graph node within `MAX_PARKING_TO_GRAPH_M = 100 m`; the parking spot's actual lat/lon is the lineup convergence target, matching `SimConnectManager.TeleportToParkingSpot`. Don't drive the listing off graph parking-tagged nodes — that silently drops parking spots whose lat/lon lacks a nearby graph node (common in third-party scenery whose taxi paths lag the parking layout).
- **Auto-inserted runway-crossing hold-shorts.** `TaxiGuidanceManager.InsertRunwayCrossingHoldShorts(route, destinationName)` runs after `LoadRoute` builds the route, AFTER `TruncateToHoldShort`. For every segment pair where the next segment's endpoint lies on a runway centerline (geometry via the existing `TaxiGraph.RunwayCenterlines`, half-width tolerance), it tags the current segment `IsHoldShortPoint = true` with `HoldShortRunway = "runway X"`. Skip the destination runway (already handled by `TruncateToHoldShort`) and skip duplicate consecutive same-runway tags. This implements FAA AIM 4-3-18 / ICAO Doc 4444: explicit hold-short and ATC continue at every runway crossing on the route. Don't disable this — VATSIM controllers expect it, and silently rolling across an active runway is a runway-incursion risk.
- **Crossings-based taxiway connectivity + full-airport fallback.** `TaxiGraph.GetConnectedTaxiwayNames(name)` BFS counts **named-taxiway crossings** (default `maxCrossings = 2`), not raw graph hops — walking along the seed taxiway and through unnamed connectors is free; only crossing into a different named taxiway consumes the budget. The previous hop-based 4-edge limit silently hid M1 from the M5 dropdown at KSFO (4–6 unnamed connectors physically lie between them) and similar patterns elsewhere. `TaxiAssistForm`'s "Add Taxiway" combo additionally lists every airport taxiway (via `GetAllTaxiwayNames()`) below the connected ones — the heuristic prioritizes the dropdown for the common case while the full list ensures the user can match any ATC-named taxiway even when the heuristic doesn't surface it. The constrained-path router and `FindRunwayBridge` resolve the actual route from any pair of selections. `GetReachableTaxiwayNames(name, maxCrossings)` is public for callers wanting a different budget.
- **Duplicate taxiways in the entered sequence are intentional.** ATC clearances commonly re-use a taxiway across a runway crossing (e.g., *"via C, hold short 04L, C"* at KBOS). `TaxiAssistForm.OnAddTaxiwayClicked` does not filter the dropdown by already-used taxiways — only the immediately-previous one is hidden, to catch accidental no-op double-picks. The router handles consecutive duplicates as a benign no-op step: `FindBestIntersection` resolves to the current node and the `currentNode == targetNode && !bridgedAcrossRunway` short-circuit at `TaxiRouter.cs` skips the redundant A* pass. The per-row user hold-short is sequence-index-keyed, so a hold-short on the first occurrence still tags the correct segment via `ApplyUserRunwayHoldShorts`. Do NOT add an "already used" filter back to the dropdown — it would break the KBOS-style clearance pattern and any other airport with the same topology. The immediately-previous taxiway is hidden from the dropdown ONLY when the previous slot has no hold-short configured — neither the row's "Hold short" checkbox nor a runway selected in its "Hold short of runway" combo. With a hold-short set, the same-taxiway duplicate is a legitimate "taxi to hold line, resume on far side of runway crossing" clearance (e.g., KBOS *"K, B, N, hold short 15R, N, hold short 22R, N"*) and must remain available. Do NOT restore the unconditional previous-taxiway exclusion — it blocks the very clearance pattern this code was written to handle.
- **Runway destination lineup uses the `start` table, not `runway_end`.** `TaxiAssistForm.PopulateDestinations` looks up each runway's lineup point via `IAirportDataProvider.GetRunwayStarts(icao)` and matches by `RunwayID`. `Runway.StartLat/StartLon` (sourced from `runway_end.lonx/laty`) is the **physical pavement edge** — it's hundreds of meters off the lineup point for runways with a displaced threshold (e.g., KLAS 26R has a 1407 ft displacement, putting the actual lineup point ~429 m west of the stored pavement-end coordinate). Anchoring the route destination AND the `_destinationThresholdMap` entry on the start-table position keeps taxi-lineup centerline math and `TakeoffAssistManager`'s centerline math (which reads `TaxiGraph.RunwayCenterlines`, also built from the start table) referencing the same physical coordinates. Fall back to `Runway.StartLat/StartLon` only when the start table has no entry for the runway name. **Do NOT revert to using `Runway.StartLat/StartLon` directly** — it silently routes the aircraft to a graph node hundreds of meters off the runway at displaced-threshold airports (the symptom: route ends on an adjacent taxiway and TakeoffAssist reports cross-track in the tens of thousands of feet).
- **Ground-speed announcer is mode-independent (every on-ground phase), not taxi-only.** Lives in `Services/GroundSpeedAnnouncer.cs`, owned by `MainForm`, fed by the always-on `GROUND_VELOCITY` continuous base variable (registered in `BaseAircraftDefinition.GetBaseVariables`). `MainForm.HandleSpecialAnnouncements` routes `GROUND_VELOCITY` events to `groundSpeedAnnouncer.ProcessGroundSpeed(value, _lastOnGround)` and returns true (suppressing the generic "value changed" announcement). It used to live inside `TaxiGuidanceManager.UpdatePosition` — which only ran while taxi guidance was active, so callouts stopped the instant takeoff assist took over or after touchdown before taxi guidance re-engaged. **Do not move it back into a per-mode manager.** **On-ground only**: `ProcessGroundSpeed` early-returns when `onGround` is false — GS callouts cover taxi, the takeoff roll, and the landing rollout, but are silent airborne. While airborne the bucket baseline is left FROZEN (not reset) so the first sample after touchdown announces the rollout speed immediately instead of spending a sample re-baselining. Controlled by `UserSettings.TaxiGuidanceGroundSpeedAnnounceInterval` (0=off, 5, 10) — the setting keeps that name to avoid a migration even though the behaviour is no longer taxi-scoped; it's surfaced in the Taxi Guidance Options dialog. Round-to-nearest-multiple via `Math.Round(gs / interval, AwayFromZero)` — 4/5/6 kt all read as "5 knots". Hysteresis: 0.5 kt margin past the rounding boundary before re-announcing — kills jitter at the midpoint. First on-ground sample establishes baseline silently. Goes through plain `Announce` (NOT `AnnounceImmediate`) so a fading "10 knots" callout doesn't displace the most recent actionable instruction in any feature's Repeat-Last buffer.
- **Tactical and safety-critical taxi announcements use `AnnounceImmediate`, not `Announce`.** `AnnounceInstruction` (the helper used for turns, hold-shorts, taxiway changes, lineup, arrival, and distance countdowns) calls `_announcer.AnnounceImmediate` internally — every tactical callout interrupts queued speech because the pilot needs the cue *now*, not after a fading "10 knots" GS callout finishes. The same applies to standalone safety callouts (speed warnings, runway-crossing alerts, off-route warnings, lineup-achieved, parking arrival). Two sites still use plain `_announcer.Announce`: (a) the `LoadRoute` route summary at start of guidance (informational, not time-critical), (b) the periodic GS announcer (must not displace the Repeat-Last buffer). When adding new taxi callouts, default to `AnnounceInstruction` / `AnnounceImmediate`; justify any plain `Announce` call explicitly.

Details: [docs/taxi-guidance.md](docs/taxi-guidance.md).

### Visual Landing Guidance (dual-tone)

**Audio rendering of PID output uses TWO `AudioToneGenerator` instances**, not one. Both always play. The DESIRED tone encodes the PID-commanded pitch (default 200–800 Hz over ±6°) and bank (stereo pan over ±5°); the CURRENT tone mirrors the same mapping against the aircraft's *actual* pitch/bank. Frequency match = correct pitch attitude (= correct VS on the 3° glideslope); pan match = correct bank. The pilot zero-beats the two by ear.

- **Visual guidance does NOT require HandFly mode.** It monitors its own attitude via `VISUAL_GUIDANCE_PITCH` and `VISUAL_GUIDANCE_BANK` events on the `VISUAL_GUIDANCE_DATA` callback (added to the struct alongside position / altitude / ground-track). Do NOT reintroduce a `!handFlyManager.IsActive` gate — that was the original design and pilots reported the three-tone overlap (HandFly's single tone + VG's two tones) made it impossible to tell which to follow.
- **HandFly tone is auto-muted while VG is active.** `MainForm.OnVisualGuidanceActiveChanged(true)` calls `handFlyManager.SuppressAudio()`; the `false` path calls `ResumeAudio()`. HandFly's announcements still fire if its feedback mode wants them — only the audio tone is muted. Do NOT remove this — VG's two tones use the same Hz/pan mapping as HandFly's tone, so playing all three together is acoustically incoherent.
- **The quick-access hotkey set (H, V, Q, S, D, B, P, A, F) is shared between HandFly and VG** via reference-counted registration. Both `RegisterHandFlyHotkeys` and `RegisterVisualGuidanceHotkeys` call `AcquireQuickAccessHotkeys` which increments a counter and registers any not-yet-held keys; the matching `Release` decrements and only unregisters when count hits zero. VG users get the SAME readout keys HandFly users do, because VG is hand-flying with extra guidance. Do NOT split the registration back into per-mode key sets — that's what created the double-register conflict the refactor fixed. The unified WM_HOTKEY dispatch (`if (handFlyHotkeysActive || visualGuidanceHotkeysActive)`) maps each key directly to its `HotkeyAction`; the action handlers self-gate where needed (e.g., `ReadTargetFPM` says "Visual guidance not active" when pressed in HandFly-only mode).
- **There is no single-tone mode for VG.** Both VG tones always run when visual guidance is active. Users who want the current tone less prominent should lower its volume in the Hand Fly Options dialog, not gate it off. Do NOT reintroduce a `VisualGuidanceCurrentToneEnabled` flag — the dual-tone IS the design.
- **Always route `cachedBank` through `VisualGuidanceManager.StandardBank(double)` before passing it to any tone API or computing a bank error.** SimConnect's `PLANE_BANK_DEGREES` is left-positive; `AudioToneGenerator` is right-positive. The PID's `desiredBank` output is already right-positive so it passes through raw — but `cachedBank` is the raw SimConnect value. `StandardBank` just returns `-simConnectBank`; its job is to make every call site self-documenting and obvious if the conversion is missing. `HandFlyManager.cs:155` does its own inline negation (different code path; left alone).
- **`VisualGuidanceManager.Initialize` is idempotent.** It calls `Stop()` first if either tone is still alive. Don't remove that guard; future callers might bypass `Toggle`'s strict on/off flow.
- **Tone Start() is deferred until the first ProcessUpdate.** `Initialize` instantiates both `AudioToneGenerator` instances and calls `Configure(...)` with the airframe's frequency mapping, but does NOT `Start()`. `StartTonesIfNeeded` inside `ProcessUpdate` kicks off WaveOut once real attitude commands exist, so the first audible note already reflects the airplane's state. Do not re-add a `Start()` call in Initialize; the deferral is what prevents the brief 500 Hz fused-tone glitch.
- **Start the current (follower) tone only if the desired tone started.** Without a reference, the follower plays a constant centre-pan tone forever — annoying and meaningless. `StartTonesIfNeeded` enforces this; don't reorder its try blocks.
- **Visual guidance auto-deactivates on the airborne→on-ground edge** in `MainForm.cs`'s `SIM_ON_GROUND` handler. From touchdown the landing-exit planner and taxi guidance take over the rollout and taxi audio respectively; running visual guidance alongside them would compete audibly. Manual activation on the ramp is still permitted (preflight test, etc.); only the edge transition triggers auto-deactivation. Do not gate this on GS or any other condition — landings of any speed should trigger it.
- **Aircraft-specific tone frequency / attitude range lives on `VisualGuidanceProfile`** (`ToneMinFrequencyHz`, `ToneMaxFrequencyHz`, `TonePitchRangeDeg`, `ToneBankRangeDeg`). Visual-guidance defaults are deliberately narrower than `AudioToneGenerator`'s native 10°/10°: pitch ±6° (50 Hz/° matching slope), bank ±5° (0.20 pan/°). The PMDG 777 widens `TonePitchRangeDeg` back to 10° (30 Hz/° — gentler beat at small errors during a stabilized approach). Override per airframe for aerobatic / fighter / glider envelopes that would saturate during normal manoeuvring.
- **Visual guidance has a hard-pan setting** (`VisualGuidanceHardPanTone`) mirroring the taxi/takeoff hard-pan options. Applied via `ApplyBank(tone, bank)` to BOTH tones in lockstep, so they still align when bank commands match. Snap deadband is ±1° standard convention.
- **Do not split lateral and vertical into separate tones.** The matching idiom relies on one oscillator per role (desired + current). Splitting would force the pilot to track four streams instead of two.
- **Default waveforms must differ between desired and current** (triangle + sine). Identical-waveform pure tones at the same frequency phase-cancel exactly at the matched state — the moment the pilot most needs them distinguishable.
- **Aircraft-specific tunables live on `IAircraftDefinition.GetVisualGuidanceProfile()`** — approach AoA, reference Vref, pitch-rate cap, bank-rate cap, tone frequency/attitude ranges, and the two glidepath-calibration biases below. A320 is the default; `PMDG777Definition` overrides for the heavy widebody (4.5° AoA, 2.0°/s pitch cap, 80 ft / 40 ft glidepath biases, 40 ft flare trigger, 4.5° flare target pitch, 10° tone pitch range). Do NOT bake aircraft-specific numbers back into `VisualGuidanceManager` as consts; that's the bug the profile was added to fix.
- **Glidepath altitude calibration — `GlideslopeAltitudeBiasFt` + `FlareAltitudeBiasFt` on `VisualGuidanceProfile`.** VG's altitude is the SimConnect reference datum (`PLANE ALTITUDE − threshold elevation`), and a bare `distance × tan(3°)` glidepath crosses the threshold at 0 ft — but a real ILS crosses at the ~50 ft reference datum height (TCH). Uncorrected, a coupled autoland reads as ~80 ft "high" on the 777 and VG's commanded vertical speed biases into excess descent. `GlideslopeAltitudeBiasFt` is **added to the ideal glidepath** (`idealAltitudeAGL = distance·tan(3°) + bias`) so VG's "on glideslope" matches the real ILS. `FlareAltitudeBiasFt` is **subtracted from the measured altitude** before the flare (30 ft) and touchdown (5 ft) phase thresholds, so the flare cue fires at true main-gear height instead of ~one bias too late (and so touchdown actually triggers — on a widebody the un-corrected datum never reaches 5 ft). The two are applied in **different code paths** (glideslope-error vs phase-detection) and were measured separately — do NOT collapse them into one shared constant. 777 values (80 / 40) are measured against a coupled autoland (the flare bias was bumped 20 → 30 → 40 across three test runs as each step moved the fire-point up by exactly 10 ft, confirming the real flare-attitude datum-vs-gear offset is 40 ft on the 777); A320 values (60 / 12) are estimates pending in-sim calibration. **`FlareTriggerWheelHeightFt`** is the true gear height at which the flare cue fires — per-aircraft because autoland and FCTM initiation heights differ (A320 = 30 ft default, 777 = 40 ft to match the autopilot's autoland). **`FlareTargetPitchDeg`** is the pitch commanded during the flare phase — per-aircraft because manufacturer flare attitudes differ (A320 default 6° per FCTM, 777 = 4.5° per Boeing FCTM's "2–3° pitch increase from approach attitude" guidance applied to the 777's ~1.5° approach pitch). **Every concrete aircraft declares its own `GetVisualGuidanceProfile()` override** — `FlyByWireA320Definition`, `FenixA320Definition`, and `PMDG777Definition` each spell out a full profile, so the math is explicitly keyed per airframe and FBW / Fenix glidepath biases can be calibrated independently of each other. The base `BaseAircraftDefinition` default is only a fallback for aircraft with no VG tuning. The Fenix A320's approach AoA / Vref / rate caps are the values validated in earlier flight testing — do not change them; only its glidepath biases are still estimates.
- **Glideslope angle is per-runway** (read from `ils.gs_pitch` in the navdata; cached on `Runway.GlideslopeAngleDeg`). Most ILSes are 3°, but London City is 5.5°, Aspen 6.59°, Innsbruck 3.8°, etc. — VG uses the published angle and falls back to 3° only when the runway has no ILS row, the DB build doesn't carry the angle, or the value falls outside the `[2°, 8°]` sanity band (defensive against corrupt navdata; Aspen at 6.59° is the steepest certified Cat I in the world). The angle feeds the ideal glidepath, the natural-descent-rate FPM target, the phase-capture deviation, and the nominal-pitch baseline — all four use the same per-runway value so they agree. The `MAX_DESCENT_RATE_FPM = -1500` safety clamp becomes **dynamic** (`min(-1500, natural × 1.3)`) so the legitimate per-runway descent profile is never clipped at steep approaches.
- **Nominal pitch baseline uses LIVE measured angle of attack**, not a static per-aircraft constant. `VisualGuidanceManager` subscribes to SimConnect `INCIDENCE ALPHA` via the existing `VISUAL_GUIDANCE_DATA` struct and uses the (EMA-smoothed, sanity-banded `[-5°, 20°]`) reading to compute `nominalPitch = -glideslopeAngleDeg + measuredAoaDeg`. Live AoA inherently encodes weight + flap + speed, so the nominal converges on whatever the airplane is actually trimmed for — no per-configuration look-up needed and the per-aircraft `TypicalApproachAoaDeg` is only a fallback for sim glitches or pre-first-sample frames. Smoothed AoA is preserved across out-of-band excursions so a single bad sample doesn't drop us back to the fallback.
- **Pitch PID has positive coefficients on `fpmError` / `fpmErrorRate`** (NOT the leading `-` that was historically there). Verified sign: with autothrust holding airspeed, MORE pitch ⇒ LESS descent. So negative `fpmError` (descending less than target ⇒ need more descent) calls for a NEGATIVE pitch correction ⇒ correction has the SAME sign as `fpmError`. The old `-fpmError * gain` produced wrong-direction guidance that was masked by autopilot tests (tight tracking ⇒ `fpmError ≈ 0` ⇒ the wrong-sign correction was negligible) and would have steered a manually-flying pilot away from the glideslope. Do NOT reintroduce the leading minus.
- **Threshold elevation is per-runway-end** (`runway_end.altitude`, cached on `Runway.ThresholdElevation`). At airports with sloped runways or different elevations between ends (KASE, LSZS, KSEA 16L/34R) this differs from the airport's published field elevation. Falls back to `airport.Altitude` only when the runway value is unknown.
- **PID math, phase machine, and lateral arc-capture logic are untouched** by the dual-tone work; the glidepath altitude calibration above is the one deliberate vertical-guidance change (a constant offset that shifts where "on glideslope" sits, not a gain/dynamics change). Failure mode of the new tone is a missing audible reference, never a wrong steering command — keep it that way.
- **Manual-query grace window.** When the pilot fires a readout hotkey (the H/V/Q/S/D/B/P/A/F set) while VG is active, `MainForm` calls `visualGuidanceManager.NotifyManualQuery()`, which suppresses the per-second bank-guidance and centerline-deviation callouts (both `AnnounceImmediate`) for 3 s so the readout isn't cut off mid-sentence. Only those two chatty callouts are suppressed — phase changes and distance callouts (rare one-shots) still fire. Skipped callouts lose nothing: both always reflect current state, so the next one carries fresh data. `IsManualReadoutAction` in `MainForm` lists the qualifying hotkey actions.

Details: [docs/visual-guidance.md](docs/visual-guidance.md).

### Multi-Aircraft Architecture

**Core interfaces:**
- **IAircraftDefinition** - Contract for all aircraft
- **BaseAircraftDefinition** - Recommended base class (provides hotkey routing, caching, helpers)
- **FlyByWireA320Definition** - Reference implementation

**Each aircraft defines:**
- `GetVariables()` - All simulator variables
- `GetPanelStructure()` - Section/panel hierarchy
- `BuildPanelControls()` - Panel-to-variables mapping (cached automatically by base class)
- `GetHotkeyVariableMap()` - Simple hotkey action → event name mappings
- `HandleHotkeyAction()` - Custom hotkey logic (optional override)

## Quick Reference

### Adding Panel Control
1. Add to aircraft's `GetVariables()` with `UpdateFrequency.OnRequest`
2. Add variable key to `BuildPanelControls()` under appropriate panel
3. Test - automatic registration and UI generation

### Adding Background Monitoring
1. Add to `GetVariables()` with `UpdateFrequency.Continuous` + `IsAnnounced = true`
2. Do NOT add to `BuildPanelControls()` - batched monitoring is automatic
3. Change detection and announcements are automatic (supports 1000 variables)

### Adding New Aircraft
1. Create class inheriting `BaseAircraftDefinition`
2. Override: `GetVariables()`, `GetPanelStructure()`, `BuildPanelControls()`
3. Add menu item in `MainForm.Designer.cs` + click handler
4. Add to `LoadAircraftFromCode()` switch statement
5. Use `FlyByWireA320Definition.cs` as template

### Variable Types
- **K:EVENT** - Standard MSFS events (via SimConnect TransmitClientEvent)
- **L:VARIABLE** - Local variables (reading aircraft state)
- **H:EVENT** - Hardware events (via MobiFlight WASM module)
- **PMDGVar** - PMDG SDK variables (read via Client Data Area broadcast)

### PMDG 777 Specific Patterns

**Switch control:** Use CDA (SetClientData) with direct position values for most switches.
- Two-position toggles: `SendPMDGEvent(eventName, eventId, targetPosition)` where targetPosition is 0 or 1
- Multi-position selectors: same, with the target position index
- Momentary buttons: `SendPMDGEvent(eventName, eventId, 1)` — parameter 1 = pressed, 0 = no-op
- Continuous knobs (brightness, temperature, EFIS baro/mins): **cannot be controlled via SDK** — do not add to panels
- **Fuel control levers:** Exception — use CDA with **inverted** parameter (1=Cutoff, 0=Run). See special case in HandleUIVariableSet.
- **Ground power switches (ELEC_ExtPwr):** Momentary push buttons — send parameter 1 regardless of target. See special case in HandleUIVariableSet.

**Radio frequencies and transponder:** Use standard SimConnect events (not PMDG SDK):
- `COM_STBY_RADIO_SET_HZ` / `COM2_STBY_RADIO_SET_HZ` for setting standby freqs
- `COM_STBY_RADIO_SWAP` / `COM2_RADIO_SWAP` for swapping active/standby
- `XPNDR_SET` for squawk code (BCD16 encoded)

**CDU array index convention:** The PMDG SDK uses `0=Captain(L), 1=F/O(R), 2=Observer(C)` for ALL crew-position arrays — including CDU data areas (`PMDG_777X_CDU_0/1/2`), `CDU_BrtKnob[3]`, and all `CDU_annun*[3]` fields. The CDU form dropdown is ordered Left/Center/Right (0/1/2), which does NOT match the SDK ordering. `PMDG777CDUForm` uses a `DataCDUIndex` computed property to remap dropdown index to SDK index (`1→2, 2→1`). The event prefix switch (`EVT_CDU_L_/C_/R_`) uses the raw dropdown index and is correct as-is.

**CDU interaction:** CDU buttons must send parameter 1 (pressed) via CDA; parameter 0 also registers as a press (not a release). Text entry sends one character at a time with 350ms delay; repeated characters need an extra 400ms for the CDU to distinguish separate presses. CDU display uses color and font-size data to detect toggle selections (non-white color or non-small font = selected, marked with `X`). Toggle detection only applies to rows with adjacent `<>` (mapped from 0xA1/0xA2 arrow symbols). Scratchpad announcements are suppressed during text entry and clearing (`_typingInProgress`/`_clearingInProgress` flags); `_previousScratchpad` is only updated when the announcement actually fires. CLR uses `_clearingInProgress` to suppress intermediate states and only announces "Cleared" once the scratchpad is empty.

**MCP dialogs:** Use `ValueInputForm` with `ToggleButtonDef` for mode toggles. Opened non-modal (`Show()`, not `ShowDialog()`) with `ShowCancelButton = false` so other windows remain accessible. Dialogs stay open after value entry (callback pattern). `MCP_IASBlank` indicates FMC-controlled speed. VS/FPA dialog uses `inputEnabledCheck` to gate input on mode engagement (`MCP_annunVS_FPA`). `EVT_MCP_VS_SET` requires VS mode to be engaged first ("VS window open").

**VS/FPA event naming (SDK names are misleading):** `EVT_MCP_VS_SWITCH` (69855) is the **engage/disengage** button. `EVT_MCP_VS_FPA_SWITCH` (69852) is the **VS↔FPA display mode toggle**. Confirmed by live sim testing — do not trust the SDK naming alone.

**Announcements:** Use `Announce()` (queued) in ProcessSimVarUpdate, `AnnounceImmediate()` only in HandleHotkeyAction. `IsAnnounced = true` is required for continuous monitoring registration. Suppress button push state (_Sw_Pushed) announcements via RenderAsButton check. Annunciator lights announce both on and off states. For variables needing cache but no auto-announcement, set `IsAnnounced = true` and return `true` from ProcessSimVarUpdate to suppress.

### PMDG 737-800 NG3 Specific Patterns

Details: [docs/pmdg-737.md](docs/pmdg-737.md). Key gotchas: two CDUs (no observer), no FPA mode, annunciator names differ from 777 (LVL_CHG / HDG_SEL / VOR_LOC), DU selectors have "reverse sequence for FO", fire handles need an active fire to test, the 737 EFB has full parity with the 777 (Dashboard / Preferences / Navdata / Performance / Ground Ops / W&B / Manuals) via the shared `PMDGEFBForm`, opened with Shift+T — the EFB app is byte-identical across all four 737 variants and the 777, so one shared bridge JS + `zzz-pmdg-efb-accessibility` Community package serves them all.

### PMDG EFB Bridge

The EFB (Electronic Flight Bag) tablet is made accessible via a JavaScript bridge injected through an MSFS mod package override.

**Architecture:** A standalone MSFS Community package (`zzz-pmdg-efb-accessibility`) overrides the EFB's `PMDGTabletCA.html` to load an additional JS script. The `zzz-` prefix ensures it loads after the PMDG package alphabetically, so our HTML takes precedence. The JS bridge communicates with the C# app via HTTP on `localhost:19777`.

**Key components:**
- **`EFBBridgeServer`** (`SimConnect/EFBBridgeServer.cs`) — HttpListener with `/ping`, `/state` (POST), `/commands` (GET) endpoints. JS pushes state, C# queues commands. Command queue capped at 50 entries; `HasPendingCommand()` enables deduplication. Auto-restarts listener on unexpected failures (5 retries, 2s delay). Start/Stop protected by lock. Fires `Error` event on server failures.
- **`EFBModPackageManager`** (`Patching/EFBModPackageManager.cs`) — Installs/updates/removes the mod package. Reads original PMDG HTML at install time (no PMDG IP in repo), appends bridge script tag with double-patch guard (checks for existing script tag before appending). Auto-updates bridge JS on app startup via `BridgeVersion` constant.
- **`pmdg-efb-accessibility-bridge.js`** (`Resources/`) — Runs inside MSFS Coherent GT. Hooks into EFB's `MessageService.messaging_bus` EventBus. Must be Coherent GT compatible (no `AbortSignal.timeout`, top-level try-catch, `var` not `let/const`, no arrow functions, `.indexOf()` not `.includes()`). Critical state types are queued on POST failure and flushed on reconnection (max 20 pending, 3 retries per entry). `tryConnect` has a connecting guard to prevent concurrent attempts; `navigraphStateSent` flag prevents duplicate Navigraph state posts. **`click_by_id` accepts an optional `fallbackText` payload field** — when `getElementById` misses (PMDG renamed a DOM id), the bridge searches button-like elements (`button, .button, [role="button"], div[onclick], span[onclick]`) for one whose `textContent` contains the fallback string (case-insensitive). The `click_result` state reports `matchedBy: "id"` or `"text"`. Use this for any button whose human-visible label is stable across PMDG releases even if the id might drift.
- **`PMDGEFBForm`** (`Forms/PMDGEFB/`) — Accessible form shared across all PMDG 737 and 777 variants, hosting Dashboard, Preferences, Navdata, Performance, Ground Ops, Weights & Balance, Manuals, and a Display debug tab. Opened via Shift+T in input mode. Form title (`"PMDG 737 EFB"` / `"PMDG 777 EFB"`) and AccessibleName are set in the constructor from `currentAircraft.AircraftCode`. Shows connection status (always visible above tabs, announced on transitions). Buttons disable on click and re-enable on response or timeout. SimBrief fetch: 30s timeout. Navigraph auth: 60s timeout.

**JS bridge constraints (Coherent GT):**
- No `AbortSignal.timeout()` — use manual Promise-based timeout
- Top-level try-catch wrapping entire script — errors must never break the EFB
- `layout.json` in the mod package must have exact file sizes — MSFS validates these
- Sim must be restarted after mod package install/update for MSFS to load new files
- The JS file is copied while the sim is closed (sim locks files while running)

**Communication flow:**
- JS → C#: `POST /state` with `{type, data}` JSON (state updates, auth codes, SimBrief data). Failed POSTs for critical state types are queued and retried on reconnection.
- C# → JS: `GET /commands` polled every 500ms, returns JSON array of `{command, payload}`. Commands expire after 30s if not polled.
- Bridge connects on startup, retries every 5s if server unavailable. On reconnection, flushes pending states and re-sends Navigraph auth status.

### FlyByWire A380X Specific Patterns

`FlyByWireA380Definition` (code `FBW_A380`), branch `fly-by-wire-A380-integration`.

**No-injection MCDU/flyPad transport.** The A380X MFD/MCDU and flyPad EFB are read AND driven live through the MSFS Coherent GT remote debugger (`http://127.0.0.1:19999`, WebKit inspector, `Runtime.evaluate`) — NOT via a Community-folder mod or HTTP bridge. `SimConnect/CoherentDebuggerClient.cs` (MFD, resolves the view by title needle `A380X_MFD`) and `SimConnect/CoherentEFBClient.cs` (flyPad, title needle `- EFB`) both implement `IMcduBridge`, so `FBWA380MCDUForm`/`FBWA380EFBForm` are transport-agnostic. The in-page agents are `Resources/coherent-a380-agent.js` and `coherent-flypad-agent.js` — **ES5 only** (Coherent GT = Chromium 49: `var`, no arrow funcs, no `String.includes`, top-level try/catch). MFD page-ids shift each session → always resolve by TITLE. **No Community-folder injection, no HTML patching, no sim restart** — the debugger is the sole A380 transport (all injection code was removed; PMDG still uses its own `EFBBridgeServer`). **Developer Mode is NOT required** — verified empirically on MSFS 2024 (cold boot, Dev Mode off): port 19999 is opened by the sim itself and `/pagelist.json` + the WebSocket `Runtime.evaluate` inspector protocol both work, so the debugger transport runs with no Dev Mode and no dev toolbar.

**flyPad = WebView2 "browser mode".** `FBWA380EFBForm` renders the scraped flyPad elements as a real semantic HTML document inside an embedded **WebView2** (already a dep; PMDG EFB precedent), so the screen reader gets native browse mode (H headings, K links, arrow-by-line, real `<input>`s, `aria-live` page-change). NO http server — `NavigateToString(PageHtml)` once, then `ExecuteScriptAsync("window.__render(<json>)")` per update; the page posts click/set/refresh back via `chrome.webview.postMessage` → `WebMessageReceived` → `EnqueueCommand`. The native flat-list is a SILENT auto-fallback only if WebView2 init fails (no user toggle). **Click index gotcha:** the agent's `clickElement`/`setValue` look up the STAMPED `data-fbwa380-efb-idx`, which diverges from the list position (the list is sorted+deduped after stamping) — so `CoherentEFBClient` carries it as `items.N.aidx` and the form sends THAT, not the loop index. **flyPad div-buttons:** many action buttons (e.g. modal Cancel/Confirm) are styled `<div>`s with no `button`/`btn` token or role — the agent classifies a `hover:`-styled leaf with its own short text as a button (interactive flyPad elements carry Tailwind `hover:` classes; static text never does). Value inputs (PAX/KGS) have no label metadata → `labelFor` reads the field's parent unit text before falling back to the section heading.

**ND OANS / BTV (Ctrl+Shift+B).** The OANS (Onboard Airport Navigation System) control panel — BTV exit selection, airport/runway/exit/stand search, LDG SHIFT, BTV stop distance — lives on the **ND** Coherent view (`A380X_ND_1`), NOT the MFD, and has no setter L-vars. `SimConnect/CoherentNDClient.cs` (a focused clone of `CoherentEFBClient`, title needle `A380X_ND_1`) drives it via `Resources/coherent-oans-agent.js`, which scrapes the `.oans-control-panel` (built from the same `.mfd-*` widgets as the MCDU) into the SAME element shape the flyPad agent emits — so it **reuses `FBWA380EFBForm`** (the WebView2 browser view, parameterised with a window title + device noun) with no new form. The agent opens the panel via the ND map **"MAP DATA" context-menu item** (`openPanel`, called only on form open/refresh via `get_display_elements` so a routine poll never re-opens a panel the user closed), types via the real FBW InputField mechanism, and returns a "not available" notice until the OANS has a position (ADIRS aligned + at/near an airport). Arming BTV itself = the Autobrake selector (`A32NX_AUTOBRAKES_SELECTED_MODE` = BTV) in the pedestal panels.

**MFD navigation = click stable element id, NOT KCCU H-events.** FBW issue #9348 says the KCCU H-events are "practically unusable for external tools". The agent clicks the MFD page-selector menu item by its stable id `{CAPT|FO}_MFD_pageSelector{Tab}_{idx}` (the click fires even while the dropdown is collapsed); KCCU key is a cross-system fallback only. Current page is readable from SimVar `A380X_MFD_{L|R}_ACTIVE_PAGE` (string URI). All 20 pages are reachable from a **"Go to MFD page" combo** in `FBWA380MCDUForm` (`AllPages` table → `navigate_by_id`), the quick page buttons, or the MFD's own tabs. The scrape agent's `INTERACTIVE_SELECTOR`/`classify` also cover RadioButtonGroup (`.mfd-radio-button`, "(selected)"), in-page TopTabNavigator sub-tabs (`.mfd-top-tab-navigator-bar-element-outer`, "(active tab)" — e.g. PERF T.O/CLB/CRZ/DES/APPR/GA), SURV/ADS-C buttons, and context-menu items.

**MFD navigation is via the always-present HEADER, NOT an "INIT hub" — KEEP the page picker (#105).** Investigated the theory that "every MFD page is reachable from INIT" (true-ish on the A320/A330 MCDU via the physical page keys): it is **FALSE for the A380**. Source (`FmsHeader.tsx`) shows every FMS page carries a persistent header of 4 dropdown tabs — **ACTIVE** (F-PLN/PERF/FUEL&LOAD/WIND/INIT), **POSITION** (MONITOR/REPORT/NAVAIDS/IRS/GNSS/TIME), **SEC INDEX** (SEC 1/2/3), **DATA** (STATUS/WAYPOINT/NAVAID/ROUTE/AIRPORT/PRINTER) — plus the top system tabs (FMS/ATCCOM/SURV/FCU BKUP). INIT is just one item under ACTIVE; it has no links to other pages. **Sighted pilots navigate via this header, not through INIT.** Crucially, the MFD scrape does NOT capture the header tabs (verified live: 0 header-tab elements scraped though 40 `_MFD_pageSelector` DOM nodes exist — they're the navigateById click targets, outside the scraped content area), so the **"Go to MFD page" combo is currently the ONLY way a blind pilot can reach most pages** (besides the quick buttons + Ctrl+arrows). It is the accessible equivalent of the always-visible header — strictly better than expanding 4 dropdowns one item at a time. **Do NOT remove the page picker** — it would strand the user. (Confirmed: keep it.)

**ATC COM / CPDLC + D-ATIS — navigate by UIService URI, NOT page-selector id.** The page-selector ids (`{CAPT|FO}_MFD_pageSelector{Tab}_{idx}`) only exist while that *system's* header is mounted, so the FMS-header `navigateById` click can't reach ATCCOM from an FMS page. The reliable cross-system path is the MFD's own **UIService.navigateTo(uri)** — verified live to jump from `fms/data/status` straight to `atccom/msg-record`. The UIService isn't always on `fsInstrument` directly; `A.mfdUiService()` resolves it from `document.querySelector('a380x-mfd').fsInstrument.uiService` OR `…mfdCaptRef/mfdFoRef.instance.uiService` (the mounted side). `A.navigateUri(uri)` (agent) → `navigate_uri` command (`CoherentDebuggerClient`) → `SendNavigateUri` (`FBWA380MCDUForm`). The six ATCCOM pages are in `AllPages` with a non-empty `Uri` field (page-selector dispatch picks `navigate_uri` when `Uri` is set, else `navigate_by_id`); the **ATC COM button** (Alt+A) now fires `SendNavigateUri("atccom/connect")` instead of the old KCCU-only `navigate_by_id("",-1,"ATCCOM")`. URIs (from `AtccomHeader.tsx`): `atccom/connect`, `atccom/request`, `atccom/report-modify/position`, `atccom/msg-record`, `atccom/d-atis/list`, `atccom/emer`. **Live-verified all 6 navigate + scrape as clean DOM text:** CONNECT (NOTIFY TO ATC / ACTIVE+NEXT ATC / DISCONNECT ALL / ADS-C toggles), MSG RECORD (the CPDLC log — ALL MSG / MONITORED MSG, where uplinked clearance text reads when `A32NX_DCDU_ATC_MSG_WAITING` fires), and D-ATIS (DEP/ARR + UPDATE/PRINT) are functional; REQUEST / REPORT & NOTIFY / EMER read "ERROR 404 NOT FOUND" — **WIP in the FBW dev build**, not our bug (will surface once FBW implements them; MSFSBA already navigates to them). The MSG RECORD page being empty with no datalink connection is correct (no CPDLC messages = nothing to read). This is the read path that complements the existing "ATC Message Waiting" auto-announce, which only flagged a message but couldn't read its content.

**SEC (secondary) flight plan reading.** The SEC pages read fully via the normal scrape — the agent + `buildFplnLines` treat them identically to the active plan (clickable waypoints + discontinuities included). URIs (from `MfdPageDirectory.tsx` / `utils.ts`): SEC INDEX = `fms/sec/index`; per-plan = `fms/sec{1|2|3}/{f-pln|init|perf|fuel-load}`. `AllPages` now reaches SEC via the UIService URI path: **SEC INDEX** (`fms/sec/index`) + **SEC 1/2/3: F-PLN** (`fms/sec{n}/f-pln`); the **SEC FPL** quick button (Alt+S in the MCDU) navigates to `fms/sec/index`. The SEC INDEX page itself exposes F-PLN/PERF/INIT/WIND/FUEL&LOAD as buttons, so the rest of each secondary is reachable from there. (Replaced the old `SecIndex` page-selector-id entries, which only worked while the FMS header was mounted.)

**BTV predicted landing distance + rudder-trim readouts.** (1) The ND Display window (`FBWA380NavDisplayForm`, ReadDisplayND) shows the BTV **predicted** landing distances for the selected runway — dry, wet, estimated stop point — from `A32NX_OANS_BTV_{DRY,WET}_DISTANCE_ESTIMATED` / `_STOP_BAR_DISTANCE_ESTIMATED`, which are **plain metres** L-vars (NOT ARINC429, unlike the in-rollout `REMAINING_DIST_TO_EXIT/RWY_END` words). Shown in metres + feet; the "not being computed" gate keys on dry/wet only (stop-bar carries a constant ~40 m front-of-aircraft offset even at rest — verified live: dry/wet 0, stop-bar 40 on the ground). (2) Rudder trim ("weather trim") in the Flight Control Computers panel now reads `A32NX_SEC_1_RUDDER_ACTUAL_POSITION` — the ARINC429 degrees word the PFD/SD show (positive = nose-**Left**), decoded in `TryGetDisplayOverride` to "Left/Right X.X degrees" / "Neutral" / "Not available" — instead of the stock `RUDDER TRIM PCT`, which does not track the FBW SEC-computed trim.

**EFB Failures page — already accessible via the flyPad WebView2 scrape (no code needed).** The flyPad EFB's **Failures** app is reachable from the EFB nav (Shift+T → "Failures" link) through the existing `coherent-flypad-agent` scrape + WebView2 browse view. Verified live: the landing page lists the ATA-chapter categories (ATA 21 Air Conditioning … ATA 46 Information Systems) as links plus a Comfort/Compact view toggle and a Search box; drilling into a chapter (e.g. ATA 24 Electrical Power) lists each individual failure as a cleanly-labelled **button** (TR 1, Generator 1-4, AC ESS, DC HOT APU, …) that arms/activates that failure via the same proven `clickElement` path the rest of the flyPad uses. The dashboard Maintenance widget also shows "No Active Failures" / the active list. The page carries the FBW caveat "Full simulation of the failures below isn't yet guaranteed." (their wording, dev build). The only rough edge is the ATA-chapter LINK labels run number+title+description together ("ATA 24Electrical Power…") — left as-is because the actionable failure BUTTONS are cleanly labelled and reworking the multi-node link text risks other flyPad pages.

**Fuel pumps + niche controls + pitch-trim correctness (post-#107 user follow-up).** (1) **Feed-tank FUEL PUMPS** — were entirely missing. The 8 cockpit pump PBs (FEED TK 1-4 MAIN + STBY) are modelled as ELECTRICAL CIRCUITS, NOT stock FUELSYSTEM pumps: state = stock `A:CIRCUIT CONNECTION ON:<id>`, set by `<id> 1 (>K:2:ELECTRICAL_BUS_TO_CIRCUIT_CONNECTION_TOGGLE)` (a TOGGLE — only fire when desired≠current). Circuit IDs from the cockpit model: FEEDTK1 MAIN=2/STBY=3, TK2=64/65, TK3=66/67, TK4=68/69. Added as Off/On combos in the Fuel panel (`_fuelPumpCircuits` map + a HandleUIVariableSet branch; the calculator-path toggle is live-verified — circuit 2 flips 0↔1). (2) **Pitch-trim correctness** — MSFSBA's "Pitch Trim" read the stock `ELEVATOR TRIM INDICATOR` (normalized −1..1), which is the WRONG representation for the A380. Fixed to read **`ELEVATOR TRIM POSITION` (radians)** and decode in `TryGetDisplayOverride` to "X.X degrees up/down" (positive = UP) — matching the A380 SD `PitchTrim.tsx` (`thsPositionRadians * RAD_TO_DEG`, `Math.sign === 1 ? 'UP' : 'DN'`). The predicted-takeoff-trim readout (`A32NX_TO_PITCH_TRIM`, ARINC429 BNR ±180 = degrees) is correct as-is. The trim WHEEL set isn't a combo (a blind pilot reads the target + actual and adjusts with the global elevator-trim keys/axis — the readouts are the accessibility win). (3) **Niche misc controls** added to the Recorder and Misc panel (settable L:vars via the catch-all): ACMS manual trigger, crew headset, service-interphone override, engine-manual-start-alternate, cabin-crew + passenger (IFE) entertainment Off, remote circuit-breaker control.

**#107 A330-Driver transcript walkthrough — accessibility gaps fixed.** Cross-referenced the full FlyByWire A380X tutorial transcript (preliminary prep → shutdown) against MSFSBA. Most procedures were already supported; the real gaps fixed: **(1) Flight-control check** — added stock surface-deflection readouts `ELEVATOR_DEFLECTION` / `AILERON_DEFLECTION` (AILERON AVERAGE DEFLECTION) / `RUDDER_DEFLECTION` + `SPOILERS_{LEFT,RIGHT}_POSITION` to the Flight Control Computers panel (live-verified the stock simvars give real degrees, e.g. elevator −20°, rudder −27.6°), so a blind pilot can sweep the stick/pedals and read each surface reach full travel (the user's explicit open question). **(2) Predicted takeoff trim** — `A32NX_TO_PITCH_TRIM` (ARINC429, the FMS-computed THS target the pilot must set before takeoff since auto-trim isn't modeled) registered + decoded in `TryGetDisplayOverride` to "X.X degrees up/down" / "Not computed". **(3) MAG/TRUE heading reference** — `A32NX_FMGC_TRUE_REF` added as a Magnetic/True combo in the Source Switching panel (live-verified settable). **(4) Brake check** — added the normal (green-system) brake pressures `A32NX_HYD_BRAKE_NORM_{LEFT,RIGHT}_PRESS` alongside the existing alternate/accumulator (the "triple indicator"; the taxi brake check wants normal ~0 while braking). RCL / TO-CONFIG-TEST buttons were already registered (ECAM control panel). **Second exhaustive transcript pass (138 items, 127 supported) — remaining gaps implemented.** Per-engine **fuel used** (`GENERAL ENG FUEL USED SINCE START:n`, kg) added to the Engines display; **rudder-trim RESET** (`A32NX_RUDDER_TRIM_RESET`) + **nosewheel-steering pedal disconnect** (`A32NX_TILLER_PEDAL_DISCONNECT`, held during the rudder flight-control check) added to the Flight Control Computers panel; **chronometer start/stop + reset** (`A32NX_CHRONO_TOGGLE`/`_RST`) added to Recorder and Misc; a new **Audio Control Panel** (pedestal) with the RMP-1 receive selectors (VHF1/VHF2/CAB/INT/PA/NAV `A380X_RMP_1_*_VOL_RX_SWITCH`, live-verified settable). NOT implemented (genuinely absent on this build): **oxygen quantity PSI** — no `A32NX_OXYGEN_*PRESSURE` L:var exists and the SD has no O2 row (the FBW dev build doesn't model crew-O2 pressure); **clock date** — `A32NX_CHRONO_DATE` reads 0 with unclear encoding (cosmetic, low value). The actual trim-*wheel set* isn't exposed (a blind pilot can't hand-set it precisely without visual feedback; the target+actual readouts are the accessibility win — read target, use trim keys/axis, read actual).

**#104 L-var catalog diff (full FBW A380 source vs MSFSBA).** Method: extracted every `A32NX_`/`A380X_` L-var token from the FBW A380 source + the cockpit model XML's actual interaction targets (`>L:` writes + `<TOGGLE_SIMVAR>`), and diffed against MSFSBA's referenced vars (`tools/_probe/fbw_lvars.txt`, `cockpit_controls.txt`, `missing_controls.txt`). The cockpit-XML interaction diff is the precise signal: **22 pilot-operable controls** existed in the cockpit but were unreferenced by MSFSBA. After triage, most were niche/maintenance/cabin (ACMS trigger, APU auto-exit test, IFE CWS/IFEC, service-interphone override, remote CB, crew headset, DLS [inop], engine-manual-start-altn) or redundant (`VENTILATION_CABFANS_TOGGLE` = the H-event for the cabin-fan PB MSFSBA already sets; `PUSH_OVHD_OXYGEN_CREW` was a false-positive — MSFSBA has it, the diff just didn't match its non-`A32NX_` prefix). The genuinely missing, valuable controls — **ADDED** (Hydraulics panel, settable via the calculator path, live-verified to actuate): the two MAIN electric hydraulic pumps **`A32NX_OVHD_HYD_EPUMPB_PB_IS_AUTO`** ("Electric Hydraulic Pump", green) + **`_EPUMPY_PB_IS_AUTO`** ("Yellow Electric Hydraulic Pump") — the cockpit's `PUSH_OVHD_HYD_ELECPUMP`/`ELECPUMPY`; and the four **engine pump DISCONNECT** PBs **`A32NX_OVHD_HYD_ENG_{1-4}AB_PUMP_DISC_PB_IS_AUTO`** ("Engine n Pumps Disconnect", `{0=Disconnected,1=Normal}`) — verified live that setting one to 0 latches `A32NX_HYD_ENG_{n}AB_PUMP_DISC`=1 (the real disconnect). Their `_HAS_FAULT` vars were already registered; only the control PBs were missing. NOTE: MSFSBA's pre-existing `EPUMPGA/GB/YA/YB_ON_PB_IS_AUTO` aux-electric-pump combos ARE real cockpit controls (8× in the XML) — kept. Readout side: the only sizeable family missing is `FQMS_*_TANK_QUANTITY` (per-tank fuel), already covered by the SD Fuel window scrape; other misses are redundant B-channel duplicates or raw input axes. The diff artefacts live in `tools/_probe/*.txt` for future re-runs.

**SURV (surveillance) controls — read + actuate via the MFD scrape.** Two pages added to `AllPages` (UIService URIs): **SURV: CONTROLS** (`surv/controls`) and **SURV: STATUS & SWITCHING** (`surv/status-switching`). CONTROLS exposes XPDR (SQWK input, AUTO/STBY, IDENT), TCAS (TA/RA / TA ONLY / STBY, ABV/BLW display range, ALT RPTG), WXR (PRED W/S, TURB, ELEVN/TILT, GAIN/MODE), and TAWS (TERR SYS, G/S MODE, FLAP MODE, GPWS) + DEFAULT SETTINGS; STATUS & SWITCHING does SYS 1/SYS 2 source switching. All controls are FBW `RadioButtonGroup` (`.mfd-radio-button` = `<label><input type=radio><span>`), inputs, and buttons — the scrape classifies them and `clickElement` actuates them via the standard path. **Live-verified the click path works:** clicking the enabled XPDR AUTO radio checked it AND cascaded to enable the TCAS sub-radios (which were `disabled` while XPDR was STBY — proper aircraft gating, not a broken click). A `disabled` radio correctly does nothing; the selected state is system-driven (a SimVar round-trip), so it updates a few frames after the click (don't expect a synchronous re-scrape to show it).

**HOLD immediate-exit / resume-hold — surfaced in the F-PLN.** On an active or decel-sequenced hold leg the MFD renders a Button into the leg's speed column (`MfdFmsFpln.renderHoldExitLine`) reading **"IMMEDIATE EXIT"** (or **"RESUME HOLD"** once exit is armed). `buildFplnLines` otherwise only emits the waypoint, so this control was invisible — it now scans each `.mfd-fms-fpln-line` for a `.mfd-button` whose normalized text is `IMMEDIATEEXIT`/`RESUMEHOLD` and emits it as its own clickable line ("Immediate exit" / "Resume hold", kind button). Enter → `onImmediateExitHold` (`fms_set_hold_immediate_exit`). Source-verified (matches `renderHoldExitLine` exactly); could NOT be live-tested because the exit line only renders on an active/sequenced leg, never on the ground (same situation as the discontinuity-delete path). Dormant no-op when no hold is active (verified the F-PLN still reads cleanly).

**Metric / imperial WEIGHT units — MSFSBA-local read-out preference, seeded from + following the aircraft (#A, redesigned).** What the FlyByWire units toggle (`CONFIG_USING_METRIC_UNIT`) actually changes, per `NXUnits.ts` (now cloned into the local fbw-aircraft sparse-checkout): **ONLY weights (kg↔lb)** and short runway/BTV/OANS distances (m↔ft) — NOT altitude (that's `A32NX_METRIC_ALT_TOGGLE`, the FCU metric-alt button, already a combo), speed, or NM route distance. So "altitude still reads 41 after toggling metric" is correct (wrong toggle for altitude). The real problem: MSFSBA reads its OWN raw kg simvars and bypasses the FMS/EFB display conversion, so the toggle had **no observable effect in any MSFSBA read-out**.
- **Source of truth = `A32NX_EFB_USING_METRIC_UNIT`** (1=kg/0=lb), the L:var the EFB `Settings/sync.ts` keeps continuously synced to `CONFIG_USING_METRIC_UNIT`. The bare `A380X_CONFIG_USING_METRIC_UNIT` / `A32NX_CONFIG_USING_METRIC_UNIT` L:vars do NOT track it (verified live). MSFSBA registers + monitors it.
- **Writing the setting:** a raw `SetStoredData('A380X_CONFIG_USING_METRIC_UNIT',…)` does NOT propagate to FBW's NXDataStore live (verified: stored=0 but the L:var + display stayed metric for 5 s+) — NXDataStore isn't reachable as a global and only its own `.set()` fires the in-process subscribers. The ONLY reliable aircraft-side write is the real EFB toggle: flyPad **Settings → Aircraft Options → "US Units"** (`setUsingMetric` → `usePersistentSetting` → `NXDataStore.set`), verified live to flip the L:var 1→0. That toggle is already accessible via the flyPad EFB (Shift+T).
- **Design:** the MCDU **Units** button (Alt+U) is therefore a **MSFSBA-local read-out preference** — `def.ToggleMetricWeight()` flips `_metricWeight` instantly (no Coherent write), and every weight read-out converts: the **gross-weight** + **total-fuel** hotkeys and the gross-weight/total-fuel **panel fields** (via `TryGetDisplayOverride`); `WeightUser(kg)` returns `(value, unit)`. The blind pilot reads MSFSBA's output, so this is what matters. `_metricWeight` (effective unit) is kept SEPARATE from `_aircraftMetric` (last L:var value) so the button and the live monitor don't fight: `ProcessSimVarUpdate` seeds both from the aircraft on first read (silent), and on a genuine AIRCRAFT change (someone flips the flyPad US Units toggle) follows it + announces "Weight units kilograms/pounds"; the button only touches `_metricWeight`.
- **General principle the user emphasised: every FBW unit/feature with an observable effect must be wired into MSFSBA's OWN read-outs — MSFSBA bypasses the cockpit displays, so display-only conversions never reach the blind pilot unless MSFSBA applies them.**

**Metric ALTITUDE — the FCU MTRS button (`A32NX_METRIC_ALT_TOGGLE`), separate from the weight toggle.** When metric-alt is ON, EVERY MSFSBA altitude read-out speaks metres (feet × 0.3048); OFF = feet. The toggle is a continuously-monitored settable combo cached into `_metricAlt` (`ProcessSimVarUpdate` returns false so the generic monitor still announces On/Off). Surfaces converted: (1) the FCU **selected-altitude** hotkey readout (`AltUser(_pAltVal)` → "FCU altitude N metres, selected/managed"); (2) the **Selected Altitude** (`FCU_ALT_VALUE`) and **Standby Altitude** (`INDICATED ALTITUDE`) panel fields via `TryGetDisplayOverride` ("N meters"); (3) the universal **current-altitude hotkeys A=MSL / Q=AGL** — gated in MainForm's `ALTITUDE_MSL`/`ALTITUDE_AGL` consumer by `currentAircraft is FlyByWireA380Definition a && a.MetricAlt` (converts feet→m and appends "meters" so the unit change is audible; the feet path stays a bare number, untouched). **Gating:** the public `MetricAlt` flag + the A380-type check mean no other aircraft and no non-metric A380 state ever hit the metric path. The standby ISIS altitude deliberately stays in feet (backup instrument).

**Fluids/quantities — verified present + accurate (units live-checked against FBW source).** HYD reservoir level GREEN/YELLOW = **gallons** (FBW's own `mod.rs` test reads `HYD_*_RESERVOIR_LEVEL` as `volume_gallon`; live ~12 gal) + LOW flag; HYD system pressure = **psi** (reads 14.7 = ambient when unpressurised on ground, i.e. real); engine oil qty ×4 (`A32NX_ENGINE_OIL_QTY:n`, fadec UNITS.Number) = **quarts** (SD `OilQuantityGauge` max = 18.3 QT, live 12–14); oil temp = celsius; total fuel + per-engine fuel-used = kg. **Oxygen pressure is NOT modelled** — `Oxygen.tsx` hardcodes crew 1829 / cabin 1854 psi (TODO in source), there is no `A32NX_OXYGEN_*` var, so it can't be read live. **Chrono elapsed IS modelled + readable (corrected — an earlier "not modelled" call was wrong).** The FBW Clock instrument writes `A32NX_CHRONO_ELAPSED_TIME` (CHR stopwatch) and `A32NX_CHRONO_ET_ELAPSED_TIME` (ET counter) in SECONDS (−1 = blank), gated on DC ESS power. The CHR is started/stopped by the **H-event** `A32NX_CHRONO_TOGGLE` and reset by `A32NX_CHRONO_RST` (NOT the K-event `A32NX.FCU_EFIS_L_CHRONO_PUSHED`, which is the ND chrono — that earlier mistest is what produced the false "not modelled"); live-verified advancing (10.8 s after toggle). The ET counter runs off the ET knob `A32NX_CHRONO_ET_SWITCH_POS` (0=Run/1=Stop/2=Reset). Both are registered and formatted in `TryGetDisplayOverride` — CHR → "M minutes S seconds", ET → "H hours M minutes", −1 → "Reset" — in the "Clock" panel.

**Metric FCU altitude INPUT.** When metric-alt is ON, `ShowFCUAltitudeDialog` treats the typed value as **metres** (range 30–14935 m), converts to the feet target the FCU actually selects (`feet = m / 0.3048`, rounded to 100 ft, `A32NX.FCU_ALT_SET`), and echoes "Altitude set to N metres, M feet". The real A380 FCU is feet internally; the MTRS window only displays the metric equivalent, so we round in feet and convert back for the read-back. Feet input is unchanged when metric is off. Heading/speed/V-S have no metric FCU mode, so only altitude input converts.

**EWD ECAM message coverage — what's already auto-announced vs what was added.** The A380X shares the A32NX's `EcamMessages` + `EclLine` rendering and even the `A32NX_EWD_*` L-var prefix (the abnormal/sensed PROCEDURES are on an in-process EventBus, no SimVar — hence the `CoherentEWDClient` DOM scrape). Already continuously auto-announced: failure **titles + action lines** (`.EclLine.AbnormalItem`, severity from Red/Amber/Cyan/White class), **memos** (left/right), **PFD memo + limitations** (string SimVars read in JS), **master warning/caution**, the full **FMA** (vertical/lateral mode + armed + autothrust via `Mon`), **AP1/2 active**, and the **SD auto-display page** (`A32NX_ECAM_SD_CURRENT_PAGE_INDEX` = the A380 equivalent of the A32NX "ECAM Warning Page" callout). **Added this pass (the genuine gap):** the bottom-of-E/WD FWS **status-area** boxes — **STS** (check STATUS page), **ADV** (advisory on SD), **FAILURE PENDING** — and the CdsDisplayUnit power-up overlays (**safety-test** / maintenance / engineering-test). `coherent-ewd-agent.js` `A.statusBoxes()` keys on the stable `.StsArea`/`.SelfTest` CSS classes (live A380X self-test text is "SAFETY TEST IN PROGRESS (MAX 30 SECONDS)"). These PERSIST, so `CoherentEWDClient` edge-detects them via `_lastStatus` (announce on appear AND on clear, seeded silently at baseline) rather than the one-shot text dedup used for failures/memos; they flow through the same `LineAnnounced` path so Ctrl+E still mutes them.

**SD pages are now DECODED into clean rows (not scraped).** The schematic SD pages scraped from `A380X_SDv2` flat-joined their X-sorted leaves and interleaved the 4 engines'/gens' values with labels ("115 V 115 APU 115 V 115") — nonsensical for a screen reader (see `Ecam screens all.txt`). Replaced with **`A380SdRows(page)`** (instance method, mirrors the A32NX `SdSystemRows`): per-page "Label: value" rows decoded from the underlying SimVars — Engine/APU/Bleed/Cond/Press/Doors/Elec AC/Elec DC/Fuel/Wheel/Hyd/F-CTL/Cruise. ARINC429 words (FQDC per-tank fuel, PRESS block-1, APU) decode via `Arinc429Word`; plain L:vars read directly; **fuel/weight rows follow the metric toggle** via `WeightUser`. `RefreshSdPageDisplayAsync(simConnect, pageIndex, ewd)` uses the decode for those pages and falls back to the scrape only for C/B / Status / Video. The ECAM-CP combo's status box auto-populates on panel show via `OnDisplayPanelShown` (no need to cycle the combo). Row vars auto-registered OnRequest in `GetVariables`.

**SD/EWD completeness + var-correctness pass (source-audited against the FBW a380x SD/EWD TSX).** Every page was cross-referenced against `fbw-a380x/.../SD/Pages/*` and the upper-EWD `EWD/*` source. **The Upper E/WD is also DECODED now** (page index 16, `ewd:true`): thrust-rating mode (`A32NX_AUTOTHRUST_THRUST_LIMIT_TYPE` → `['',CLB,MCT,FLX,TOGA,MREV]`, + FLEX temp), per-engine N1 / N1-command / EGT / N2 / N3 / FF / engine-state, then the live ECAM memo lines (from `_lastEwdCode` + `EWDMessageLookupA380`) — replacing the schematic scrape ("THR XX THR XX…"). **Wrong-var fixes that were silently reading 0/static:** PRESS outflow valves use the ARINC `..._OPEN_PERCENTAGE_B1` word (the un-suffixed name doesn't exist → 0); ELEC-DC batteries are NUMERIC-indexed `A32NX_ELEC_BAT_{1,2,3,4}_POTENTIAL/CURRENT` (3=ESS, 4=APU — the string `_BAT_ESS_/_APU_` names don't exist → 0); ELEC-DC APU bus = `A32NX_ELEC_309PP_BUS_IS_POWERED`; AC ESS bus = `A32NX_ELEC_AC_ESS_SHED_BUS_IS_POWERED` (the bare `_AC_ESS_BUS_` is AC EMER); WHEEL gear uses the L:vars `A32NX_GEAR_{CENTER,LEFT,RIGHT}_POSITION`; engine oil pressure clamps ≤0 → "not available" (genuinely not modelled on the A380 dev build). **Completeness additions:** DOORS = all 16 passenger doors via `INTERACTIVE POINT OPEN:0-15` + cargo + cockpit sliding windows; BLEED += per-engine PR/HP valve + precooler outlet temp + pack outlet temps + ram air; COND += cabin air extract + ram air; ELEC-DC += ESS/APU TRs + TR currents; ELEC-AC += STAT-INV freq, EMER-GEN load, APU-GEN-2 load, RAT; HYD += pressure-switch/reservoir-low/overheat/air-press + per-engine EDP pump PB + DISC + electric-pump active/overheat; FUEL primary source switched to `A32NX_FQMS_*_TANK_QUANTITY` (FQDC was the fallback) + per-engine/APU fuel-used + fuel-flow; F/CTL += every surface deflection (ailerons/elevators/rudders/spoilers, normalized→%) + THS pitch trim. **Failures in a status box:** the live warnings/cautions/memos ARE the Upper E/WD memo lines (decoded, clean) + the always-on auto-announce; the full INOP-SYS / STATUS / LIMITATIONS list is **EventBus-only in the FBW dev build (NOT exposed as SimVars** — the legacy `A32NX_SD_STATUS_INOP_*` L:vars are unwritten), so it surfaces only via the STATUS-page DOM scrape (the scrape fallback) when FBW renders it. Deferred (complex ARINC bit-packed discrete words, low value-per-effort): COND CPIOM cabin-fan / cargo-isolation states, BLEED pack-flow-rate gauges.

**(legacy) Reading an SD page from the ECAM-CP combo + the gap-aware scrape.** The ECAM Control Panel "System Display Page" combo (`A32NX_ECAM_SD_CURRENT_PAGE_INDEX`) drives the page via the **calculator path** (a `SimVar.SetSimVarValue` from a Coherent view does NOT switch the page — view-local/lost on disconnect; only the MobiFlight `(>L:)` write sticks); the scrape (`CoherentDisplayClient("A380X_SDv2")`, `AnnounceSdPageAsync`) remains the fallback for the non-decoded pages. `coherent-display-agent.js` now joins X-sorted leaves **gap-aware**: touching fragments (split decimals/colons like "64" "." "8") join with no space → "64.8"/"06:49:47", real values keep a space — so every display (SD/EWD/PFD/ND/ISIS) reads cleanly. Tabular pages (ENG/BLEED/HYD/COND/ELEC) come out clean; schematic pages (WHEEL/FUEL/DOOR) read flat (use the per-item SimVars for those). **To get LIVE SD data for testing**, power the aircraft up via the FBW preset: `1 (>L:A32NX_AIRCRAFT_PRESET_QUICK_MODE) 1 (>L:A32NX_AIRCRAFT_PRESET_LOAD_EXPEDITE) 4 (>L:A32NX_AIRCRAFT_PRESET_LOAD)` (1=cold&dark…4=ready-taxi engines running…5=ready-takeoff) — then the SD pages show real values. `tools/sd-page-tour.ps1` reads the current page; switch pages via the MCP/app calc write.

## VARIABLE / CONTROL TROUBLESHOOTING PLAYBOOK — UNIVERSAL (read this before saying "X doesn't work")

**This applies to EVERY aircraft in MSFSBA, not just the A380** — the FlyByWire A32NX/A380X, the Fenix A320, the PMDG 737/777, the HorizonSim 787, and any future proprietary add-on. The *mechanisms* differ per aircraft (one uses L:vars, another K-events, another a DOM bridge or a debugger socket), but the **method for finding the mechanism and proving a control works is identical**. The A380 is the worked example below because that's where we learned it the hard way; translate the specifics to whatever aircraft you're on.

**The #1 mistake this project makes repeatedly is declaring a control "broken"/"computed-output"/"reverts"/"not modelled" from a single weak test, then removing or stubbing it — and being WRONG.** It has happened on the chronometer, the Flight Director, the fuel pumps, the ECL window, the wipers, a whole batch of GPWS/recorder switches, and the MFD/KCCU interaction. Every one of those was actually fine; we just tested it wrong or assumed the hard path. The case studies at the end of this section show exactly how each false "it's broken" was overturned, so you can recognise the pattern. So:

**GOLDEN RULES**
1. **Never conclude "doesn't work" from the MCP `set_lvar` tool.** It uses a native data-def write that is UNRELIABLE for FBW L:vars and produces false reverts. ALWAYS test writes via the **calculator/MobiFlight path** — `execute_calculator_code "<val> (>L:<VAR>)"` (MCP) or `simConnect.ExecuteCalculatorCode` (app) or `SimVar.SetSimVarValue('L:VAR','number',v)` in a Coherent view.
2. **A variable can be fully working even with NO audible/visible feedback.** Many switches (GPWS modes, entertainment, NSS, service interphone) toggle and stick but the sim plays no sound and shows nothing a blind pilot notices. "I can't tell if it did anything" ≠ broken. **Confirm by READ-BACK, not by listening.** If `get_lvar` shows the new value held after ~2 s, it works — ship it.
   - **BUT: the inverse trap — a DEAD/OUTPUT-MIRROR L:var passes the read-back yet drives nothing.** Some L:vars are output mirrors the model WRITES (or legacy vars nothing reads). You write `(>L:VAR)`, it holds your value on read-back (rule above seems satisfied!), but no system reacts — because the real control is a *stock simvar* or *input event*, and your L:var is downstream scenery. **So read back the DOWNSTREAM EFFECT, not the L:var you wrote:** the stock simvar it should drive, the bus/light/valve output, the system state. If the L:var holds but the downstream value doesn't move, the L:var is dead — find the real actuator (stock event). Live-burned on this on the A380 ext-lighting (`LIGHTING_STROBE_0`, `A380X_OVHD_EXTLT_NOSE`, `A380X_OVHD_EXTLT_RWY_TURNOFF` all hold writes but the installed Asobo-template lighting ignores them — `LIGHT STROBE/TAXI` never moved; the working path was the stock `STROBES_SET`/`TOGGLE_TAXI_LIGHTS`). The prior session's "verified" nose light was this exact false positive (it read `LIGHT TAXI:2` changing — but that's the runway-turnoff index, coincidence). Conversely, a *mirror* L:var (GEN/APU-GEN `PB_IS_ON`, copied from a stock simvar each frame) makes the stickiness test LIE the other way: writing the value it already holds "sticks" trivially — to test it you must write the OPPOSITE of the current downstream simvar, or just drive the stock event and watch the mirror follow.
3. **Test EVERY state of a multi-position control, not just 0→1.** A 3-position selector (e.g. ANN LT TEST/BRT/DIM, ENGINE MODE CRANK/NORM/IGN, autobrake DISARM/BTV/LO/L2/L3/HI) must be set to each value and read back. Something can stick at 1 but revert at 2, or vice-versa.
4. **Stickiness test:** read original → write target via calculator path → **wait ~1–2 s (several frames)** → read back. Held = settable. Reverted within a frame = genuine computed output. Then RESTORE the original. (An immediate same-tick read is NOT a revert test — computed outputs revert a frame later.)

**WRITE-MECHANISM DECISION TREE** (try in this order; each is live-proven on the A380):
- **Plain FBW L:var** (most controls): `<val> (>L:VAR)`. Works for switches, FD (`A32NX_FCU_EFIS_L/R_FD_ACTIVE`), ND mode/range, EFIS filters, niche toggles. This is the A380 def's catch-all in `HandleUIVariableSet` for `A32NX_`/`A380X_`/`FBW_` keys. (NOT the transponder mode — that is systems-host-managed on the stock `TRANSPONDER STATE:1` and reverts any external write; only squawk code + IDENT are settable.)
- **Stock K-event:** `SendEvent("EVENT", param)` or `<param> (>K:EVENT)`. e.g. `XPNDR_IDENT_ON`, `TOGGLE_FLIGHT_DIRECTOR` (note: the stock FD event did NOT map on this build — the L:var write did; always try both).
- **Indexed K-event:** `SendEvent("EVENT:index", val)` or multi-param `<a> <b> (>K:N:EVENT)` where N = arg count. e.g. engine anti-ice `ANTI_ICE_SET_ENGn`, ignition `TURBINE_IGNITION_SWITCH_SETn`.
- **H-event:** `(>H:EVENT)` — for things wired only to HTML events. e.g. the clock CHR: `(>H:A32NX_CHRONO_TOGGLE)` / `(>H:A32NX_CHRONO_RST)` (NOT the EFIS chrono K-event — wrong button). Verified the elapsed-time L:var then advances.
- **Electrical CIRCUIT controls** (wipers, feed-tank fuel pumps, emergency exit lights, some lights): the FBW behaviour XML drives a circuit, not a plain L:var. On/off = `<circuitId> (>K:ELECTRICAL_CIRCUIT_TOGGLE)` (state read from `A:CIRCUIT SWITCH ON:<id>`); power/speed = `<percent> <circuitId> (>K:2:ELECTRICAL_CIRCUIT_POWER_SETTING_SET)` (read `A:CIRCUIT POWER SETTING:<id>`). Fuel pumps use `<id> 1 (>K:2:ELECTRICAL_BUS_TO_CIRCUIT_CONNECTION_TOGGLE)` with `A:CIRCUIT CONNECTION ON:<id>`. **Find the circuit id in the behaviour XML** (`#CIRCUIT_ID_…#` in `model/behaviour/legacy/generated/A32NX_Interior_*.xml`), don't guess.
- **FCU values:** the A380 has NO `A32NX_FCU_AFS_DISPLAY_*` vars; use the `A32NX.FCU_*_SET` events (heading/speed/alt) and read the raw `A32NX_AUTOPILOT_*_SELECTED` SI-unit vars (convert rad→deg, m/s→kt/fpm). Signed values (V/S, FPA) must go via calculator code, not `SendEvent` (uint overflow).
- **Genuine computed outputs** (rare): a handful of overhead PBs are recomputed every frame by the FBW systems (some bus-ties). Only after the stickiness test ABOVE reverts on EVERY path do you treat it read-only — keep the read+announce, drop the set.

**WHERE TO DIG when a mechanism is unknown** (do ALL of these before giving up):
1. The live L:var catalog: `tools/_probe/fbw_lvars_clean.txt` (every L:var in the running aircraft) — grep the system name and its synonyms (e.g. wiper → also RAIN, REPELLENT).
2. The FBW **behaviour XML**: `fbw-a380x/.../model/behaviour/` — the cockpit node templates show the EXACT K/H/L mechanism and circuit ids (`awk '/Template Name="…"/,/<\/Template>/'`).
3. The **Rust systems** crate `fbw-common/src/wasm/systems/` and the **instruments** `fbw-a380x/src/systems/instruments/` (TSX) — for what publishes/consumes a var and its units (e.g. reservoir level = gallons, oil = quarts).
4. Live probe with `tools/coherent-eval.ps1 -Title A380X_SYSTEMSHOST -ExprFile <probe.js>` (reads/sets L:vars + stock simvars in-context) and the **simconnect MCP** (`get_lvar`/`get_simvar`/`execute_calculator_code`/`trigger_event`/`search_lvars`).
5. The FBW docs (docs.flybywiresim.com) for intended behaviour.

**TOOLS that exist in this repo:** `tools/coherent-eval.ps1` (Coherent eval), `tools/_probe/*.js` (probes + README), `tools/fbw_lvars_clean.txt` (catalog), the `fbw-aircraft` sparse clone at `C:/Users/franc/Documents/development/fbw-aircraft`. The simconnect MCP is the fastest write-stick tester.

**Captain/First-Officer rule:** where a control GENUINELY exists per side AND both sides work, expose BOTH as a split `… Captain` / `… First Officer` panel — never one shared control, and never invent a First-Officer var that doesn't exist. Verify each side's var is real (reads a value, not ERR) and settable before adding it. (A380 examples: EFIS, baro, FD, ND mode/range, navaids, QNH, and the RMP-1/RMP-2 audio panels.)

**GENERALISING TO OTHER AIRCRAFT.** The decision tree and "where to dig" above are A380/FBW-flavoured, but the *shape* is universal — for any aircraft, the job is (1) find the variable/event the cockpit actually uses, (2) find the write mechanism that sticks, (3) prove it by read-back. Where to look depends on the add-on's architecture:
- **FBW (A32NX/A380X):** open-source — L:var catalog, the behaviour XML, the Rust `systems` crate, the instruments TSX (everything in the `fbw-aircraft` clone). Writes via the MobiFlight calculator path or K/H events.
- **Proprietary with a SimVar/L:var surface (Fenix, PMDG):** no source — discover vars empirically. PMDG exposes a documented data struct + custom events (`send_pmdg_event`, `get_pmdg_var`); Fenix uses L:vars settable via the native data-def path (the one case where data-def IS the right write). Use the MCP `search_lvars`/`list_lvar_*`/`get_pmdg_*` to enumerate, then the stickiness test.
- **DOM/web add-ons (the A380 MFD/flyPad/ECL, any WebView/Coherent UI):** there's no SimVar — the control is in a rendered document. Read/drive it through the debugger socket (`coherent-eval.ps1`, `CoherentEWDClient`) or the WebView. Two hard-won rules: **Coherent GT (Chromium 49) allows only ONE inspector socket per page** (share the connection — that's why the ECL "showed nothing": a second socket was rejected), and a control may have **no DOM click handler** (the ECL is driven by the ECP push-button L:vars, not by clicking the lines; the MFD by the FBW `InputField` keypress path, not the KCCU cursor H-events). When a UI "can't be clicked", find what the real cockpit input actually drives underneath — don't assume the obvious element is the lever.

**CASE STUDIES — false "it's broken", and the reasoning that overturned each** (recognise these patterns):
- **Chronometer ("not modelled"):** first test pulsed the wrong control (the EFIS/ND chrono K-event) and the elapsed-time L:var didn't move → wrongly "not modelled". Reality: the clock CHR runs off the **H-event** `A32NX_CHRONO_TOGGLE` (gated on DC ESS power); fired that, the L:var advanced (10.8 s after toggle). LESSON: a var not changing means *your trigger* might be wrong, not that the var is dead — find the actual input in the source.
- **Flight Director ("writes fail, removed"):** an earlier pass wrote the engage L:var via the data-def path, saw no effect, removed the control. Reality: the **calculator-path** write to `A32NX_FCU_EFIS_L/R_FD_ACTIVE` holds (set 1 → still 1 after 2.5 s). LESSON: re-test removed controls with the calculator path before trusting an old "doesn't work" note.
- **Fuel pumps ("reverts/uncontrollable"):** the MCP `set_lvar` (data-def) appeared to revert → wrongly "computed output". Reality: feed-tank pumps are electrical **circuit connections** toggled with `(>K:2:ELECTRICAL_BUS_TO_CIRCUIT_CONNECTION_TOGGLE)`; via the calculator path they stick. LESSON: never conclude from `set_lvar`; and some "switches" are circuits, not plain L:vars.
- **Wipers ("completely inop"):** the code only set circuit *power* and never *toggled the circuit on*, so they never started. Reality (from the behaviour XML): on/off is `<id> (>K:ELECTRICAL_CIRCUIT_TOGGLE)`, speed is `<pct> <id> (>K:2:ELECTRICAL_CIRCUIT_POWER_SETTING_SET)`; both verified live. LESSON: read the cockpit node template for the EXACT sequence — a partial write looks like a dead control.
- **ECL window ("shows nothing"):** the failure monitor already held the one allowed Coherent inspector socket, so the checklist's second connection was silently rejected. Reality: share the single socket. LESSON: for Coherent UIs, suspect the one-socket-per-page limit first.
- **GPWS / recorder / NSS / entertainment ("unreliable / can't tell if it works"):** write-stick tested all of them — every one set and held after 2 s. They were never broken; the sim just plays no sound and shows nothing a blind pilot notices. LESSON: "I can't tell" is not data — read back the value.
- **MFD / KCCU interaction ("needs the cursor / can't be driven"):** assumed the MFD needed the KCCU trackball/cursor and the ECL needed DOM clicks. Reality (from the instrument source): the MFD takes text via the FBW `InputField` (focus-click + `keypress` with keyCode, Enter=13) and the ECL is driven by the ECP button L:vars — neither needs the KCCU cursor. LESSON: trace what the real hardware input writes; the visible widget is often not the thing you poke.
- **Ext lighting Strobe/Nose ("verified working" — but DEAD):** the def drove `LIGHTING_STROBE_0` and `A380X_OVHD_EXTLT_NOSE` as L:var combos; both *hold* writes, so a naive L:var read-back "passed". Reality: the installed A380X uses Asobo stock-simvar lighting templates — those L:vars are output-only/legacy and appear nowhere in the FBW source; writing 0/1/2 left `LIGHT STROBE:1`/`LIGHT TAXI:*` unchanged. The working actuators are the stock `STROBES_SET` and `TOGGLE_TAXI_LIGHTS` events (live-verified to move the simvars). The prior "verified" nose-light note read `LIGHT TAXI:2` (the runway-turnoff index, not nose) changing — a coincidence. LESSON: read back the DOWNSTREAM simvar, not the L:var you wrote; an L:var that holds but drives nothing is dead. (`A380X_OVHD_EXTLT_RWY_TURNOFF` is dead the same way — left UNexposed.)
- **GEN / APU-GEN pushbuttons ("stickiness says settable"):** `A32NX_OVHD_ELEC_ENG_GEN_n_PB_IS_ON` / `_APU_GEN_i_PB_IS_ON` are COMPUTED MIRRORS the FBW WASM copies from stock `GENERAL ENG MASTER ALTERNATOR:n` / `APU GENERATOR SWITCH:i` each frame. Writing the value they already hold "sticks" → the stickiness test falsely says settable, but a real off-command is overwritten next frame. The working control is the stock event (`TOGGLE_MASTER_ALTERNATOR` engine-index for ENG GEN; indexed `APU_GENERATOR_SWITCH_SET` for APU GEN) — verified by toggling and watching BOTH the stock simvar AND the L:var mirror follow. LESSON: when an L:var is a mirror of a stock simvar (find the `.copy(aircraft(...), aspect(L:var))` in the Rust `*_systems_wasm/src/lib.rs`), drive the stock event; keep the mirror for read/announce.
- **HYD electric-pump A/B ON/OFF ("reverts to AUTO"):** the green/yellow EMP A/B `_ON_PB_IS_AUTO` / `_OFF_PB_IS_AUTO` are toggled by the cockpit (`TOGGLE_SIMVAR`) but the FBW `A380ElectricPumpController` returns them to AUTO when the backup pump isn't needed (on the ground, EDPs pressurising). So a calc write reverts — but that is CORRECT conditional behaviour, not a dead var (the ENG-driven pumps and the two MAIN electric pumps `EPUMPB`/`EPUMPY` hold a manual command and are fully settable — the difference is the per-pump A/B controller logic). Keep them settable (probe-heat precedent); the auto-announce re-reads true state. Also: the ON button is `On/Auto` (0 = forced ON), the OFF button is `Off/Auto` — don't label the ON button OffAuto (selecting "Off" would force it ON). Each EMP genuinely has TWO physical pushbuttons, so two combos is faithful — not a unify candidate.
- **Annunciator / integral LT TEST is render-only — do NOT synthesise:** setting the ANN/INT LT knob to TEST illuminates every overhead light in the SVG but changes NO L:var (live-verified: no `_PB_HAS_FAULT`/annunciator var moves). There is nothing real to announce, so the knob just speaks its position (Test/Bright/Dim) and the real per-system fault lights announce when a genuine fault appears. LESSON: MSFSBA announces real state; if a "test"/action has no readable consequence, announce the control's own position, never a hand-written narration of what the bulbs would show.
- **Every panel control is a COMBO, not a hardware button** (screen-reader principle, user-mandated): a momentary action (TEST / ACK / RESET / chrono / rain-repellent) renders as an `Off`/`Activate` (or `Idle`/`Activate`) combo whose "Activate" option fires the pulse/H-event in `HandleUIVariableSet` (guarded on `value > 0.5`); not auto-announced (the handler speaks "<name> pressed"). The only remaining true buttons are the FCU momentary knob push/pulls (genuine knob actions with post-press value read-outs via `OnPanelButtonFired`) and value-entry fields (squawk code) — those aren't on/off switches.

**Pitch trim (`ELEVATOR_TRIM` panel display) reads `ELEVATOR TRIM POSITION` in DEGREES**, the same unit as the shared base `MON_ElevatorTrim` (Shift+T continuous trim-announce) — so no same-simvar unit mismatch. Decoded to "X.X degrees up/down" (positive = UP, matches the A380 SD PITCH TRIM block). The Shift+T trim-announce runs entirely off the base var and is unchanged on the A380 and every other aircraft (the A380 panel display is OnRequest-only and does not touch it).

**Variable provenance.** The A380X reuses the `A32NX_` prefix for almost all systems (index ranges extended: 4 engines/gens/IDGs/bleeds/FADECs, 11 fuel tanks, 16 brakes, 4 outflow valves). `A380X_` is reserved for genuinely new hardware (RMP suite, EFIS-CP/ND, fuel transfer/jettison, battery display knob, ANN/INT-LT, OIT, storm light, MFD page URIs). Source completeness from the FBW **A32NX docs** (docs.flybywiresim.com) too — MSFSBA's own A320 definition is incomplete, so don't treat it as the ceiling. Reference catalogs in `tools/`: `a380-simvars-catalog.md`, `a380-fcu-vars.md`, `a380-engine-start.md`, `a380-fault-status-vars.md`, `a380-sd-pages.md`, `fbw-a380-mfd-source-findings.md`, and the live L:var dump `_probe/fbw_lvars_clean.txt`. FCU mapping (the A380 has NO `A32NX_FCU_AFS_DISPLAY_*`/`*_LIGHT_ON` vars) is in `a380-fcu-vars.md`.

**FCU is ported from the A320** (set dialogs + paired readouts via `HandleHotkeyAction`; A380 autoflight vars). Not out of scope — it reuses the A320 `A32NX.FCU_*_SET` events with A380 value/managed/mode vars.

**A380 FCU windows (Fenix-style, input mode).** Ctrl+S/H/A/V/P/B open dedicated `Forms/FBWA380/FBWA380{Speed,Heading,Altitude,VS,Autopilot,Baro}Window` forms (shared base `FBWA380FCUWindowBase` — non-modal `ShowForm()`, focus capture/restore, Escape-to-close), replacing the old bare `ValueInputForm` dialogs (`ShowFCU*Dialog`/`ShowA380BaroSetDialog` deleted). Each form is thin UI delegating to public `FlyByWireA380Definition` methods — `SetFCU{Heading,Speed,Altitude,VS}Value`, `FireFCUButton(evt)` (Push/Pull/SPD-MACH/TRK-FPA, readback via `OnPanelButtonFired`), `RequestFCU*WithStatus` (spoken live readout on open + after each action), `RequestAutopilotStates`, `ToggleMetricAltitude`, `SetAltIncrement`, and `ApplyUIVariable` (a `HandleUIVariableSet` passthrough that looks up the `_varCache` def) — no FCU logic in the forms. The **Baro** window enters QNH ONCE and applies to both altimeters (`CAPT_QNH_SET` → `KOHLSMAN_SET` moves both); the STD/QNH and hPa/inHg toggles sync both sides via `*_EIS_BARO_IS_STD` / `XMLVAR_Baro_Selector_HPA_{1,2}`. The **Autopilot** panel (Ctrl+P, previously unhandled on the A380) covers AP1/AP2, A/THR engage (`AUTO_THROTTLE_ARM`) + A/THR + AP disconnect, APPR/LOC/EXPED, with button-state labels refreshed from the cache and **FD read-only** (uncontrollable on this build). Per the screen-reader rule the windows never re-announce button presses — only value-set confirmations + readouts speak. Verify live with `tools/fcu/fcu-read.ps1` (full FCU state) / `fcu-roundtrip.ps1`; note probe-side `A32NX.FCU_*` sets are no-ops (set via the app, read back via the harness).

**FCU readouts need SI→display conversion (the A380 has NO `A32NX_FCU_AFS_DISPLAY_*` vars).** The A320 reads those pre-formatted display vars; the A380 lacks them, so MSFSBA reads the raw `A32NX_AUTOPILOT_*_SELECTED` targets, which the SimConnect data-def read returns in **SI base units**. `ProcessSimVarUpdate` converts: heading/track radians→deg (`Math.Abs(v)<=2π ? v*180/π`), FPA radians→deg (`<=0.2 ? *180/π`), V/S m/s→fpm (`*196.8503937`, round to 100), speed m/s→kt (`*1.943844`). Altitude is a SimVar read in feet already. Symptom if a conversion is missing: V/S reads "15" instead of 2000, heading "004" instead of a real bearing. FCU set/readback (`Shift+A/H/S/V`), V/S sign, and the managed-mode dot are all live-verified.

**Flight director IS controllable (corrected 2026-06) — settable per-side combos.** The earlier "uncontrollable" verdict tested the WRONG vars (`TOGGLE_FLIGHT_DIRECTOR`, `A320_Neo_FCU_FD_n_PUSH`, `A380X_EFIS_L_FD_BUTTON_IS_ON`, and the mis-named `A32NX_FCU_LEFT_EIS_FD_ACTIVE`) — all of those genuinely fail. The CORRECT vars are **`A32NX_FCU_EFIS_L_FD_ACTIVE`** / **`_R_`**, written via the calculator path: live-verified (2026-06) that `1 (>L:A32NX_FCU_EFIS_L_FD_ACTIVE)` holds (0→1, still 1 after 2 s, restored to 0). They are exposed as the "Flight Director 1/2" `OnOff` combos. The stock combined `AUTOPILOT FLIGHT DIRECTOR ACTIVE` ("FD_ACTIVE") is kept as an extra read-only status. (FD bars themselves are visual-only, but the engage toggle is real — don't rip out the combos.)

**EFIS baro is decoded for both speech AND the panel display.** `A32NX_FCU_{LEFT,RIGHT}_EIS_BARO_HPA` is an ARINC429 word (raw ~14 billion — the "altimeter 14 million" bug). The decoded value auto-announces in `ProcessSimVarUpdate` on knob turn and on STD↔QNH and hPa↔inHg unit changes, is queryable via the altimeter hotkey, AND now reads cleanly in the EFIS Captain/First Officer panels via **`TryGetDisplayOverride`** (see below) — "1013 hPa" / "29.92 inHg" / "Standard", never the raw word. Set-QNH routes through `KOHLSMAN_SET` (param = millibars×16), unit-aware. Minimums (`FM1_MINIMUM_DESCENT_ALTITUDE` / `_DECISION_HEIGHT`) likewise ARINC-decoded for both speech and the **Minimums** panel ("200 feet" / "Not set").

**EFIS baro UNIT (hPa/inHg) comes from `XMLVAR_Baro_Selector_HPA_{1,2}` (1=hPa, 0=inHg), NOT `A32NX_FCU_EFIS_*_BARO_IS_INHG`.** The IS_INHG var is stuck at 0 on the A380X (verified live via the Coherent debugger: the F/O reads `XMLVAR_Baro_Selector_HPA_2 = 0`/inHg while `A32NX_FCU_EFIS_R_BARO_IS_INHG` stays 0/hPa). The combo always wrote the XMLVAR, but the announce/display read the stuck IS_INHG — so a pilot who selected inHg kept hearing hPa. `_baroInHgL/R` is now tracked off the XMLVAR in `ProcessSimVarUpdate` (inHg when `value < 0.5`); IS_INHG is no longer registered. This feeds `BaroPhrase` and `TryGetDisplayOverride`.

**Live Coherent eval (the AI's own A380 read/write/DOM access).** The MCDU/flyPad/cockpit run in Coherent GT views exposed by the sim's remote inspector on `http://127.0.0.1:19999` (same transport `CoherentDebuggerClient` uses). You can open `ws://127.0.0.1:19999/devtools/inspector/<id>` and run `Runtime.evaluate` to scrape DOM, click elements, AND read/write ANY L:var via `SimVar.GetSimVarValue`/`SetSimVarValue` from inside a cockpit view — independent of the SimConnect MCP (which goes stale on focus loss). Resolve pages by TITLE from `/pagelist.json` (ids shift each session): flyPad EFB title-needle `- EFB`, MFD = `A380X_MFD`, ND_1 = `A380X_ND_1`, FCU = `A380X_FCU`, PFD_1 = `A380X_PFD_1`, SD = `A380X_SDv2`, EWD = `A380X_EWD`. **The convenience wrapper for all of this is `tools/coherent-eval.ps1`** (tracked in the repo — `-Title <view-needle> [-PreFile <agent.js>] -ExprFile <probe.js>|-Expr "<js>"`; its header lists every view title-needle + usage examples), and `tools/_probe/` holds a worked catalogue of probe scripts (with a `README.md`) covering every A380 feature — copy one, tweak the var/page, run it. This is how the baro-unit bug above was diagnosed, and it's the path for #43 (read verification) and #60 (write-mechanism probe). Direct `SetSimVarValue` to some XMLVARs doesn't apply same-tick — use a separate eval to read back, or the `(>L:VAR)` calculator form. The flyPad/MCDU agents expose `labelFor`/`fieldName`/`scrape`/`clickElement`/`navigateById` as methods on their `window.__MSFSBA_FLYPAD` / `window.__MSFSBA_A380` global, so an injected probe can call them to see exactly what a screen reader hears; MFD pages navigate via `navigateById(prefix,index,kccuKey)` (table = `FBWA380MCDUForm.AllPages`). **Live-verified label fixes** (found this way): flyPad value inputs read by field name (Cargo/ZFW/Passengers/Current Altitude/…) via `A.fieldName`; icon buttons via `A.tooltipSibling`; MCDU multi-span buttons space-joined via `A.spacedText` ("ACTIVATE APPR" not "ACTIVATEAPPR"); empty MFD entry fields (U+25AF boxes) read "(empty)" via `A.normEmptyBoxes`; FBW-rendered "null"/"undefined" cell text skipped; FMS-gated MFD pages (WIND/REPORT/GNSS/TIME/DATA sub-pages, disabled until a flight plan loads) return "disabled" from `navigateById`.

**`TryGetDisplayOverride(varKey, value, out text)` — panel-display decode hook.** New virtual on `IAircraftDefinition` / `BaseAircraftDefinition` (default returns false). `MainForm.UpdateDisplayText` calls it BEFORE the ValueDescriptions / numeric formatting, so an aircraft can render a decoded string for a value whose raw double isn't presentable (ARINC429 words, etc.). The A380 overrides it for the EFIS baro + minimums words. Use this for any future display field that needs unit-aware or ARINC decoding rather than re-adding raw words to a display set.

**#56 wing anti-ice + probe heat (LIVE-VERIFIED 2026-05).** Engine anti-ice uses `K:ANTI_ICE_SET_ENGn` (the L:var/XMLVAR don't drive it — verified live). WING anti-ice: the old momentary `XMLVAR_MOMENTARY_PUSH_OVHD_ANTIICE_WING_PRESSED` pulse 1→0 does NOT actuate the system (confirmed: pulsing it left `_SYSTEM_SELECTED` at 0). The combo now writes `A32NX_PNEU_WING_ANTI_ICE_SYSTEM_SELECTED` directly through the calculator path — verified 0/1 both stick; `A32NX_PNEU_WING_ANTI_ICE_SYSTEM_ON` (the valve-open output) stays the read-only "Wing Anti-Ice Flowing" status (it is gated by the on-ground inhibit, so it does not follow SELECTED on the ramp). PROBE/window heat writes `A32NX_MAN_PITOT_HEAT` (the exact var the cockpit `FBW_Airbus_Probes_Window_Heat` template toggles); it AUTO-forces ON whenever AC2 bus is powered or an engine runs, so a "set Off" reverts — that is correct A380 behaviour (probe heat is automatic), and the `Mon` auto-announce re-reads the true state. Both go through the reliable MobiFlight calculator path (the data-def `SetLVar` is unreliable for FBW L:vars).

**Disabled controls read "dimmed", and flyPad "(selected)" needs a `bg-theme-accent` sibling.** The MCDU form says `…, button, dimmed` (matching the flyPad WebView2 view and the guide), not "disabled". The flyPad agent's selected-option marker (`coherent-flypad-agent.js` `isSelectedSegment`) only fires when a SIBLING carries the unselected-segment token `bg-theme-accent` — because FBW's primary `Button` component uses the same `bg-theme-highlight` background that segmented controls use for their active option, so the bare token falsely marked every action button (Back/Apply/Reset/…) as "(selected)". (Open item: the throttle-calibration detent low/high value inputs still read as bare numbers — they have no associated label in the DOM; precise labeling needs the live DOM or a FBW-source dive.)

**INIT REQUEST disabled is sim behavior, not a bug.** The MCDU INIT REQUEST (SimBrief route fetch) is dimmed until BOTH: no destination is set on the MSFS World Map (a World Map dest makes the FMS import that plan and hide INIT REQUEST) AND SimBrief/Navigraph is configured in the flyPad. The agent scrapes it correctly as "dimmed". See `docs/a380-manual.html` Part 5.

**Engine start.** ENG MASTER 1-4 = `FUELSYSTEM_VALVE_OPEN/CLOSE` (per-engine `EventParam`); engine MODE selector fans `TURBINE_IGNITION_SWITCH_SET{1..4}` out to all engines via `HandleUIVariableSet` (same place the 3-speed wipers send `ELECTRICAL_CIRCUIT_POWER_SETTING_SET:141/143`).

**ARINC429 readouts.** Many SD-page quantities (FQMS per-tank fuel, FOB/GW/CG, cabin alt/VS/ΔP, APU N/EGT) are ARINC429 words — decode with `SimConnect/Arinc429Word.cs` (numeric-truncate the double to u64; low 32 bits = IEEE-754 float in engineering units; bits 32-33 = SSM, `0b11` = NormalOperation). The generic per-panel display path shows the raw double, so these are read in the dedicated **`Forms/FBWA380/FBWA380SystemDisplayForm`** (ECAM System Display window — Fuel/Engine/Press/APU/Elec/Hyd/Cond, ARINC429 decoded), opened via `ShowStatusPage`/`ShowECAM`. The form auto-registers its read vars in the A380 def (add-if-absent loop in `GetVariables`) and is the single source of truth for the var names.

**Display read-out windows (port of the A320 windows / Fenix-PMDG `ReadDisplay*` parity).** `FBWA380SystemDisplayForm` (SD/ECAM, ARINC429), `FBWA380NavDisplayForm` (ND — mode/range, TO waypoint via the 6-bit ident unpack, XTE/RNP/ILS), and the **shared** `Forms/A32NX/PFDForm` reused for the A380 (FMA modes, approach capability, PFD messages — all shared `A32NX_` names; PFDForm AP-status falls back to `A32NX_AUTOPILOT_n_ACTIVE` since the A380 has no FCU light vars). Wired in `HandleHotkeyAction`: `ShowPFD`/`ReadDisplayPFD`, `ShowNavigationDisplay`/`ReadDisplayND`, `ShowStatusPage`/`ShowECAM`/`ReadDisplayUpperECAM`/`ReadDisplayLowerECAM`/`ReadFuelInfo`. FMA vertical/lateral mode + approach capability also announce **live** (`Mon` vars). On-demand hotkeys ported too: `ReadFlaps`/`ReadGear`/`ReadAltimeter`/`ReadGrossWeightKg`/`ReadSpeed{VLS,F,GD,S,VS,VFE}` (full A32NX speed-tape parity; `ReadSpeedVFE`→A380 `A32NX_SPEEDS_VFEN`)/`ReadFuelQuantity`/`ReadApproachCapability`/`ReadWaypointInfo` (the last reuses the shared SimConnectManager request-370 waypoint handler). **ISIS** standby-instrument window `FBWA380ISISForm` (Alt+I / `ReadDisplayISIS`): standby attitude/heading/IAS+Mach/altitude+baro-ref/LS, read from standard air-data simvars (registered with proper units, not the generic auto-register "number") + `A32NX_ISIS_BARO_MODE` (QNH=0/STD=1, per source) / `A32NX_ISIS_LS_ACTIVE`.

**Settable temperature selectors + thrust detents.** Air-cond cockpit/cabin (18-30 C) and cargo fwd/bulk (5-25 C) temp selectors are numeric Celsius inputs (`COND_*_TEMP_SET`/`CARGO_*_TEMP_SET` keys end `_SET` → MainForm numeric box); `HandleUIVariableSet` converts °C → the FBW 0-300 knob via the `_tempSelectors` table (`knob = (temp-Lo)/(Hi-Lo)*300`). Thrust-lever detents settable via the **Thrust Levers** pedestal panel (`THROTTLE_ALL_DETENT` + per-engine, `THROTTLEn_AXIS_SET_EX1`), and **auto-announced** on lever movement (`A32NX_AUTOTHRUST_TLA:1` monitored → `ProcessSimVarUpdate` speaks the detent reached). Manual pressurization knobs are pass-through position writes (no documented engineering-unit mapping in the dev build).

**Checklist + guides.** `Checklists/FBW_A380_Checklist.txt` (cold-and-dark→shutdown, wired in `ChecklistForm`), `docs/a380-manual.html` (the single complete screen-reader manual — cockpit/MFD/flyPad/checklists/displays/flight-planning/systems/full-flight walkthrough/hotkeys; the former `a380-mcdu-guide.md` was merged into its Parts 4–5). MCDU text entry uses the real FBW `InputField` mechanism (focus-click + `keypress` with `keyCode`, Enter=13) — NOT KCCU H-events; see `coherent-a380-agent.js`.

**Checklist hotkeys split (Shift+C vs Ctrl+Shift+C).** **Shift+C** (OUTPUT) now opens the **static text checklist** (`ChecklistForm`, which already supports `FBW_A380` → `FBW_A380_Checklist.txt`) for ALL aircraft — the A380 special-case was removed from `MainForm.ShowChecklistDialog`. The **live ECL** moved to its own key **Ctrl+Shift+C** (`HOTKEY_CHECKLIST_ECL`=9213, `MOD_CONTROL|MOD_SHIFT|0x43`, OUTPUT mode → `HotkeyAction.ShowChecklistECL` → `MainForm.ShowChecklistECLDialog`, A380-only). **ECL window UX overhaul** (`FBWA380ChecklistForm`): the checklist overlay is now **auto-shown on open** and **auto-hidden on close** (pulse `A32NX_BTN_CL`; tracked by `_weShowedOverlay`) — this is what fixed the "Checklist not reachable" error, which only ever meant the overlay was hidden (the `.EclLine` rows don't exist until C/L is toggled — verified live: after a C/L pulse the agent returns the full menu COCKPIT PREP/BEFORE START/… + abnormal procedures). The redundant Show-hide / Up / Down / Check-select **buttons were removed** (they duplicated arrows+Enter and confused the workflow); only Abnormal-procedures / Refresh / Close remain. Keys: arrows = cursor, Enter = check/select, **Backspace = Clear/back**.

**Live Electronic Checklist (ECL) — FULLY interactive accessible window (Ctrl+Shift+C on the A380).** The A380 ECL (normal checklists + active ECAM abnormal procedures) renders as `.EclLine` rows on the `A380X_EWD` Coherent view. `Forms/FBWA380/FBWA380ChecklistForm` (opened by `ShowChecklistECL` via `MainForm.ShowChecklistECLDialog`) reads every line into a navigable list that **mirrors the FWS cursor** (the `.EclLine.Selected` line), announces items as they become checked, and drives the checklist via the ECP buttons. `Resources/coherent-ecl-agent.js` (`window.__MSFSBA_ECL`) scrapes structured rows (type = headline/item/abnormal/completed; `checked`; `selected`; style = cyan-action / amber-caution / white-manual).

**CRITICAL: Coherent GT (Chromium 49) allows only ONE inspector socket per page — the ECL is read through the SHARED `CoherentEWDClient` connection, NOT its own.** The earlier claim that "the Coherent debugger allows multiple inspector connections" was **WRONG and caused the regression where the ECL window showed nothing**: opening a second WebSocket to `A380X_EWD` is rejected ("remote party closed the WebSocket connection without completing the close handshake" — verified live by holding one connection open and trying a second). Since the failure monitor (`CoherentEWDClient`) is always connected to `A380X_EWD` from A380 load, the old standalone `CoherentEclClient` could never connect → `ScrapeNowAsync` always returned empty → empty window. **Fix:** `CoherentEWDClient` now installs the ECL agent on its OWN socket alongside the E/WD agent (both are isolated-IIFE globals — verified they coexist and both scrape on one connection), exposes `EclActive` (poll-gate set by the window), `EclRowsUpdated` (pushed on change), and `ScrapeEclAsync()` (on-demand after an ECP pulse). `FBWA380ChecklistForm` takes the shared `CoherentEWDClient` (passed from `MainForm.ShowChecklistECLDialog`, which starts the monitor if needed) and never opens or disposes a connection. `CoherentEclClient.cs` was deleted; `EclRow` moved into `CoherentEWDClient.cs`. **General rule: every A380X_EWD consumer MUST funnel through the one `CoherentEWDClient` socket — never `new` a second client against the same view.**

**Interaction — the ECP buttons FULLY drive the ECL (verified live end-to-end; NOT cursor-limited).** The `.EclLine` rows have no DOM click handlers (pointer-events-free SVG over `ewd-main`), so interaction goes through the real ECP push-button L-vars, which `FwsCore.ts` buffers each cycle into pulse nodes and `FwsNormalChecklists.ts` acts on: `A32NX_BTN_CL`→show/hide; `A32NX_BTN_UP`/`_DOWN`→`moveUp`/`moveDown` (cursor in the menu AND inside a checklist); `A32NX_BTN_CHECK_LH`/`_RH`→ on the menu (`checklistId===0`) `navigateToChecklist(keys[selectedLine])` **opens** the selected checklist, inside a checklist `checkSelected()` **ticks** the selected manual item and auto-advances; `A32NX_BTN_CLR`→`navigateToParent`; `A32NX_BTN_ABNPROC`→abnormal list. **Precondition (FwsNormalChecklists ~L451):** `if (!checklistShown) return;` — Up/Down/Check do nothing until `C/L` shows the checklist (this is why an earlier test "didn't move the menu"; the earlier "needs the KCCU cursor / FBW #9348" conclusion was WRONG). The form pulses these via the MobiFlight calculator path (`1 (>L:…)`/`0 (>L:…)`); its list-box Up/Down arrows drive `BTN_UP`/`_DOWN` and Enter drives `BTN_CHECK_LH`, so the reader's selection always mirrors the cockpit cursor. Sensed items tick themselves; manual items tick on Check. Verified live: C/L showed the menu (BEFORE START selected) → DOWN→AFTER START → CHECK opened it (cursor on ANTI ICE) → CHECK ticked ANTI ICE and advanced to PITCH TRIM → navigated to RESET + CHECK reset it. The KCCU keys are H-events `H:A32NX_KCCU_L_<KEY>` (ESC/DIR/ENT/UP/DOWN/…) consumed by the MFD, not needed for the ECL. The A380 ECL does its own sensing (`skipCompletedSensed`), so sensed items tick themselves.

**Two SEPARATE checklist systems — don't confuse them.** (1) The **E/WD ECL** above is the real aircraft checklist (FWS-driven) — this is what MSFSBA's Checklist hotkey opens and what matters for flying. (2) The **flyPad EFB Checklists page** (`/checklists` tab) is a *separate, tablet-only tracker*: the A380 DOES mount the shared `fbw-common` Checklists page (verified — earlier "A380 has no EFB checklist page" was wrong; only the *Pages* subdir is A380-specific, the page itself is shared via `EfbWrapper`/`ToolBar`), but ticking a flyPad item writes **only EFB-local Redux state** (`trackingChecklists`), never an aircraft L-var/SimVar — it does NOT drive the E/WD ECL. The persistent setting `EFB_AUTOFILL_CHECKLISTS` (real stored key **`A380X_EFB_AUTOFILL_CHECKLISTS`**; prefix = uppercased `AIRCRAFT_PROJECT_PREFIX`=`A380X`, confirmed live via `A380X_CONFIG_USING_METRIC_UNIT`) flips the flyPad page from manual ticking to read-only auto-sensing: every 1 s the EFB evaluates each item's `condition` (SimVar/L-var compares from `config/a380x/a380-842/checklists.json5`) and auto-marks matches. Set it through the EFB store: in the `- EFB` Coherent view, `SetStoredData('A380X_EFB_AUTOFILL_CHECKLISTS','1')` (persists; same store the Realism settings toggle writes). It only affects the tablet, not the aircraft.

**STATUS / INOP SYS / LIMITATIONS (#2) — REACHABILITY SOLVED via the systems-host FwsCore; data still FBW-WIP.** The earlier "EventBus-only, unreachable" conclusion was wrong. Each FBW instrument is a custom element whose JS instance/objects are reachable from the debugger: `document.querySelector('a380x-mfd').fsInstrument` exposes `bus` + `fmcService` (full flight plan via `fmcService.master.flightPlanInterface.active.allLegs`), and **`document.querySelector('systems-host').fwsCore`** (on the `A380X_SYSTEMSHOST` view) exposes the FWS directly: live Subjects `inopSysAllPhasesKeys` / `inopSysApprLdgKeys` / `inopSysRedundLossKeys`, `limitationsAllPhasesKeys` / `limitationsApprLdgKeys`, `ecamStatusNormal`, `presentedAbnormalProceduresList` / `activeAbnormalNonSensedKeys`, and `pfdMemoLines` / `pfdLimitationsLines` (each `.get()` / `.value`). So the channel is OPEN — a future systems-host agent can read these and map the keys to text (EcamInopSys/EcamLimitations dicts) and announce them. BUT the dev build still doesn't POPULATE inop/limitations even on failures (verified: PRIM 1 failure left `inopSysAllPhasesKeys.get()` `[]`, `ecamStatusNormal` true) — that computation is WIP on FBW's side, out of our control. So integrate the reader when FBW ships the data; today it would surface empty (the abnormal *procedures* already come through the ECL, and per-failure "FUNCTIONS NOT AVAIL" through the failure-procedure scrape). **General lesson: the per-instrument `fsInstrument`/element objects (bus, fmcService, fwsCore) are the master key to anything previously deemed "not exposed".**

(Original note kept for context:) The PFD/E/WD LIMITATIONS come through the `A32NX_PFD_LIMITATIONS_LINE_1..8` string SimVars already read by the E/WD monitor (see PFD memo+limitations above); the E/WD also renders a `.LimitationsContainer`. The INOP-SYS list is EventBus-only (no SimVar). Live-tested with cabin-fan AND PRIM 1 failures: the FBW A380 dev build does NOT yet populate limitation text, PFD-limitation strings, or any INOP/STATUS DOM area (the limitations/abnormal-status system is WIP — many procedures show "(WIP)"). So #2's data channel is in place and will surface automatically once FBW computes it; there is nothing more to scrape today (the per-failure "FUNCTIONS NOT AVAIL" is already captured by the failure-procedure scrape).

**flyPad Dashboard ("Your Flight") — column-aware reading order.** The EFB Dashboard is a two-column flex layout (`fbw-common Dashboard.tsx`: one `flex w-full` row of two `w-1/2` columns): LEFT = `FlightWidget` ("Your Flight"), RIGHT = `RemindersWidget` ("Important Information": Weather / Pinned Charts / Maintenance / Checklists, order from persistent `REMINDER_WIDGET_ORDERED_KEYS`). The generic row-by-row, left-to-right enumerate in `coherent-flypad-agent.js` interleaved the two columns into a jumble. Fix: `enumerate()` detects the dashboard via `A.isDashboard(root)` (an `<h1>` containing "Your Flight") and, for that page only, sorts by **column first** (`_col` = left/right of `rootRect.width*0.48`, nav-rail still last) then row then left — so it reads the whole left widget, then the whole right widget. Other pages keep the row-wise order (so a Settings name+toggle stay paired). Widget/section titles are already captured as `h1.font-bold` / `h2.font-medium` headings, so each block reads under its own title.

**A380 MFD company-route (SimBrief) insertion — verified live.** After requesting on the FMS INIT page, the scratchpad shows "COMPANY F-PLN RECEIVED WAITING FOR INSERTION" and the top-right button reads **"RECEIVED CPNY F-PLN"**. Clicking it opens a dropdown with **`INSERT*`** (and `CLEAR*`); `INSERT*` calls `fmc.insertCpnyFpln(Active)` → copies the Uplink plan straight into the Active plan (no TMPY for the uplink itself). FLT NBR / FROM / TO / ALTN come pre-filled from SimBrief and stay editable afterwards (no TMPY, Preflight). Runway+SID selection IS a TMPY (DEPARTURE→RWY→SID→`INSERT TMPY *`). MSFSBA drives all of this through the existing `__MSFSBA_A380.clickElement(idx)` path — the elements (button, then the `INSERT*` menu item) appear in the normal scrape. Verified live: requested → RECEIVED CPNY F-PLN → INSERT* (VCBI→OMDB, ALTN OMAA, FLT NBR UAE111, CRZ FL380, CI60) → DEPARTURE → RWY 22 → SID BASU1D → INSERT TMPY.

**MCDU readout polish.** Empty *editable* MFD fields now read **"blank"** (not "(empty)") — `coherent-a380-agent.js` `normEmptyBoxes`/`elementLabel`/`lineText` emit `blank`; disabled fields still show their native dashes (`----`). The leading element-index number (`"11: …"`) was dropped from `FBWA380MCDUForm.RefreshDisplay` — it was an internal click handle (`el.Index`, still used to click), not a user input, and the trailing role word ("…, button" / "…, edit") already marks actionable lines.

**MFD grid pages — row merge + dedicated F-PLN builder.** Grid pages render each cell as a separate absolutely-positioned text node, so `enumerateLines` was emitting one cell per line (token-salad). General fix: after the row-sort, the agent MERGES consecutive same-row STATIC-TEXT cells (same `top` bucket) into one line joined with **", "** (comma → screen-reader pause). Interactive controls (`idx>0`) always keep their own line. Leg distances get **"NM"** appended (DIST-column class `mfd-fms-fpln-leg-upper-row` + pure-integer pattern; track has `°`, FPA has a sign, so neither matches). This serves PERF / FUEL&LOAD.

**The F-PLN has its OWN per-waypoint builder** (`A.buildFplnLines`), because the row merge still scattered it (waypoint cells and leg cells sit at different y within one `.mfd-fms-fpln-line`). Each `.mfd-fms-fpln-line` container groups a waypoint with its INBOUND leg + constraints; the builder folds it into **one clean line**: `IDENT, via AIRWAY, N NM, track T°, <constraint>, ETA hh:mm` — dropping empty/dash predictions, stripping the `@` overfly marker, deduping the repeated procedure name (via X once until it changes), and decoding constraints (`+N`→"at or above N feet", `-N`→"at or below N feet"). `enumerateLines` calls it when `.mfd-fms-fpln-line` exists and then skips the whole plan grid in the generic pass via `A.insideFpln` (any ancestor class containing `mfd-fms-fpln`); the header row is dropped (each line is self-describing). Builder items carry `fpln:true` so the merge never touches them. Verified live: the VCBI→OMDB plan reads "DER22, via BASU1D, 2 NM, track 217°" / "BI613, 5 NM, track 294°, at or below 5000 feet" / "TVM, via P570, 80 NM, track 294°". The F-PLN shows a WINDOW of ~12 waypoints with DEST pinned at the bottom — page down (Ctrl+Down) to walk the rest of the route (documented in docs/a380-manual.html Part 4).

**F-PLN waypoint lines are CLICKABLE → lateral-revision menu (incl. clearing a discontinuity).** Clicking a `.mfd-fms-fpln-line-ident` via plain DOM `.click()` opens the real MFD **revisions menu** (`.mfd-context-menu` of `.mfd-context-menu-element` items: FROM P.POS DIR TO, INSERT NEXT WPT, DELETE \*, DEPARTURE, ARRIVAL, OFFSET, HOLD, AIRWAYS, OVERFLY \*) — no KCCU cursor needed (verified live via `__MSFSBA_A380.clickElement(idx)`). The menu items were already captured as kind `menu`→role "option", so the only gap was the lines themselves. `A.buildFplnLines` now (a) stamps each waypoint's ident cell with the `data-fbwa380-mcdu-idx` and emits it as kind **`fplnwpt`** (role word "waypoint", form `RoleWord`), so Enter on a waypoint opens its revision menu (then `ScheduleRefresh` shows the options); and (b) handles **special lines** (`.mfd-fms-fpln-line-special`, source `renderSpecialLine`: a delimiter band `- - - DISCONTINUITY - - -` with NO ident child). Those were previously DROPPED by `if(!ident) continue` — so a **discontinuity was invisible** to the reader. Now the label is surfaced; a `DISCONTINUITY` line is stamped on the line div itself (which carries the same `onIdentClicked` handler) and emitted as kind **`fplndisc`** (role "button"), so Enter→menu→**DELETE \*** clears it (calls `flightPlanInterface.deleteElementAt(legIndex,false,…)`). Other markers (END OF F-PLN…) stay plain text (their click opens nothing). `buildFplnLines` now threads/returns the `idx` counter; the merge step already skips `fpln:true` items so clickable lines keep their own row. This is the non-redundant path: a flight-plan **edit** (clear disco, DIR TO, HOLD, insert/delete) is impossible via SimVars and had no prior MSFSBA route. Source: `fbw-a380x/.../F-PLN/MfdFmsFpln.tsx` + `FplnRevisionsMenu.tsx`. (No live disco existed in the test plan to click — waypoint path verified end-to-end live; disco path is the identical handler, source-verified.)

**Combo boxes announce expanded / collapsed.** MFD dropdowns (RWY/APPR/STAR, source selectors, etc.) now read their open state, e.g. "RWY: 30R, combo box, collapsed" → Enter → "…, combo box, expanded" (and the option list folds in as `option` lines). Detection (verified live): the FBW DropdownMenu button gains an **`opened`** class and its sibling `.mfd-dropdown-menu` flips to **`display:block`** when open (`MsfsAvionicsCommon/UiWidgets/DropdownMenu.tsx`). `coherent-a380-agent.js` `A.comboExpanded(n)` walks ≤6 ancestors for an `opened` class or a `.mfd-dropdown-container`'s menu `display`, returns true/false; `enumerateLines` sets `expanded` (true/false for combos — kind `dropdown` or isChoice input — else null). Wire-through: `ScrapeElement.expanded` (`bool?`) → `CoherentDebuggerClient` emits `items.N.expandstate` = "expanded"/"collapsed"/"" (and folds it into the change-hash so a toggle re-renders) → `FBWA380MCDUForm.McduElement.ExpandState`, appended after the role word in `RefreshDisplay`. Non-combo controls send "" (no state). Verified live on ARRIVAL: 5 dropdowns report `false` collapsed, the expanded APPR reports `true`.

**Open menu/combo options are placed directly UNDER the control that opened them.** A popped-up combo/context menu overlays other content, so the geometric sort scatters its options between unrelated lines. `enumerateLines` tags each option with `menuGroup` (container index into `menuConts`) + `menuOrder` (DOM order), then a final regroup pulls each menu's options into one contiguous block (menu order) and inserts it **immediately after its opener** (NOT at the top — the user asked for "right under the combo"). Opener resolution (`triggerIdxFor`): a **combo box** → the stamped control in the same `.mfd-dropdown-container` as the open `.mfd-dropdown-menu` (skip the option elements via `ancestorWithClass(...,'mfd-dropdown-menu')`); a **F-PLN waypoint/discontinuity revision** (`.mfd-context-menu`) → the F-PLN line whose ident the FWS marked **`.selected`** (`MfdFmsFpln` `selectedForRevision.sub` adds class `selected` to the `identRef`); fallback `.mfd-button.opened`. A group whose opener can't be found falls back to the top. Verified live: expanding "F-PLN INFO" lists ALTERNATE/CLOSEST AIRPORTS/FIX INFO/… right under it; opening DER22's revision lists DIR TO/INSERT/DELETE*/HOLD/… right under DER22, then the route continues.

**Combo boxes announce their SELECTED value.** A DropdownMenu combo carries its value in `.mfd-dropdown-inner` (already read). Button-style dropdowns (ARRIVAL/DEPARTURE RWY/APPR/STAR/TRANS) have NO inner value — the field name is the button text and the selection sits in a summary grid directly beneath the matching header. `A.comboSelectedValue(label, node)` finds the header text cell equal to the button label, then reads the value cell immediately below it (≤45px, x-overlapping); dashes → "" (unset). `A.lineText` folds it in so the combo reads "RWY, 30R" not bare "RWY" (verified live: RWY→"30R", APPR/VIA/STAR/TRANS→unset). Combined with the expand-state this reads e.g. "RWY, 30R, combo box, collapsed".

**Silent on first aircraft detect.** Aircraft-specific `ProcessSimVarUpdate` branches announce directly (bypassing the `simVarMonitor` 5s grace), so the A380 altimeter setting spoke on first detect (its `_lastBaroL`/`_baroStdL` caches start at sentinels, so the first real sample read as a change). Fix is global + minimal: `ScreenReaderAnnouncer.Suppressed` drops the AUTOMATIC paths (`Announce`/`AnnounceQueued`/`AnnounceWithQueue`) but NOT `AnnounceImmediate` (user hotkeys still talk); `MainForm` sets it true on connect and false when the existing 5-second grace timer fires (`MainForm.cs:457-467`). The per-var caches still update under suppression (branches run, just don't speak), so the FIRST post-grace change announces correctly. Consistent with the existing `_tlaBaselineDone` thrust-lever suppression and the `simVarMonitor`/ECAM grace.

**D / Shift+D distance hotkeys (FMS progress).** PMDG-style: **D** = distance to destination, **Shift+D** = distance to Top of Descent. The A380 has no PMDG SDK struct and no stock SimVar for these, so they come from the MFD's FMS guidance controller via the Coherent debugger. `coherent-a380-agent.js` `A.flightInfo()` returns `{distToDest, distToTD, distToTC, timeToTD, timeToTC}` (JSON): `distToDest` = `guidanceController.alongTrackDistancesToDestination.get(0)` (active plan, works on the ground — verified 1810 NM VCBI→OMDB); `distToTD/TC` derived from `currentPseudoWaypoints` (ident `(T/D)`/`(DECEL)`/`(T/C)`, `distanceFromStart`) via `toGo = distToDest - (totalPlanDist - pwp.distanceFromStart)`, where `totalPlanDist` = last leg `calculated.cumulativeDistance` — these are null until airborne (FMS computes the descent in flight). Wiring: the keys are already globally bound (`HotkeyManager` D=0x44 / Shift+D → `ReadDistanceToDest`/`ReadDistanceToTOD`, the same actions the 787 uses) → `FlyByWireA380Definition.HandleHotkeyAction` casts `parentForm` to `MainForm` and calls `MainForm.AnnounceA380FlightInfo(tod)` → async `CoherentDebuggerClient.EvalForResultAsync("__MSFSBA_A380.flightInfo()")` → parse + `AnnounceImmediate`. **Shift+D also announces the TIME to top of descent** — `flightInfo()` adds `timeToTD`/`timeToTC` from the matched pseudo-waypoint's `flightPlanInfo.secondsFromPresent` (the FMS's OWN time-to-go, which accounts for the descent/decel/wind profile — more accurate than distance/ground-speed; null until the FMS computes the descent), and `MainForm.FormatEtaSeconds` formats it as `": HH:MM:SS"` to match the PMDG TOD readout EXACTLY (mirrors `PMDG737Definition.FormatEtaFromDistance`), e.g. **"145 miles to top of descent: 00:16:58"** — live-verified in flight. Destination ETA/EFOB still not included. **The A32NX has the SAME D/Shift+D readout** (added later; identical wording/format). It reads its OWN FMS guidance controller — `document.querySelector("a32nx-mcdu").fsInstrument.legacyFms.guidanceController` — which exposes the same `alongTrackDistancesToDestination.get(0)`, the `(T/D)`/`(T/C)` pseudo-waypoint with `flightPlanInfo.distanceFromAircraft`/`secondsFromPresent`, and `A32NX_FMGC_FLIGHT_PHASE` as the A380. The A320 drives its MCDU over the **SimBridge relay** (NOT the Coherent MCDU bridge), so it can't reuse the A380's persistent `CoherentDebuggerClient`; instead `MainForm.AnnounceA32NXFlightInfo` runs a **ONE-SHOT** eval of the self-contained `Resources/coherent-a32nx-flightinfo.js` via the lightweight `SimConnect/CoherentEvalClient` (resolve the `A32NX_MCDU` view → eval → close, no agent/scrape-loop, zero A380 risk). `FlyByWireA320Definition.HandleHotkeyAction` routes `ReadDistanceToDest`/`ReadDistanceToTOD` there, and BOTH jets share the parse+announce (`MainForm.AnnounceFlightInfoJson`) so they read identically (PMDG-format TOD, phase-based "Past top of descent"). Live-verified `distToDest` on the A320; the TOD/time path uses the same pseudo-waypoint structure as the A380 (verify on an A320 descent). Destination ETA/EFOB still not included.

**MFD action auto-refresh (dropdowns / company-fpln insert).** `FBWA380MCDUForm` only re-fetched the page on navigation, never after a `click_mcdu_element`. So activating the company-fpln dropdown button opened its `INSERT*`/`CLEAR*` menu in the sim but the list never updated to show them — the action read as "nothing happened" (the user's "Enter didn't click"). `ScheduleRefresh()` (a 450 ms post-action re-fetch, mirroring the page-selector) now runs after every click, field-commit and clear, so dropdown menu items and page changes appear. Insertion path itself = "RECEIVED CPNY F-PLN" button (Enter) → menu appears → arrow to `INSERT*` → Enter.

**External-power / GSX announcement fix.** The GPU-available vars read the FBW L-var `A32NX_EXT_PWR_AVAIL:n`, whose name literally contains a colon index; the SimConnect data-definition path (`L:{Name}`) mis-parses `:n` as an index of a (nonexistent) base var, so the value was never read and GSX connecting ground power announced nothing. Switched to the **stock indexed simvar `EXTERNAL POWER AVAILABLE:n`** (`SimVarType.SimVar`) — natively indexed via data defs AND exactly what GSX / stock ground power set (verified live: `:1`=1 with GSX power). NOTE: the only other colon-indexed L-var in the def is `A32NX_AUTOTHRUST_TLA:n` (thrust-lever angle → detent); if that announce is ever found broken, it has the same root cause.

**Page navigation.** The MCDU window steps pages on Ctrl+Up = previous and Ctrl+Down = next (plus PageUp/PageDown), each announcing "Previous/Next page" + re-fetching the page (the new title also auto-announces). The MFD has one LINEAR page order (plus vertical scroll within a page) — no horizontal page axis — so Left/Right are NOT bound to paging. Plain arrows move the line cursor.

**KCCU keys reach the MFD ONLY via its own EventBus (key finding).** `Coherent.trigger("H:A32NX_KCCU_L_<KEY>")` and `SimVar.SetSimVarValue("H:…")` do NOT reach the MFD from the external debugger — verified live (even a PERF *navigation* key was ignored), which is why F-PLN scrolling and KCCU input silently did nothing. The MFD subscribes to `'hEvent'` on its **own msfs-sdk EventBus**, reachable at `document.querySelector('a380x-mfd').fsInstrument.bus` (the `<a380x-mfd>` custom element also exposes `fsInstrument.fmcService` and `mfdCaptRef`/`mfdFoRef`). `coherent-a380-agent.js` `A.fireKey` now publishes the KCCU H-event there via `A.mfdBus()` (`bus.pub('hEvent','A32NX_KCCU_L_<KEY>',true)`), Coherent/SimVar as fallback. This makes **every** KCCU key work — navigation, EXEC, and F-PLN line scroll. **F-PLN scrolling is solved:** KCCU UP/DOWN moves the window one waypoint, so `A.pageCommand` fires the scroll key 6× for a page; the MCDU window's Ctrl+Up/Down / PageUp/PageDown now walk the whole route (verified: DER22→BASUR→DER22). The EventBus is also the path to `fmcService` (`fmcService.master.flightPlanInterface.active.allLegs` = the leg list). A user-facing "whole route" reader from `allLegs` was prototyped then REMOVED — scrolling is preferred, and notably the DOM F-PLN scroll renders MORE than `allLegs` (the full approach/missed-approach fixes, e.g. DB520…OMDB30R…HOLDR, which `allLegs` collapses to "IMPED, ‹disco›, CF, OMDB30R"), so the scroll is the more complete view. **Scroll verified non-skipping:** paging the F-PLN (6 KCCU-DOWN per page) over a ~8-waypoint window leaves a ~2-waypoint overlap between consecutive windows, so the whole route reads in sequence with nothing skipped (checked window-by-window VCBI22→OMDB approach). **F-PLN paging flakiness fix:** `coherent-a380-agent.js` `fireKey` was firing each KCCU H-event TWICE (`Coherent.trigger` AND `SimVar.SetSimVarValue("H:…")`), so F-PLN page up/down double-scrolled / behaved erratically; it now fires ONCE (Coherent.trigger, with the SimVar write only as a headless-eval fallback). Verified live: one page-down scrolls the F-PLN by exactly one window (VCBI22→BASUR). Setting the **arrival** (STAR/approach) uses the destination waypoint's lateral revision — scroll the F-PLN to the OMDB line at the end, activate it, choose ARRIVAL → approach; the bottom DEST/OMDB summary buttons do NOT open the revision.

**flyPad non-dashboard pages + KCCU.** The column-first read order is scoped to the Dashboard (`A.isDashboard`) — it was the only side-by-side two-widget page. Other EFB pages (Performance TOD calc, Ground, etc.) are single-widget/single-column and read acceptably top-to-bottom; generalize the column split only if a specific page is reported jumbled (a blind global split would break single-column pages). KCCU: MSFSBA drives the MFD via DOM click + `keypress` (the reliable path per `coherent-a380-agent.js`), NOT KCCU H-event emulation — the KCCU keys (`A32NX_KCCU_L_*`) are the cockpit hardware path and are less reliable for an external tool; DOM input is equivalent or better, so "using the KCCU" is not required.

**Native-additions audit (2026-05).** Most high-value A380 native data is ALREADY surfaced: autobrake selected/armed mode + RTO + `A32NX_BTV_STATE` + brake temps 1-16 (def §brakes), FQMS gross weight/CG/FOB + pax + DEST-EFOB-below-min (System Display window + Status), RMP active/standby freqs + squawk, EWD memos/warnings (EWD agent). FMS PERF V-speeds are NOT L-vars on the A380 (only `A32NX_SPEEDS_MANAGED_SHORT_TERM_PFD`) → would need an MFD PERF-page scrape.

**ROW/ROP runway-overrun protection (landing/taxi safety) — ADDED.** `A32NX_ROW_ROP_WORD_1` and `A32NX_OANS_WORD_1` are plain (non-indexed) ARINC429 discrete L-vars, registered Continuous+IsAnnounced and decoded in `ProcessSimVarUpdate` with `Arinc429Word.BitValueOr` (1-based, SSM-gated — identical to the FBW PFD `AttitudeIndicatorWarnings`). Bits → call-outs: ROW_ROP_WORD_1 **13 = "Max braking"**, **14 = "If wet, runway too short"**, **15 = "Runway too short"**; OANS_WORD_1 **11 = "Runway ahead"**. Announced on each bit's RISING edge (tracked in `_rowRopActive`), silent at baseline (verified live: ROW_ROP=`0x3_00000000` SSM-NormalOp value 0; OANS=0 → SSM invalid → fallback false). Honours the Ctrl+M mute.

**BTV (Brake-To-Vacate) rollout distance call-outs — ADDED.** During the landing rollout MSFSBA speaks the distance to the selected runway exit and the runway remaining as they count down. `A32NX_OANS_BTV_REMAINING_DIST_TO_EXIT` and `A32NX_OANS_BTV_REMAINING_DIST_TO_RWY_END` are **ARINC429 metres** (that's why a raw `get_lvar` reads 4294967296 = 2^32 when unset — decode with `Arinc429Word.ValueOr` + `IsNormalOperation`, like the ROW/ROP words). Registered Continuous+IsAnnounced; decoded in `ProcessSimVarUpdate`, announced as each descends through fixed thresholds (exit: 1200/800/500/300/150 m; rwy-end: 900/600/300/150 m), once each, then reset between landings. Gated on the **BTV state** (`A32NX_BTV_STATE`, now `Mon` = auto-announces its transitions Armed/Rotation Optimised/Decel/End of Braking, and captured into `_btvState`): distance call-outs only fire while state is Rotation Optimised(2)/Decel(3), i.e. the actual braking rollout — so they cannot fire on approach or the ground. Phrases: "{N} meters to exit", "{N} meters runway remaining". Honours the Ctrl+M mute (entries "BTV Distance to Exit", "Runway Remaining", "Brake To Vacate"). Verified: vars exist + are ARINC429 (2^32 sentinel on the ground, BTV_STATE=0); **the rollout numbers/cadence need verifying on a real landing**. Other BTV vars available if wanted: DRY/WET/STOP_BAR_DISTANCE_ESTIMATED (plain metres), OANS_RWY_LENGTH/BTV_ROT (ARINC). KCCU note: `A32NX_KCCU_{L,R}_KBD_ON_OFF` verified = 0 in the powered cockpit (FBW defaults to touchscreen), so sighted players have the KCCU keyboard OFF — MSFSBA leaves it off to match (no cosmetic write).

**Thrust-lever startup-spam fix.** The detent announce (`ProcessSimVarUpdate`, `A32NX_AUTOTHRUST_TLA:n`) began with `_lastEngDetent`/`_lastAllDetent` = null, so first start read out "Thrust lever 1/2/3/4 Idle, All thrust levers Idle". A `_tlaBaselineDone` guard now records the baseline detents SILENTLY until all four levers have reported once; only real detent CHANGES after that announce.

**Announcement-quality patterns (2026-05 quality pass).**
- **ARINC429 enum guard.** Some FBW discretes are written `write_arinc429`, so an announced enum var arrives as a huge SSM-encoded word (e.g. `A32NX_APU_LOW_FUEL_PRESSURE_FAULT` = 12884901888 = 0x3_00000000 = SSM NormalOp, payload 0) that matches no 0/1 entry → the generic announce printed "<label>: 12884901888". `ProcessSimVarUpdate` opens with a generic guard: any ANNOUNCED enum var (`_varCache` lookup, `IsAnnounced`, `ValueDescriptions.Count>0`) whose raw value is ARINC-large (`>= 2^32`) is decoded via `Arinc429Word.ValueOr(0)` to its 0/1 payload, announced via its `ValueDescriptions` ON CHANGE only (default prev=0 so the initial no-fault is silent), honouring the Ctrl+M mute, and returns true. Future-proof for every such discrete. (Announced ARINC NUMERICS — EIS baro, minimums — are handled by their own `MonNum` + custom decode, so they have no ValueDescriptions and skip this guard.)
- **Annunciators are NOT panel read-out fields.** `GetPanelDisplayVariables` ends with a filter that strips every ANNOUNCED 2-state enum var (faults + non-fault binary lights) from the display sets — a pilot navigates a panel to operate CONTROLS, not to scan "X Fault: Normal" rows. KEPT: numeric/analog read-outs, multi-state (3+) status (ADIRU/engine state, autobrake/BTV/FMA modes), and the intentionally-silent `ReadEnumQuiet` binaries (their only surface). The stripped annunciators still auto-announce on change and stay in the Ctrl+M monitor (which lists every `Continuous+IsAnnounced` var regardless of type, so SimVar-state combos like the engine masters/crossfeeds/doors are included).
- **No double-announce: annunciator L-var vs ECAM memo.** When a status has BOTH a PB-light L-var and an E/WD memo, the L-var is made `ReadEnumQuiet` and the memo (from the scrape) is the single call-out — e.g. `A32NX_OVHD_APU_START_PB_IS_AVAILABLE` was announced twice ("APU Available: Available" + "APU AVAIL") → now only the memo speaks.

**PFD memo + limitations (string SimVars, scrape-read).** FwsCore publishes the 3 PFD MEMO lines + 8 PFD LIMITATIONS lines as FBW **'string'** SimVars (`A32NX_PFD_MEMO_LINE_1..3` / `A32NX_PFD_LIMITATIONS_LINE_1..8`), NOT numeric codes — so MSFSBA's numeric SimConnect read returns 0 and never sees them. The E/WD scrape agent (`coherent-ewd-agent.js` `A.pfdLines()`) reads them as strings from the Coherent JS context (`SimVar.GetSimVarValue(..., 'string')`, FWC codes stripped), and `CoherentEWDClient` announces them on change with the same baseline + dedup as the memos (SET HOLD SPD, SPEED LIM, …). The **INOP SYS** and **SD STATUS** lists are EventBus/DOM-only (like the abnormal procedures — `inopSysAllPhasesKeys`/`limitationsAllPhasesKeys` are EventBus Subjects, not SimVars), so they would need a DOM scrape of the STATUS area to surface. Source-mining note: the OIT "FLT OPS" terminal is a second EFB-like app (login/flight-folder/charts) — redundant with the accessible flyPad EFB and shows "not available" without the avionics network, so not exposed; BAT/FCU/RMP display content is already covered by the SimVar path.

**ECAM E/WD monitoring — TWO paths (memos via SimVar, failures via DOM scrape).** The A380 FwsCore splits its output: MASTER WARNING/CAUTION + the MEMO columns are on L-vars, but the sensed abnormal/warning PROCEDURES (failure titles + ECAM action items) are published on an in-process **EventBus, NOT SimVars** — they exist only as rendered text in the `A380X_EWD` Coherent view's DOM. So there are two readout paths, deliberately non-overlapping:
- **Memos (SimVar path).** Upper E/WD memo lines `A32NX_EWD_LOWER_LEFT/RIGHT_LINE_1..10` (uppercase `EWD`, 10/side — vs A320's `Ewd`/7; both are MEMO columns) are numeric message CODES decoded via `SimConnect/EWDMessageLookupA380.cs` (1507-entry table: ported A380 `EcamMemos`/`EcamInfos`/`EcamLimitations`/`EcamInopSys` + abnormal titles; own ANSI stripper using fixed `` escapes) and announced live in `ProcessSimVarUpdate`. Dedup is per-SET via `_announcedEwdCodes` HashSet (kills scroll re-announce).
- **Failures (DOM scrape path).** `SimConnect/CoherentEWDClient.cs` is a **background monitor** (started on A380 load in MainForm, no window needed) that connects to the `A380X_EWD` Coherent view, installs `Resources/coherent-ewd-agent.js` (`window.__MSFSBA_EWD`, ES5), and polls `scrape()` once/sec. The agent reads each `.EclLine.AbnormalItem` row's **SVG-rendered tspans** (the clean text — the sibling `.EclLineText` span carries raw FWC codes `<4m`/`4m`/`m` and the checkbox holds `□`, so those are skipped). The C# client cleans each line (leader-dot runs → ", ", strip leading "-"), **filters the fixed ABN-PROC manual-procedure menu** (ABNORMAL PROC / SMOKE-FUMES / EMER EVAC / ENG / F/CTL / L/G / NAV / FUEL / MISCELLANEOUS / … — exact-match so "F/CTL PRIM 1 FAULT" ≠ the bare "F/CTL" menu entry), dedups by text, and **silently baselines on first scrape** (only failures appearing AFTER connect announce — matches every other MSFSBA monitor, avoids re-reading the whole screen on reconnect). Honours the Ctrl+M/Ctrl+E `FBWA380_ECAM_MEMOS` mute. The client ALSO announces the DOM `memos[]` (PARK BRK ON, ELEC EXT PWR, …) so the E/WD auto-call-outs come from ONE source — and while the scrape monitor runs, the SimVar `EWD_LOWER` memo auto-announce is suppressed (via the def's `EwdScrapeHandlesAnnounce` flag, set by `StartA380EWDMonitor`) to avoid double speech; the SimVar dedup sets are still maintained so the on-demand `ReadAllEwdWarnings` (Alt+E) decode works, and the flag defaults false so SimVar still announces if no scrape monitor is active. Live-verified end-to-end on a triggered PRIM 1 failure → announces "F/CTL PRIM 1 FAULT", "PRIM 1, OFF THEN ON", "IF NOT SUCCESSFUL :", "LDG DIST AFFECTED", and the FWS 1+2 & CPIOM FAULT items. The failure TITLES still in EWDMessageLookupA380 are dead weight for SimVar decoding (failures never appear as SimVar codes) — kept only as a code→text fallback.

**Live display DOM-scrape (SD / E/WD / ND / PFD / ISIS) — the generic readout core.** Every A380 cockpit display is a Coherent GT view with a DOM, so the crew-visible DECODED text (oxygen PSI, GW/FOB, per-tank fuel, N1/EGT, temps, pressures, doors, FMA, baro, …) can be read directly — which beats the SimVar path for TEXT-based displays (it needs no ARINC429 decode and catches values the SimVar set never modelled). Scrapability splits cleanly: **text displays win** (SD decoded values, E/WD, MCDU, flyPad); **graphical tape displays** (PFD/ND/ISIS current airspeed/altitude/attitude) are POSITIONAL so a flat scrape returns scale tick-marks, not readings — their SimVar windows stay primary there. Core pieces:
- `Resources/coherent-display-agent.js` (`window.__MSFSBA_DISP`, ES5) — GENERIC row reconstruction: collect leaf text elements (skip ones with element children), cluster by Y (±12 px), sort each cluster by X, join with spacing → readable rows exactly as a sighted pilot reads the screen. De-dups stacked outline+fill glyph copies. Works on any display view.
- `SimConnect/CoherentDisplayClient.cs` — GENERIC, title-needle-parameterised reader; installs the agent, polls `scrape()`, raises `RowsUpdated` only on change; `SetActive(false)` pauses polling when no window is open; `ScrapeNowAsync()` for F5.
- **SD window (ShowStatusPage/ShowECAM) — REPLACED** with the live scrape (`FBWA380SystemDisplayForm`): a page selector drives `A32NX_ECAM_SD_CURRENT_PAGE_INDEX` (verified writable — index map from `SD/SystemDisplay.tsx`: 0 ENG,1 APU,2 BLEED,3 COND,4 PRESS,5 DOOR,6 ELEC-AC,7 ELEC-DC,8 FUEL,9 WHEEL,10 HYD,11 F/CTL,12 C/B,15 VIDEO,-1 default) so the user reads ANY page decoded — including Door/Wheel/F-CTL/C-B/Video the old ARINC429 decode never covered. The legacy SimVar+ARINC429 render is an automatic fallback when the SD view is unreachable.
- **`FBWA380LiveDisplayForm`** — generic live-scrape window for the rest: E/WD (`ReadDisplayUpperECAM`/Alt+E also opens it — engine N1/EGT/FF + memos + warnings), and ND/ISIS ("&Live scrape" button + **F6**) and PFD (shared `PFDForm`, **F6** gated to `AircraftCode=="FBW_A380"`, no visible button so the A320 is unaffected). For the tape displays this AUGMENTS the SimVar window (positional values read as ticks).

**Monitor Manager (Ctrl+M) + ECAM toggle (Ctrl+E).** `FBWA380MonitorManagerForm` dynamically lists every auto-announced var (Continuous + IsAnnounced) with checkboxes, persisting un-ticked keys to `UserSettings.A380DisabledMonitorVariables` (consulted in `MainForm.OnSimVarUpdated`; the synthetic `FBWA380_ECAM_MEMOS` key mutes ECAM call-outs in `ProcessSimVarUpdate`). `HotkeyAction.ToggleECAMMonitoring` (Ctrl+E) flips that sentinel directly. **Annunciators auto-announce:** overhead pushbutton FAULT lights and system warnings are `ReadEnum`/`Mon` (Continuous+IsAnnounced) so they speak on change and the user can mute any in Ctrl+M — covers HYD per-engine A/B + electric-pump faults, ELEC emer-gen-line, COND ram-air + TADD, and the APU EGT / thrust-lever / performance / TCAS warnings (var names from the FBW A380 source). Mirrors the Fenix/PMDG monitor managers. The framework auto-monitor, read-only per-panel display fields, and combo auto-update-on-change are all free (driven off `GetVariables`/`GetPanelDisplayVariables`).

**Most names are verified against FBW docs/source, not the running sim** — but a chunk has now been live-verified: FCU set/readback + SI conversions, V/S sign, managed-mode dot, EFIS baro decode/STD/QNH/unit + `KOHLSMAN_SET`, minimums decode, the #59 FCU toggle events (AP1/AP2/LOC/APPR/EXPED = `A32NX.FCU_*_PUSH`; A/THR = stock `AUTO_THROTTLE_ARM`, NOT a dot-event; TRK/FPA = direct L-var write, no event; SPD/MACH = `A32NX.FCU_SPD_MACH_TOGGLE_PUSH`), all EFIS-CP controls via the MobiFlight calculator path (`ExecuteCalculatorCode("{val} (>L:{key})")` — `SetLVar`/data-def write is unreliable for FBW L-vars), thrust-lever detents, reversers (inboard 2/3 only), and that the FD is uncontrollable. Still flagged uncertain / need an in-sim pass: SURV pushbuttons, RMP per-channel TX, wing/pitot anti-ice write path (#56). Hotkey guide: `HotkeyGuides/FBW_A380_Hotkeys.txt`.

**#103 BREAKTHROUGH — the "computed-output PBs revert" conclusion was WRONG; ALL overhead pushbuttons are settable (live Coherent + calculator-path write-then-readback, 2026-05).** `HandleUIVariableSet` ends with a catch-all that routes every settable `A32NX_OVHD_*` / `A380X_OVHD_*` / `A32NX_KNOB_OVHD_*` / **`XMLVAR_*`** combo through the MobiFlight calculator path (`ExecuteCalculatorCode("{int} (>L:{key})")`) instead of MainForm's generic data-def `SetLVar`. **The earlier #60 verdict that PACK 1-2, HOT AIR 1-2, ENGINE 1-4 BLEED, CABIN/AIR-EXTRACT FANS, the HYD ENGINE/ELEC PUMP PBs, ELEC BUS-TIE/GALLEY, HYD PTU and the EMERGENCY-EXIT sign are "computed outputs that revert and cannot be set externally" was an ARTIFACT of testing with the MCP's native data-def write (`set_lvar` / `AddToDataDefinition` / `SetDataOnSimObject`), which is unreliable for FBW L:vars — exactly as the data-def READS are.** Re-tested live with the CALCULATOR path (`{val} (>L:{var})`, the one MSFSBA actually uses): EVERY one STICKS in both directions for 3+ s (pack set OFF stayed OFF; bus-tie/galley/PTU/HYD-eng-pumps/HYD-elec-pumps/eng-bleeds/cabin-fans/air-extract/hot-air all stuck; the EMEREXIT XMLVAR stuck). Source confirms why: the Rust `OnOffFaultPushButton` **READS** `{name}_PB_IS_ON/_IS_AUTO` as the pilot input every frame and only **WRITES** the `_HAS_FAULT` output — so an external set to the IS_ON/IS_AUTO var IS the button press (the `write_by_name("..._PB_IS_ON")` calls in `air_conditioning/mod.rs` are in `#[cfg(test)]` harness code, not the controller). **So all these overhead combos actuate correctly through the calculator path — there is NO hard FBW limitation.** These were already registered as settable combos (`OnOff`/`OffAuto`/`Sel`) and already routed through the OVHD calculator catch-all, so the OVHD ones worked already; the only real gap was the **`XMLVAR_` sign combos** (No Smoking, Emergency Exit, Altitude Increment) which fell through to the unreliable data-def path — **now routed through the calculator path too** (added `XMLVAR_` to the catch-all). **General lesson: to test whether an FBW L:var write sticks, ALWAYS use the calculator path (`(>L:VAR)` via `execute_calculator_code` or Coherent `SimVar.SetSimVarValue`), NEVER the data-def `set_lvar` — the latter silently fails for many FBW vars and produces false "reverts/uncontrollable" conclusions.** The only PBs that genuinely need a stock event are the seatbelt sign (`CABIN_SEATBELTS_ALERT_SWITCH_TOGGLE`, handled earlier) and engine anti-ice (`ANTI_ICE_SET_ENGn`).
- **Writable single-writer PBs (always worked):** COMMERCIAL, IDG disconnect, EXT PWR, battery selector, all FUEL transfer + jettison PBs, DITCHING, RAM AIR, APU BLEED, lighting selectors.

Other #60 findings:
- **Non-OVHD FBW settable L-vars are writable** via the calculator path — verified live for SURV (`A32NX_SWITCH_RADAR_PWS_Position`, `A32NX_RADAR_MULTISCAN_AUTO`, `A32NX_RADAR_GCS_AUTO`), GPWS (`A32NX_GPWS_*_OFF`), and the source-switching knobs (`A32NX_FMS_SWITCHING_KNOB`, `A32NX_ATT_HDG_SWITCHING_KNOB`, …). These still go through MainForm's generic data-def `SetLVar` (not the catch-all, which is OVHD-only) and that works for them — left as-is to avoid touching the many controls currently functioning.
- **Fuel crossfeed was broken → fixed.** The four XFEED combos wrote `XMLVAR_Momentary_PUSH_OVHD_FUEL_XFEED{n}_Pressed`, which is only the cockpit model's press-animation flag and never actuates the valve (verified: pulsing it left `FUELSYSTEM VALVE SWITCH:n` unchanged). The FBW `FBW_Airbus_Fuel_Crossfeed` template drives `#ID# (>K:FUELSYSTEM_VALVE_TOGGLE)` with valve IDs **46-49**. Now a single **Closed/Open combo** per valve (state = SimVar `FUELSYSTEM VALVE SWITCH:id`, set fires `FUELSYSTEM_VALVE_OPEN`/`_CLOSE` via `HandleUIVariableSet`). OPEN/CLOSE (not TOGGLE) because TOGGLE was observed to stick mid-transition. NOTE: the MCP's `trigger_event` does not know the `FUELSYSTEM_VALVE_*` family ("not found"), but MSFSBA's `SendEvent` (`MapClientEventToSimEvent` + `TransmitClientEvent`) maps them by raw name and they work — proven by the engine masters. The momentary-button rule generalises: **a `_PRESSED`/`_Pressed` XMLVAR is almost always a model-only animation flag, not the actuator** — find the real K-event or state var instead of pulsing it.

**ECAM-CP and momentary/test controls are now BUTTONS (user request 2026-06, reversing the all-combo rule for these).** The `Btn` and `PressSilent` helpers render `RenderAsButton = true` and route the click through the `_momentaryButtons` path (pulse the L:var 1→0, speak "<name> pressed") — so every A380 ECAM-CP key (ALL/ABNPROC/CL/CLR/CLR2/RCL/TOCONFIG/EMERCANC/UP/DOWN/MORE/CHECK_LH/CHECK_RH) plus the chronometer Start-Stop / Reset is a push-button. HOLD tests (Fire Test, the new **Cargo Smoke Detection Test** `A32NX_FIRE_TEST_CARGO`) use a new `ToggleBtn` helper (`_toggleButtons`): the click reads the current state, writes the opposite via the calculator path, and announces the new On/Off. The **System Display Page selector stays a combo** (it's a real selector, not a button). Mirror this on the A32NX (its ECAM keys are HVar PressEvent/ReleaseEvent — a different mechanism, convert with care). Fenix is the template: it renders ECAM-CP controls as `RenderAsButton` L:var buttons (optionally with a `StateVariable` indicator) and the generic MainForm button path fires + announces.

**Generic ARINC429 auto-decoder.** `SimVarDefinition` has `IsArinc429` + `Arinc429Unit`/`Arinc429Format`/`Arinc429NotAvailableText`; `BaseAircraftDefinition.TryDecodeArinc429(varKey, value, out text)` decodes any flagged var via `Arinc429Word` ("<value> <unit>" on NormalOp/FuncTest, else not-available). Hooked in `MainForm.UpdateDisplayText` (after `TryGetDisplayOverride`, so ad-hoc baro/minimums/rudder decoders still win) and the announce path (only for vars `ProcessSimVarUpdate` didn't handle, so no double-decode). Flag any new ARINC var (BTV distances, FQMS/PRESS percentage words) `IsArinc429 = true` and it surfaces decoded on panels + announce instead of a raw ~14-billion word — no per-var case needed.

**Ground services + calls (A380).** The cabin/mechanic Calls push-buttons (`PUSH_OVHD_CALLS_ALL/FWD/AFT/MECH`) are panel controls. (Earlier `A380X_GND_*` truck "Activate" combos firing `REQUEST_FUEL_KEY`/`REQUEST_LUGGAGE`/`REQUEST_CATERING` were NEVER added to the A380 def — the trucks are requested on the flyPad Ground page, and MSFSBA auto-announces their connection via the door monitor — see the "Ground-service AUTO-ANNOUNCE" note below. The only `A380X_GND_*` vars are chocks/cones/GPU-avail.)

**Surveillance panel + transponder/TCAS for VATSIM — the verdict (2026-06, FBW docs + live).** FBW's own docs state the **pedestal Surveillance panel is "not yet fully implemented" in the A380X** — so there is NOTHING to make accessible there (it doesn't exist as a working panel), and the **Weather Radar is inoperative**. ALL XPDR/TCAS/TAWS/WXR setup is done on the **MFD SURV CONTROLS page** (`surv/controls`, already in `AllPages` → reachable via the MCDU window's page picker; clickable radio buttons, live-verified). When FBW ships the pedestal panel, add it then. **Transponder IS genuinely AUTO-managed (a real A380 thing FBW models):** you set the SQWK code + click **AUTO** on the MFD SURV page; AUTO auto-selects the reply mode (Standby/Mode C) by air/ground state. The stock `TRANSPONDER STATE:1` (what vPilot/swift read on VATSIM) is what it drives, so it Just Works — STBY on ground, Mode C airborne (live-verified the systems-host holds STBY on ground and reverts any external write). **For TAXI you set the TRANSPONDER (XPDR AUTO + squawk), NOT the TCAS** — TCAS TA/RA is the in-flight traffic system, separate from the transponder ATC sees. MSFSBA surfaces the result as the read-only auto-announcing "Transponder Mode" (so the pilot hears "Mode C" on departure). **Flight-deck API sweep** (docs.flybywiresim.com/aircraft/a380x/a380x-api): the documented settable LVARs are all already exposed by MSFSBA (autopilot, baro, ND range, ADIRS, elec, packs, temp selectors, hydraulics, flaps/spoilers/gear/parkbrake/autobrake, lighting presets) — no transponder/TCAS settable var exists (confirms EventBus-only). **Cockpit-prep INOP list (do NOT expose as working):** RECORDER GROUND CTL, EVAC, ELT, BODY ACCUMULATOR PRESSURE, COCKPIT DOOR SWITCH, CVR TEST, OXYGEN MASKS TEST.

**Ground-service AUTO-ANNOUNCE when called via the flyPad (2026-06).** The flyPad Ground page drives every A380 service through the stock SimVar **`INTERACTIVE POINT OPEN:n`** (source: `fbw-common/.../EFB/Ground/Pages/Services/A380_842/A380Services.tsx`): cabin doors 0/2/9/10 (jet-bridge/stairs/catering attach to these), GPU = `A32NX_EXT_PWR_AVAIL:1-4`, **front cargo hatch (baggage truck) = point 16**, **fuelling (fuel truck) = point 18**. MSFSBA's door monitor (`_doorDefs`) covered points 0-15 + the 2 cargo-LOCKED L:vars, so a flyPad **baggage** or **fuel** call went UNannounced (the cabin-door/GPU services already announce via their doors / EXT PWR). Fix: added points 16 + 18 to `_doorDefs` (`A380X_MSFSBA_DOOR_16` "Front Cargo Door (Baggage)", `A380X_MSFSBA_DOOR_18` "Fuel Truck") so they ride the proven once-per-transition door announce (Continuous+IsAnnounced, `_doorOpen` edge-detect). The fuel-truck point (18) speaks **connected/disconnected** (special-cased verb in the door-announce branch); the cargo hatch keeps open/closed; both also show in the Doors status window. Live-verified both are valid readable SimVars (0.0 = closed on the ground). The trucks have NO separate "connection" var beyond their interactive point — these ARE the readable states; catering/stairs/jet-bridge have no point of their own (they reuse the cabin doors), so labelling them as distinct services isn't possible without a GSX dependency.

**RMP = a dedicated SCRAPED WINDOW (`Ctrl+Shift+R` input mode → `Forms/FBWA380/FBWA380RmpForm`), NOT a control panel.** The old per-key RMP button panel was impractical (one digit at a time) and was REMOVED (registrations, `p["Radio Management Panel"]` + its structure entry, the `A380X_MSFSBA_RMP1_` HandleUIVariableSet branch, the `_SET` freq field). The window scrapes the live `A380X_RMP_1` (Captain) / `A380X_RMP_2` (First Officer) Coherent views via `CoherentDisplayClient` + **`Resources/coherent-rmp-agent.js`** (a new ES5 agent that registers as `window.__MSFSBA_DISP` — same `{ok,rows}` contract as the generic display agent, so `CoherentDisplayClient` reads it unchanged; the client gained an optional `agentFileName` ctor param). The agent zips the flat `.active-frequency` / `.transceiver-ident` / `.stby-frequency-cell` rows by index and reads the `.stby-frequency` div's `textContent` (which concatenates its 6 digit children + dot into "121.950" — and shows the live SCRATCHPAD while typing), with `reverse-video`=transmit, `.selected`=the keyed row. **Live-verified both RMP_1 and RMP_2 scrape clean rows** ("VHF1: active 122.800, standby 121.950, transmit, selected" / VHF2/3 / message line). The window: a **Captain / First Officer / Overhead combo (RMP 1/2/3)** (re-points the scrape view `A380X_RMP_{n}` + the keypad H-event index `RMP_{n}_*`; the overhead RMP 3 was added 2026-06 — `A380X_RMP_3` view confirmed live + agent installs + `RMP_3_*` H-events accepted, structurally identical to the verified RMP 1/2; the `SendRmpKey*` guard was widened from `1|2` to `1..3`), a read-only multiline TextBox you read with the arrow keys AND type the digits into (no separate field — see below; the FBW RMP AUTO-COMPLETES — `8`→118.000, `11850`→118.500 — so the decimal is stripped, per `VhfComController.onDigitEntered`), and page buttons (VHF / Transponder) in the tab order; all soft keys are keyboard shortcuts firing the real cockpit keypad H-events. **Write path live-verified:** firing `(>H:RMP_1_DIGIT_8_PRESSED/RELEASED)` via the calculator path types into the standby scratchpad (the `KeypadController` only acts when the EventBus `RmpState` is On — which it IS even though the legacy L:var `A32NX_RMP_1_STATE` reads 0; ignore that var). Def helper: `SendRmpKey(rmp, key, …)` (a single keypad key, press+release) plus `SendRmpKeyPress`/`SendRmpKeyRelease` for the held full-clear, parameterised for RMP 1/2/3. (The old batched `SendRmpKeypad(text, …)` was removed — digits are keyed one at a time as the user types.) The **active VHF freq auto-announces on change** (`FBW_RMP_FREQUENCY_ACTIVE_n` → `MonNum` + a `ProcessSimVarUpdate` branch that formats raw-Hz→MHz, range-gated to 118–137 so the uninitialised ~19 MHz value while unpowered is cached silently; standby stays an on-request readout, so no duplicate). Source: `fbw-a380x/.../instruments/src/RMP/` (`KeypadController`, `VhfComPage`/`VhfComController`). The window is a **live region**: `FBWA380RmpForm.AnnounceChanges` speaks the SELECTED row's standby when it auto-completes/changes ("Standby 118.000") + any new RMP message, on every scrape change (silent on the first scrape / side switch); the active freq is intentionally NOT spoken here (the global active-announce owns it — no dup). **The old "Radios" panel (stock COM standby-set + swap) was REMOVED** — live-verified the FBW A380 IGNORES the stock `COM_STBY_RADIO_SET_HZ` / `COM*_RADIO_SWAP` events (set COM1 standby to 119.000 → stayed 121.95); all tuning is RMP-only, so the stock panel was dead. The read-only **"RMP" panel** (3 state readouts + the `FBW_RMP_FREQUENCY_*` active/standby freq box, `d["RMP"]`) is KEPT as a quick-glance in the panel list (the window is the interactive interface). **UI is MODELLED ON THE ACCESSIBLE MCDU/CDU FORMS (decluttered, keyboard-driven).** The display is a read-only multiline **TextBox** (the RMP touchscreen IS text), Consolas, focused on open, caret-preserved across refreshes. NO action buttons — every soft key is a keyboard shortcut using the SAME scheme as `FlyByWireMCDUForm`/`FenixMCDUForm`/`PMDG777CDUForm` (left=Ctrl, right=Alt, honouring the shared `MCDUUseAlternateLSKKeys` setting): **Ctrl+1/2/3 = `LSK_1/2/3`** (the left line keys — select the radio, or load the standby if already selected); **Alt+1/2/3 = `ADK_1/2/3`** (the right keys — swap active↔standby, MANUAL, never auto); F1/2/3 + F7/8/9 in alternate mode. Pages **Alt+V** (VHF) / **Alt+Q** (transponder — the only two the FBW build models) + buttons in the tab order; **Alt+C** = full clear; **Alt+Home** = focus the screen. (The VHF3 standby-mode cycler — keypad `UP`/`DOWN` → `vhf_com_set_standby_mode_3` — was wired but **live-verified to have NO observable effect in the FBW dev build** — the publish doesn't round-trip to `vhf_com_standby_mode_3` — so the `Alt+Up/Down` keys were removed; the agent still surfaces a `standby emergency` suffix from `.stdby-mode`=EMER if the mode is ever set elsewhere.) **There is NO separate entry field — you TYPE the digits on the read-only display itself** (`_display.KeyPress`→`SendRmpKey DIGIT_n` keyed live; the box is read-only so the digits go to the radio, not the text; Backspace→one `DIGIT_CLR`; no decimal — the RMP auto-completes), **Enter** (`_display.KeyDown`) = `LSK_{selectedRow}` (load standby). Arrow keys still read the screen. **Announce-on-change mirrors the MCDU scratchpad**: a 300 ms debounce timer speaks the selected radio's `Standby <freq>` when it changes AND any new RMP **message** (e.g. "VHF FREQ NOT VALID") — via `_announcer.Announce` (queued), silent on the first scrape. Two FBW gotchas remain the reason the flow needs care: (1) `onDigitEntered` early-returns while the entry is invalid, so a stuck bad entry blocks all digits — **Alt+C** HOLDS CLR past the FBW 1 s timer (`SendRmpKeyPress`/`SendRmpKeyRelease "DIGIT_CLR"`, 1150 ms) for a full clear; (2) typing only builds a scratchpad — Enter LOADs (LSK), swap is separate (ADK). 500 ms auto-poll; `RaiseRows` marshals to the UI thread via `_syncContext` (same as the working E/WD window). **Live-verified the data path end-to-end** (held-clear → real-time `118500` incl. repeated 1,1/0,0 → `LSK_1` → standby 118.500; `ADK_1` swaps active). NOTE: the A380 MCDU is cursor-driven (no LSK), so the RMP keyboard model is copied from the A320/PMDG CDU forms, not the A380 MFD — the RMP uses its own keypad H-events. **PAGES — the FBW A380 RMP dev build models exactly TWO** (`RmpPageStack` routes only `VhfPage`/`SquawkPage`): **VHF** (the 3 transceivers) and **Transponder/SQWK** (mode AUTO/STBY + squawk code + IDENT). HF/TEL/NAV/MENU do NOTHING (the page keys are dropped) — so the window offers only **VHF + Transponder** as buttons (in the tab order) + Alt+V/Alt+Q; the dead keys were removed. The page selectors are live-verified both directions on Captain AND First Officer. The agent was page-aware-fixed: the hidden page's rows still exist in the DOM (display:none, rect 0×0), so the transceiver loop now **skips rows where `!vis(actives[i])`** and the transponder reader keys on `.transponder-mode`/`.transponder-code .cyan`/`.transponder-ident` — otherwise the SQWK page read the hidden VHF rows (and vice-versa). SQWK-page actions route the same keys differently (the RMP routes page keys to the active page): digits = squawk entry, Enter (`LSK`) = `onValidate`, Alt+3 (`ADK_3`) = `onIdentPressed`; source `TransponderPage.tsx`/`TransponderController.ts`.

**RMP REDESIGN (2026-06 — SUPERSEDES the scrape-standby + SQWK-keypad description above; user-verified working).** After many live iterations the window (`FBWA380RmpForm`) was reworked so NO readback depends on a racy scrape. The scrape is still used for the freq DISPLAY + the message line, but never for the spoken readback. Key changes:
- **Unified page-mode screen.** One read-only screen TextBox for both pages; **`Alt+V`** = VHF page, **`Alt+T`** = Transponder page (these also drive the cockpit RMP to the matching page, best-effort). The page buttons have NO mnemonic — `Alt+V`/`Alt+T` are handled in `OnFormKeyDown` so they can't steal focus onto a button. The standalone "Squawk" field was removed (the unified screen does both).
- **VHF readback is computed from the TYPED digits, NOT the scrape.** The FBW autocomplete settles over SEVERAL FRAMES after the last keystroke (`VhfComController.onDigitEntered`: a completed entry = `entered.padEnd(6,'0')` shown `XXX.XXX`), so a single/polled scrape catches a transient (`123`→123.000 while the final is 123.500 — the user's bug). The form now remembers the typed digits (`_vhfEntry`) and formats the readback with that exact rule (`FormatVhfEntry`): `123500` and `1235` both → "VHF standby N, 123.500"; `12345`→123.450. Falls back to a scrape-settle read only for the rare leading-omitted shortcut (`8`=118.000) that doesn't form a 118–137 MHz value. Announced on the typing-settle debounce (~350 ms) and FORCED on Enter / re-press of the selected radio.
- **Row selection is authoritative from `Ctrl+1/2/3`; the scrape syncs `_selectedRowIndex` ONLY on the first scrape (init).** FBW rule (`VhfComPage` onLineSelectKey/onAdkKey): an LSK/ADK on a NON-selected row only SELECTS it; a SECOND press on the already-selected row loads (LSK) / swaps (ADK). A per-poll `_selectedRowIndex` sync RACED the LSK-select registration lag and reset a fresh VHF2/3 selection back to row 0 (symptom: "says VHF standby 1, won't swap"; VHF1 worked only because it's the default selected row). Now it's synced once then driven by `Ctrl+N`, and the form's own LSK presses keep the cockpit in sync.
- **Squawk via the stock `XPNDR_SET` event — RMP-page-INDEPENDENT.** The page-switch + keypad + auto-validate chain was unreliable to drive from the app (the squawk silently never set). On the SQWK page typed digits build a 4-digit (0–7) code committed on the 4th digit (or Enter) by **`def.SetSquawkFromForm`** = `0x{code} (>K:XPNDR_SET)` (live-verified to set `TRANSPONDER CODE:1` regardless of RMP page). It speaks "Squawk NNNN" once and primes the `XPNDR_CODE` monitor (`_formSetSquawkBcd`) to skip its duplicate (no race: the monitor matches the VALUE, not a timestamp). **IDENT = `Alt+I`** → `def.SendTransponderIdent` = `(>K:XPNDR_IDENT_ON)`.
- **Transponder mode readout is EXACT** — `XPNDR_STATE` enum verified against FBW `RMP/Data/TransponderPublisher.ts` + `MFD/SURV`: **0 Off · 1 Standby · 2 Test · 3 Mode A · 4 Mode C · 5 Mode S**. Mode S (5) on the ground in AUTO is CORRECT, not a bug (an earlier decode mislabelled 5 as "Mode C"). The squawk line is built from the reliable stock simvars (`XPNDR_CODE` BCO16 decode + `XPNDR_STATE`).
- **Live region = announce DIRECTLY in `Apply`, no debounce timer.** The old 300 ms standby/message debounce-timer announce was RESET by the 300 ms scrape poll every cycle, so it never ticked (that was "autocomplete never announced"). The message line now announces directly in `Apply` (deduped). The per-keystroke standby announce was REMOVED entirely (chatty) — only the LOADED value is read back.
- **`Apply` marshals to the UI thread** (`if (InvokeRequired) BeginInvoke`): the async `ScrapeNowAsync` continuation can resume OFF the UI thread, where `_announcer.Announce` silently fails AND the dedup key still updates (so the next UI-thread poll then sees "no change") — that masked the readbacks. The squawk/Enter announces always worked because they fire synchronously on the UI thread.
- Side combo **Captain / First Officer / Overhead** (RMP 1/2/3) unchanged. The coherent-rmp-agent scrape, the `SendRmpKey`/`SendRmpKeyPress`/`SendRmpKeyRelease` helpers, and the held-clear (`Alt+C`) are unchanged. **`Alt+C` no longer collides with a `&Close` button mnemonic** (button mnemonics were removed; Escape/Tab still close).

**Landing-rate + landing-G output hotkeys (aircraft-AGNOSTIC — work on every aircraft).** `Ctrl+Shift+R` = last landing rate (`PLANE TOUCHDOWN NORMAL VELOCITY` ×60 → fpm, read live from the persistent SimVar cache — the sim latches it at touchdown), `Ctrl+Shift+G` = last landing peak g. New `Services/LandingRateAnnouncer` opens a 4 s peak-g capture window at the airborne→ground edge (`justTouchedDown` in MainForm's `SIM_ON_GROUND` handler), fed by the always-on `G FORCE` base SimVar. New base SimVars `G_FORCE` + `PLANE_TOUCHDOWN_NORMAL_VELOCITY` (Continuous, IsAnnounced=false) in `BaseAircraftDefinition.GetBaseVariables`. Both hotkeys say "No landing recorded this session" before any landing. (Output-mode chords; each mode has its own keyset so they don't collide with the input-mode Ctrl+Shift+R RMP / output-mode R-TCAS.)

**Unified Cockpit panel.** The former separate "Cockpit Door", "Windows and Shades" and "Cockpit" pedestal panels are merged into ONE organized `p["Cockpit"]` (door + door-video → sliding windows → shades/visors → openable panels → seats/armrests); door video pulled out of Recorder and Misc; `d["Cockpit"]` carries Cabin Ready. Brake-values check: verified live the brake temps/pressures read real values (temp 25.7 °C, accumulator 5250 psi, normal-brake 0 unbraked) and the Autobrake panel re-requests them on every open (`RequestPanelVariables`) — they update when given.

**Crew SEATS are 8 toggle BUTTONS (press to start, press again to stop + speak position) — 2026-06, user request, supersedes BOTH the b065970 "ramp the slider" AND the intermediate 3-state-combo motor design.** Two earlier bugs drove this: (1) the "just a tick, no sustained motor sound" — the seat motor audio (`sound.xml` `seatmotor{fwd,back,up,down}`) is a Wwise **`Continuous` loop gated by a `Derived="true"` RTPC = the per-frame DERIVATIVE of the position L:var** (`SEAT_{CPT,FO}_MOVE_{UP_DOWN,FWD_AFT}`, 0–100); a write must change the var EVERY frame or the loop only plays its onset then fades (`FadeOutTime=0.1`). (2) the combo "start/stop/start + spurious down/up 5%" — MainForm periodically re-reads a combo's backing var; the synthetic motor var reads 0 → snaps the combo to "Stopped" → re-fires `HandleUIVariableSet` → restart + bogus announce. **Fix = `RenderAsButton` toggle buttons** (MainForm fires `HandleUIVariableSet(varKey, 1, …)` on the click edge only, no periodic re-read, so the snap-back can't happen). Eight buttons `SEATBTN_{CPT,FO}_{UP,DOWN,FWD,AFT}` (`SeatBtn` helper) + four read-only position vars `SEAT_{CPT,FO}_MOVE_{UP_DOWN,FWD_AFT}` (`SeatPos`). `_seatButtonMap` maps each button → `(PosVar, Dir ±1)`; the HandleUIVariableSet branch calls `ToggleSeatMotor`: if that axis is already moving this dir → stop + `AnnounceSeatPosition` (+ `RequestVariable` forceUpdate); else seed `_seatMotorPos` from the cache and start a **20 ms** UI-thread `SeatMotorTick` firing a UNIQUE clamped read-modify-write via the calc path `<seq> 0 * (L:VAR) 0.4 + 100 min (>L:VAR)` (up) / `0.4 - 0 max` (down) — the `<seq> 0 *` prefix makes each calc string unique so MobiFlight doesn't DEDUP it (identical strings are registered+fired once; that dedup was the *real* "tick once" cause), and the per-frame change keeps the derivative non-zero → sustained motor noise. **Position read-out on stop** speaks a BAND not a percentage: `SeatBand` → fully up/up/slightly up/mid travel/slightly down/down/fully down (+ "{pct} percent"); `TryGetDisplayOverride` renders the 4 position vars the same way. 8 s safety cap stops a forgotten-moving axis; at the 0/100 clamp the writes no-op and the loop fades on its own. **Direction labels (Up=+var / Forward=+var) NOT visually confirmed** — if a seat moves the wrong way, swap the Dir sign in `_seatButtonMap` (trivial). **Armrests / sunshades / visors KEEP the `RampSliderTo` slider** — no motor sound in `sound.xml`, so a position target is the right model there.

**A32NX PRESS SD page fix.** The plain `A32NX_PRESS_CABIN_ALTITUDE` / `_CABIN_VS` / `_CABIN_DELTA_PRESSURE` names DO NOT EXIST on the A32NX (read static 0) — the A32NX publishes cab-press via the CPC ARINC429 words `A32NX_PRESS_CPC_1_CABIN_ALTITUDE` / `_CABIN_VS` / `_CABIN_DELTA_PRESSURE` / `_OUTFLOW_VALVE_OPEN_PERCENTAGE` (decoded, "not available" when the SSM is invalid). Fixed.

**Button rule clarified (2026-06 audit): only TRUE one-shot momentary actions are buttons; any on/off or multi-state control is a COMBO** (so the value is always visible/correct and a second press isn't a no-op). A button that toggles a hidden state and never updates its label is the bug to avoid — e.g. the A32NX APU Start PB was a `RenderAsButton` toggle whose label never changed → reverted to a combo. The A380 Fire Test + Cargo Smoke Detection Test were likewise reverted to Off/On combos. Buttons that REMAIN: the ECAM-CP keys, chronometer Start-Stop/Reset, cabin/mechanic calls, ATC ack (all momentary pulses). If a button must show state, set `StateVariable` (the 0/1 indicator L:var) so MainForm updates its label on each press (the Fenix pattern). The unused `ToggleBtn` helper was removed.

**Ground/external power kept next to the battery controls** in the ELEC panel (user request): A380 `p["ELEC"]` moves the 4 `EXT_PWR_*_PB_IS_ON` right after the 4 battery PBs; the A32NX already had EXT PWR adjacent to the 2 batteries.

**ARINC bit-packed discrete words DECODED (no longer deferred), both aircraft.** A `Bit(bit, set, clr, invert)` helper (A380SdRows) decodes a 1-based SSM-gated bit of an ARINC discrete word. A380 COND: cabin fans enabled + fan 1-4 faults (VCS word bits 17/18-21), fwd/bulk cargo extract fans (13/15), fwd/bulk cargo isolation valves (14/16), hot-air 1/2 valves (TCS 15/16), pack 1/2 operative (AGS 13/14); BLEED: per-pack-valve flow (`A32NX_PNEU_PACK_n_FLOW_VALVE_m_FLOW_RATE` 0..1 -> %) + pack operative. A32NX COND: hot-air valve (ACSC word 1 bit 20, **inverted** — open = bit clear), hot-air switch (23), cabin fan 1/2 fault (25/26), trim-air valve positions; BLEED: HP valve, RAM AIR, pack 1/2 flow (`A32NX_COND_PACK_FLOW_n` %).

**Engine page "values don't change" = the SD box is a SNAPSHOT, not a wrong var.** Verified live on the A380: `A32NX_ENGINE_N1:1` tracked 18.9% (idle) -> 76.5% (full throttle). The SD/ECAM-CP status box is OnRequest — it updates on page-select / F5, not continuously. (Possible enhancement: auto-refresh the SD box while open.) **A32NX engine-page LIVE check is DEFERRED until loaded into the A32NX** — verify `A32NX_ENGINE_N1:1/2` etc. track with throttle and that the SD Engine page reads them; same snapshot behaviour expected.

**DOCUMENTED FOLLOW-UPS (exact var lists in the session agent reports — execute mechanically when loaded into the A32NX):** (1) A32NX ECAM-CP HVar buttons already fire as momentary buttons via `SendButtonPressRelease` (no change needed; cosmetic-only if unifying with the A380 style). (2) A32NX SD completeness: ELEC (TR 1/2/ESS V+A, gen 1/2 freq, bat 1/2 current, EXT PWR V+Hz, STAT INV V+Hz, IDG temps), FUEL (5 per-tank quantities + crossfeed), WHEEL (gear down-lock per leg via `A32NX_LGCIU_{1,2}_DISCRETE_WORD_1/3`, anti-skid), APU (bleed pressure, gen freq), HYD (reservoir low/air-press/OVHT, pump PB state, e-pump OVHT). (3) A32NX decoded upper-E/WD path (thrust mode + N1/EGT/N2/FF/FOB + SFCC flap config) — currently scrape-only. (4) A32NX panel adds: RMP NAV button, GPU-available readout, rain repellent, RAT manual deploy, integral/standby-compass lights, ELEC GALLEY.

**Every other A380 cockpit control is a COMBO BOX (selectors; buttons only for the ECAM-CP/test set above and the FCU push/pull).** Standardised for consistent screen-reader navigation: Sel/OnOff/OffAuto/Press all render combos (the `Sel` helper forces `RenderAsButton=false`; the `button:` param is ignored). The previously button-rendered `Evt` controls were converted:
- Engine MASTER 1-4 and the 4 CROSSFEED valves → one Off/On (Closed/Open) combo each, state from the stock fuel-valve SimVar, set fires `FUELSYSTEM_VALVE_OPEN/CLOSE` (ids 1-4 / 46-49).
- DOORS → **REMOVED from the A380 entirely (2026-06).** They were the root cause of the aircraft-detection break — see the "doors root-caused" note in the DEFERRED block below. External door-opens are still surfaced by the EWD "DOOR … NOT CLOSED" memo.
- JETWAY / STAIRS control combos were REMOVED — these services are operated on the flyPad Ground page, not via panels. Only a read-only `JETWAY_MOVING_STATE` (Moving/Stopped) motion readout remains; `TOGGLE_JETWAY`/`TOGGLE_RAMPTRUCK` are no longer used in the def.
- Transponder IDENT, VHF SWAP → Off/Idle action combos via the new `Act()` helper (a combo with NO real backing var whose set is fully handled in `HandleUIVariableSet`; OnRequest so a non-existent L-var isn't monitored).
**Enabler:** MainForm's SimVar-combo set path now calls `currentAircraft.HandleUIVariableSet(...)` FIRST (mirroring the LVar-combo path) — so a combo whose STATE is a SimVar but whose CONTROL is a K-event works; the A320 light cases return false and fall through unchanged. **KEPT as buttons:** the FCU/AP push-pull (`A32NX.FCU_*_PUSH/PULL`, Event type, hit the final button branch). **EDIT FIELDS:** any `_SET` key (squawk `TRANSPONDER_CODE_SET`, COM `COM_STANDBY_FREQUENCY_SET:n`, temps, manual pressurization) renders a numeric/text input via MainForm's `varKey.Contains("_SET")` branch. All conversions live-verified (engine valve 1, crossfeed 46 actuated; door controls have since been removed — see the DEFERRED door note).

### HorizonSim 787-9 FMC Bridge

The WT Boeing FMC (CDU screen) is made accessible via a JavaScript bridge injected into the HS787 MFD and EFB HTML. Port 19778. **Status: working in both FS2020 and FS2024** as of BridgeVersion 18 — but the two sims use different installation architectures (see below). Both MFD and EFB bridges share the same HTTP server with namespaced command channels (`/commands/mfd`, `/commands/efb`).

**Two installation architectures — picked at runtime by `HS787ModPackageManager.IsFs2024()`:**

| | FS2020 | FS2024 |
|---|---|---|
| Approach | Override package | **In-place patching** of `horizonsim-aircraft-787-9` |
| Package created | `zzz-hs787-accessibility/` in Community | None (the directory is deleted if found, leftover from older versions) |
| Script tag style | `<script src="hs787-mfd-bridge.js">` | `<script type="text/html" import-script="/Pages/.../hs787-mfd-bridge.js">` (matches original instrument's loader style) |
| Originals safe-keeping | N/A (originals never modified) | `*.msfsba_backup` files saved alongside originals |
| Version tracking | `bridge-version.txt` inside override package | `msfsba-bridge-version.txt` inside `horizonsim-aircraft-787-9/` |
| Layout.json | Generated for override package | `horizonsim-aircraft-787-9/layout.json` patched in place (sizes updated for 4 HTMLs, 2 new entries for bridge JS); `layout.json.msfsba_backup` saved |
| Detection (`IsFs2024`) | delegates to `EFBModPackageManager.IsPathFromFs2024()` → returns false | delegates to `EFBModPackageManager.IsPathFromFs2024()` → returns true |

**`EFBModPackageManager.IsPathFromFs2024(path)` — 3-tier detection chain:**
1. **UserCfg.opt lookup (primary):** reads `%AppData%\Microsoft Flight Simulator 2024\UserCfg.opt` (Steam/standalone) and `%LocalAppData%\Packages\Microsoft.Limitless_8wekyb3d8bbwe\LocalCache\UserCfg.opt` (MS Store), parses `InstalledPackagesPath`, compares `InstalledPackagesPath\Community` to the given path (`Path.GetFullPath` + `OrdinalIgnoreCase`). Covers all users who have run the sim at least once, including external-drive installs.
2. **"Limitless" substring fallback:** catches the MS Store default path when UserCfg.opt doesn't exist yet (sim installed but never launched).
3. **"Microsoft Flight Simulator 2024" substring fallback:** catches Steam default path (`%AppData%\Microsoft Flight Simulator 2024\Packages\Community`) before UserCfg.opt exists. Safe: FS2020 Steam folder is "Microsoft Flight Simulator" with no year.

`HS787ModPackageManager.IsFs2024()` delegates to this helper. `GetManifestJson()` delegates to `IsFs2024()`. `FindAllCommunityFolders()` and `FindCommunityFolderPath()` also include the Steam FS2024 default path as a discovery fallback — this benefits both the HS787 bridge and the PMDG EFB bridge. When auto-detection finds no community folders at all, `CheckAndOfferHS787ModPackage()` shows `HS787CommunityFolderForm` (Browse + sim-version combo) and persists the result to `UserSettings.Hs787CommunityFolderOverride` / `Hs787SimVersionOverride`.

**Adding a bridge for a new aircraft — what's shared vs. per-aircraft:**

The community folder detection layer in `EFBModPackageManager` is shared infrastructure. A new aircraft mod manager gets the following for free just by delegating to it:
- `EFBModPackageManager.IsPathFromFs2024(communityFolderPath)` — correct FS2024 detection for MS Store, Steam, and external-drive installs
- `EFBModPackageManager.FindAllCommunityFolders()` — discovers community folders for both sims via UserCfg.opt + hardcoded fallbacks (already includes Steam FS2024)
- `EFBModPackageManager.FindCommunityFolderPath()` — single-path variant, same coverage

The manual fallback layer is **per-aircraft** and must be added separately for each new aircraft:
- `UserSettings` needs its own override fields (e.g. `Xyz123CommunityFolderOverride` / `Xyz123SimVersionOverride`)
- A new dialog (copy-adapt `HS787CommunityFolderForm`) or generalize it into a shared `CommunityFolderPickerForm` if three or more aircraft need it
- `MainForm` needs its own `BuildXyz123FolderList()` / `SaveXyz123FolderOverride()` helpers and an updated `CheckAndOfferXyz123ModPackage()` that uses them

**When to generalize:** Two aircraft needed this pattern → YAGNI says duplicate. If a third aircraft needs the same treatment, that's the right moment to extract a shared `CommunityFolderPickerForm` and a generic `UserSettings` override mechanism.

**Why FS2024 needs a different approach (do NOT revert to override-package on FS2024):**
1. **FS2024 VFS doesn't honor community-on-community overrides under `html_ui/Pages/VCockpit/Instruments/Airliners/`** — the Airliners namespace is protected. Modified HTML in an override package is silently dropped (verified: `document.documentElement.outerHTML.indexOf('hs787-mfd-bridge')` returns `-1` in the loaded MFD webview). PMDG EFB's same-architecture mod (`zzz-pmdg-efb-accessibility`) DOES work on FS2024 because it targets `PMDGTablet/`, a non-Airliners path. The `globally_overriden_base_sim_files` manifest field (panel-raas-style) is base-sim-files-only and won't accept community-package paths.
2. **`<script src>` doesn't execute inside template-loaded HTML on Coherent GT 2.x.** The original HSB789_MFD.RR.html loads MFD789.RR.js via `<script type="text/html" import-script="...">`, and FS2024's Coherent only honors that style inside templates. FS2020's Coherent GT 1.x was lenient and ran both. Our injection must match the original style.

**Key components:**
- **`EFBBridgeServer`** (`SimConnect/EFBBridgeServer.cs`) — shared HttpListener (port 19778 for 787, 19777 for PMDG). `/ping`, `/state` (POST), `/commands/mfd`, `/commands/efb` (GET). `IsBridgeConnected` checks heartbeat within 15s.
- **`HS787ModPackageManager`** (`Patching/HS787ModPackageManager.cs`) — installs/updates/removes. Branches `IsFs2024()` → `InstallFs2024` / FS2020 path. Reads original HS787 HTML from `horizonsim-aircraft-787-9` (never hardcoded). Auto-updates on app start via `BridgeVersion` constant. Bump `BridgeVersion` whenever bridge JS or script tags change.
  - FS2024-specific helpers: `InstallFs2024`, `RemoveFs2024`, `IsInstalledFs2024` (patch-marker check on HSB789_MFD.RR.html), `PatchHtmlInPlace` (re-derives from backup so re-patches don't stack), `UpdateHorizonsimLayoutJson` (slash-agnostic match — important: horizonsim's layout.json uses forward slashes but auto-detection from the first entry can misfire on top-level files like locPaks; `Canonical()` matches regardless of separator).
- **`hs787-mfd-bridge.js`** (`Resources/`) — reads WT Boeing FMC CDU rows from DOM (`.fmc-row`, `.fmc-letter`), pushes screen state, polls commands. Double-load guard (`window._mfd_bridge_loaded`).
- **`hs787-efb-bridge.js`** (`Resources/`) — reads Boeing EFB buttons and page text. Same guard, same server, same port.
- **`HS787FMCForm`** (`Forms/HS787/HS787FMCForm.cs`) — displays CDU rows, scratchpad input, page buttons. `_statusTimer` (1.5s) drives `UpdateConnectionStatus`. Status label shows bridge diagnostic stage when not connected.
- **`HorizonSim787Definition`** (`Aircraft/HorizonSim787Definition.cs`) — registers `HS787_BridgeStage` (`L:MSFSBA_787_STAGE`, Continuous, IsAnnounced=true). `BridgeStage` property updated silently in `ProcessSimVarUpdate`.

**SimVar diagnostic (`L:MSFSBA_787_STAGE`):**
Written by `hs787-mfd-bridge.js` via `SimVar.SetSimVarValue`; read by C# via SimConnect continuous monitoring. Displayed in `HS787FMCForm` status label when bridge is not connected.
- **0** — L-var never written: script did not execute. Most commonly means the patched HTML wasn't actually loaded by the sim (not the script being sandboxed — verify by inspecting the DOM via Coherent debugger at `http://127.0.0.1:19999`).
- **1** — Script executed: `window._mfd_bridge_loaded = true` ran and `SimVar` is available.
- **2** — Fetch failed: `tryConnect` threw (very rare; HTTP to localhost works in both Coherent versions).
- **3** — Connected: HTTP fetch to `127.0.0.1:19778` succeeded (should match `IsBridgeConnected = true`).

**Caveats for maintainers:**
- A HorizonSim update to `horizonsim-aircraft-787-9` overwrites our in-place patches and backup files. On the next MSFSBA startup the patch marker is gone → MSFSBA re-runs the FS2024 install path, takes fresh backups from the new originals, re-applies the patch. Idempotent.
- Do not run MSFSBA while MSFS is running — file locks prevent patching.
- The FS2020 override-package path works because Coherent GT 1.x runs `<script src>` and FS2020's VFS uses alphabetical priority (`zzz-` prefix wins). Do not "modernize" the FS2020 path to in-place patching — pointless invasion of the user's HS787 install when the override mechanism works there.
- The `BackupSuffix = ".msfsba_backup"` constant is used for HTML files AND `layout.json`. The horizonsim package may also contain a `layout.json.bak` from other tools — don't conflate; use our suffix specifically.
- Injection paths in import-script tags are absolute from html_ui root (`/Pages/VCockpit/Instruments/Airliners/HSB787_9/MFD/hs787-mfd-bridge.js`) — relative paths don't resolve inside template-loaded HTML.

**Communication flow:**
- JS → C#: `POST /state` with `{type, data}` — types: `mfd_connected`, `heartbeat`, `fmc_screen`, `cdu_visible`, `cdu_not_visible`, `efb_connected`, `efb_screen`
- C# → JS: `GET /commands/mfd` or `/commands/efb` polled every 400ms, returns JSON array of `{command, payload}`
- Bridge retries every 5s on failure; heartbeat every 5s when connected

### FlyByWire Accessible flyPad EFB (A320 + A380 — ONE shared flyPad)

**Feature:** Screen-reader-accessible Electronic Flight Bag (flyPad) for **both** FlyByWire aircraft. The **same** form, client, and in-page agent serve the A32NX flyPad and the A380X flyPad — only the window title differs. Opened via the shared `HotkeyAction.ShowPMDGEFB` dispatch, branched by `AircraftCode` (`"A320"` and `"FBW_A380"` both call `ShowFBWA380EFBDialog`). It renders **whatever flyPad page is currently open** as a browsable document; there are no curated per-feature tabs.

**Architecture — fundamentally different from the PMDG EFB bridge.** The PMDG/HS787 bridges inject JS via a Community mod package and talk over an HTTP server (`EFBBridgeServer`). The FBW flyPad uses **CDP (Chrome DevTools Protocol) over Coherent GT's devtools port (`:19999`)** with **no mod package and no Community-folder patching** — a generic in-page agent is installed at runtime via `Runtime.evaluate`. `EFBBridgeServer` stays for PMDG/HS787; the FBW flyPad does **not** use it.

```
MSFS FBW flyPad (React / Coherent GT, devtools :19999)   — A32NX or A380X, same "- EFB" view
   window.__MSFSBA_FLYPAD   ← ONE generic in-page agent (installed at runtime)
        ▲ CDP Runtime.evaluate over a persistent WebSocket (/devtools/inspector/{id})
CoherentEFBClient.cs   (.NET ClientWebSocket; IMcduBridge; ~400ms scrape poll → push)
        ▲ fbwa380_efb_connected / fbwa380_efb_elements state pushes
FBWA380EFBForm.cs   (generic WebView2 form; browser mode + native-list fallback)
        ▲ Shift+T (ShowPMDGEFB dispatch; AircraftCode "A320" or "FBW_A380")
```

The A380 reuses this SAME form a third time for the **OANS airport map / BTV**, driven by `CoherentNDClient` (resolves the ND view) instead of `CoherentEFBClient` — see `ShowFBWA380OansDialog`. The form is generic over `IMcduBridge`, with `windowTitle` + `deviceNoun` ("flyPad" / "OANS") parameters.

**Key components:**
- **`Resources/coherent-flypad-agent.js`** — the ONE generic DOM scraper/clicker for both aircraft (Gus Pacleb's A32NX agent, a strict superset of the older A380 one — adds React-onClick detection for handler-less clickable `<div>`s, refuel/boarding action-button state, Ground-service tile detection). **ES5 / Coherent-GT-safe** (`var`, no arrow funcs, `.indexOf()` not `.includes()`, top-level try/catch — a scraper bug returns an error string, never throws into the flyPad). Public global `window.__MSFSBA_FLYPAD`: `ping()`, `scrape()` → `{ok,page,elements[]}`, `clickElement(idx)`, `setValue(idx,text)`, `powerOn()` (sets `L:A32NX_EFB_TURNED_ON=1` — same L:var on both jets — + a synthetic interaction to resume the dormant render loop; idempotent). Each element carries a stamped `idx` (internal attribute `data-fbw-efb-idx`, re-stamped every scrape) plus `kind`/`text`/`value`/`controlType`/`clickable`/`level`/`live`/`disabled`/`options`.
- **`SimConnect/CoherentEFBClient.cs`** — `IMcduBridge` push client over **.NET `ClientWebSocket`** to `ws://127.0.0.1:19999/devtools/inspector/{id}` (the same endpoint `tools/coherent-eval.ps1` uses; it does NOT hit the non-standard-upgrade issue an older manual handshake worked around). Resolves the `- EFB` page from `:19999/pagelist.json` by title every (re)connect (ids shuffle per session); installs the agent once, then `powerOn()`; **polls `scrape()` ~every 400ms** and pushes `fbwa380_efb_connected` / `fbwa380_efb_elements` only when an element-hash (incl. stamped idx) changes. `EnqueueCommand("get_display_elements" | "click_display_element" | "set_element_value")` maps to agent calls. Receive pump correlates responses by CDP message `id`; captured `SynchronizationContext` marshals to the UI thread.
- **`Forms/FBWA380/FBWA380EFBForm.cs`** — the generic **WebView2** form hosting a generated **accessible HTML document** (inline shell, keyed reconcile). Browser mode is primary; a native-list fallback exists if WebView2 fails to init.

**CRITICAL — single-connection constraint.** Coherent GT's debugger accepts only **ONE** devtools connection per page. `CoherentEFBClient` owns the `- EFB` socket while the form is open; it is disposed on the unconditional aircraft-swap cleanup. The `tools/coherent-eval.ps1` harness and the app's client **cannot both be connected at once** — close the flyPad form before driving the page with the tool, and vice-versa. (Verified live on the A32NX via `tools/_probe/drive_tour.ps1`, which tours all 10 tabs and exercises ping/scrape/click/setValue.)

**CRITICAL — why WebView2 HTML, not native WinForms controls.** The first implementation rendered the scrape as native WinForms controls; in-sim NVDA testing showed **only focusable controls were reachable — headings and static text were invisible** because a WinForms form is not a document and has no browse mode. The renderer hosts a WebView2 accessible HTML document instead, giving NVDA full browse mode (H-key heading nav, arrow-key reading of all text, real fields/links). **The WebView2 shell JS may use modern Chromium syntax** (unlike the ES5 Coherent agent).

**CRITICAL — keyed DOM reconcile (stops NVDA focus jumps).** Do NOT wipe `#root.innerHTML` and rebuild every poll — that steals screen-reader focus. The shell does a **keyed in-place reconcile** (reuse nodes by key, only patch changed text/value/checked, never touch the focused element). The reconcile key is element **CONTENT, NOT the scrape idx** (`kind + controlType + label`), because the agent re-stamps idx from 1 in DOM order on EVERY scrape — so idx is unstable across pages AND across **Ground sub-tabs** (Services/Fuel/Payload/Pushback all report `page` = `"Ground"`). With idx keys, Fuel idx 15 (a button) and Payload idx 15 (an input) collided and cross-patched. Content keys keep a node stable across same-page polls (values patched in place, focus preserved) while a sub-tab/page switch cleanly swaps controls. The live click/setValue idx is carried on each control and patched in place. Because jitter is absorbed here at the reconcile layer, the client can include idx in its push-hash without harming the NVDA browse position. The reference shell `Resources/flypad-shell.html` + its jsdom reconcile tests (`tools/flypad-shell-test/`) are retained as the canonical reconcile spec (they fail on idx keying, pass on content keying); the shipping inline shell mirrors the same logic.

**Sync:** ~400ms auto-poll (client) + F5 manual refresh + delayed re-scrape after a click/setValue. `powerOn()` runs automatically on connect (no wake button). Errors degrade gracefully: no `- EFB` page → "flyPad not detected" silent retry; dormant flyPad → `powerOn()` backoff; CDP timeout/malformed JSON → keep last good render, skip the cycle.

**Dev tool:** `tools/coherent-eval.ps1 -Title "- EFB" -PreFile ...coherent-flypad-agent.js -ExprFile ...` installs + drives the agent against the live page; `tools/_probe/drive_tour.ps1` / `flypad_tour.js` tour every tab. Subject to the single-connection constraint above.

**Ground page tabs + Services accessibility (A320 + A380).** Active-tab indication: the selected nav-rail page reads **"(current page)"** and the selected Ground sub-tab (Services/Fuel/Payload/Pushback) reads **"(selected)"**, both keyed on FBW's standalone `bg-theme-accent` token (inactive carry only `hover:bg-theme-accent`). `orderGroundServices` re-bands the Ground page so the sub-tab strip reads as one group apart from the service tiles (the two-column read order otherwise dropped the strip between the left- and right-column tiles). **Wheel Chocks / Safety Cones are READ-ONLY status, not controls** — FBW renders them as `cursor-pointer` divs with NO click handler whose text colour encodes state (`text-green-500` = placed, `text-gray-500` = not placed; source `A380Services.tsx`/`A320Services.tsx` comment: "only visual information"). `A.groundStatusTile` detects them structurally (language-independent) and surfaces **"Wheel Chocks: placed / not placed"** as a status line; do NOT try to make them clickable (there is no setter — they only become green when ground equipment is placed). The real service tiles (Door/GPU/Stairs/Baggage/Catering/Fuel-truck) keep their existing `(active)`/`(called)` green/amber `serviceState`.

**Ground Payload & Fuel pages — dedicated accessible builders (A320 + A380).** The flyPad Ground→Payload and Ground→Fuel pages are a `<table>` / tank-readout grid wrapped in sighted-only **canvas** visuals (Payload seat map + CG/MAC chart, Fuel wing schematic). A flat positional scrape flattened every table cell into stray fragments (split leading-zero pad spans, lone units, the CG-chart `15%…40%`/`MTOW`/`MLDW`/`MZFW` axis labels) and hid the fuel-target numeric input — unreadable. `coherent-flypad-agent.js` now has two dedicated builders (the `buildFplnLines` pattern), invoked from `enumerate` when the matching sub-tab is active (`A.activeGroundSub(root,'payload'|'fuel')`, detected by the `/ground/<x>` link's standalone `bg-theme-accent` token):
- **`buildPayloadLines`** parses the W&B `<table>` (cols: label / Planned-input / Current-display): re-surfaces each editable `<input>` (stamps `data-fbw-efb-idx` so set/click are unchanged) with `current X, max Y, unit` folded into its accessible name; emits ZFWCG as a read-only line; relabels the per-pax/bag weight inputs (no default-value hint) by pairing them to their weight tooltips in DOM order. Owns (suppresses) every non-actionable section child + the CG-chart card + the table + all absolute `pointer-events-none z-50` tooltips (every label/limit/caption is a tooltip; `findMaxFor` reads the maxes from them first). Leaves the boarding buttons + rate selector to the generic pass.
- **`buildFuelLines`** is scoped to **ROOT, not the tank `h-content-section`** — on the A380 the Total/Trim cards AND the refuel input + rc-slider are absolutely-positioned OUTSIDE the tank section. `fuelVal` matches both layouts (A320 `cur / cap UNIT` single node; A380 `0…0value UNIT` `ValueUnitDisplay` with no capacity) on `textContent` taking the innermost match; `fuelTankName` reads the enclosing `<tr>` first cell / nearest heading / preceding label. The fuel target is identified by the input under the "Refuel" heading and emitted as BOTH an accessible **`<input type="range">` slider AND a number field** sharing the input's idx (both write the same backing input → the in-sim rc-slider follows); slider max = the A320 Total tank's capacity, else the stock `FUEL TOTAL CAPACITY × FUEL WEIGHT PER GALLON` SimVar (the A380 tanks show current-only).
- **`range` control type** is threaded agent → `CoherentEFBClient` (`min/max/step` on `ScrapeElement` → `items.{i}.min|max|step`) → `FbwEfbForm` shell (a `rng` rendered node = `<input type="range">` posting `set` with `controlType:'range'`).
- **`setValue` commits via Enter+blur.** FBW `SimpleInput` commits (clamp + write to the sim) on Enter / blur, NOT on the React `onChange` — so `A.setValue` dispatches a synthetic Enter keydown/keyup + `blur()` after `setNativeValue`. Verified live: setting the fuel target moves `L:A32NX_FUEL_DESIRED` (asynchronously, next frame — a same-tick read still shows the old value).
- **Suppressed everywhere:** the canvas visuals, the rc-slider widget, stringified-boolean leaks (`/^(true|false|undefined|null)$/` — FBW `${cond && t('…')}` template literals render "false" in the Fuel rate-selector tooltips), and the per-page **"Fill … from SimBrief"** controls on both pages (`A.suppressSimbrief` — caption tooltip + the CloudArrowDown icon button, never the value `<input>`; the user imports via the Dashboard). DISABLED FBW buttons carry `pointer-events-none` too (`opacity-20 pointer-events-none`) so tooltip-suppression gates on `absolute` to avoid dropping a disabled Start-deboarding / SimBrief button. Spec + plan: `docs/superpowers/specs/2026-06-03-flypad-ground-payload-fuel-accessibility-design.md`, `docs/superpowers/plans/2026-06-03-flypad-ground-payload-fuel-accessibility.md`.

**Ground page polish — rate selector, time display, door labels, focus.** (1) **Boarding-time display**: FBW renders the boarding duration as a parenthetical CHILD of the label (`<div>Boarding Time<span>(11:00 minutes)</span></div>`), so the generic pass captured only `directText` "Boarding Time" and the parenthetical-skip rule dropped the time. `buildPayloadLines` now emits the element's FULL text ("Boarding Time (11:00 minutes)") and owns it. (Fuel's "Estimated duration … : 0.5" is one text node — already fine.) (2) **Selected rate option** (Instant/Fast/Real): `A.isSelectedSegment` gained a third check — FBW wraps EACH SelectGroup option in its own TooltipWrapper `<div>`, so the SELECTED option's only sibling is its tooltip and the immediate-sibling peer test missed it (symptom: "only Instant showed (selected)"). It now also walks ≤4 ancestors for the SelectGroup container (class `divide-x`) holding ANOTHER `cursor-pointer` option. (3) **"Refuel Time" heading suppressed** in `buildFuelLines` — it labels the rate selector but reads as a redundant, oddly-placed second time line after "Start refueling" (the estimated duration already shows above; the rate options are self-evident, matching the Payload boarding flow which has no separate rate heading). (4) **Door tiles** are grouped together (`orderGroundServices` band 150, gated on `/door/i` + `kind==="button"`) and labeled with the door's REAL name, matched to **MSFSBA's own `_doorDefs` table** (which is also what the read-only door-state announcements speak — so the flyPad label and the spoken name agree). FBW labels several DISTINCT doors with the same tile text ("Door Fwd"/"Door Aft"), and its flyPad enum DIGIT is **unreliable**: e.g. the A380 `Main4Right` controls `INTERACTIVE POINT OPEN:9`, which the FBW MODEL itself (`door_animations.xml`) names **M5R** and the EWD calls **"MAIN 5R"** = MSFSBA's **"Main Door 5 Right"**, NOT "Main 4". So `A.doorIdentity(n)` does NOT trust the digit — it parses the enum NAME from the handler comment (`() => handleButtonClick(N /* Main4Right */)`) and looks it up in the fixed `A.DOOR_NAMES` map (cabinleftdoor→"Forward Left Door", main1left→"Main Door 1 Left", main4right→"Main Door 5 Right", upper1left→"Upper Door 1 Left", …). **Keep `A.DOOR_NAMES` in sync with `FlyByWire{A320,A380}Definition._doorDefs`.** The handler may sit on the stamped node, an ancestor, OR an inner descendant, so it scans n + ≤4 ancestors + the tile subtree. **State-dependence + cache**: FBW sets `onClick=undefined` (and drops the comment) while an attached service (jet bridge/stairs) DISABLES that door, so the identity is only parseable when the door is operable — `doorIdentity` therefore STAMPS the resolved name on the node (`data-fbw-door`, NOT cleared by the per-scrape stale sweep) and reuses that cache when the handler later disappears. **GUARDED**: an unknown enum, a Cargo handler, or a no-comment build returns `""`, and `enumerate` falls back to the robust column-based Left/Right (screen LEFT column = aircraft LEFT side; the `xr`-anchored wrapper holds CabinLeftDoor/Main1Left = interactive point 0) — so the worst case is a side-only label, never a wrong door number. Applied BEFORE the state suffix so the focus-stable key strip still trims `(active)`/`(called)`; "Cargo Door" is matched by `!/cargo/i` and left alone. (5) **Focus jump on activation fixed** (`FbwEfbForm` shell): the WebView2 keyed-reconcile keyed on the FULL label, so a control gaining "(active)"/"(called)"/"(selected)" got a NEW key → its node was destroyed + rebuilt → the screen-reader focus jumped off the control the user just activated. `keyOf` now strips the dynamic state suffixes (`baseLabel`) so the node is reused and patched in place; the visible label still updates. **Verification:** all five are live-verified on BOTH the A320 and A380 (boarding/refuel time, rate-selected state, "Refuel Time" removal, door grouping + precise handler-based labels, tanks/slider). The A380 forward doors read Door Main 1/2 Left + Door Upper 1 Left; the A320's read Door Forward/Aft Left/Right (the A320 handler-based labels are mechanism-verified on the A380 — the A320 enum names map the same way — but re-confirm the exact A320 wording when next loaded). Focus-jump is shell-level (aircraft-agnostic, A320-NVDA-confirmed).

### FlyByWire A32NX Panel Parity (Phase 3)

The A32NX panel set in `FlyByWireA320Definition.cs` is now at parity with the A380 (`FlyByWireA380Definition.cs`) — every A380 overhead/glareshield/pedestal/ground-services panel exists on the A320, with matching labels. Notes for future maintainers (all live-verified against the running A32NX via the SimConnect MCP calculator path):

- **EFIS is split Captain / First Officer.** The ND mode + range CONTROL is the FCU knob `A32NX_FCU_EFIS_{L,R}_EFIS_MODE/RANGE` — it drives the computed `A32NX_EFIS_{L,R}_ND_MODE/RANGE` DISPLAY output. The bare `A32NX_EFIS_*_ND_*`, `_OPTION` (CSTR/WPT/VORD/NDB/ARPT filter) and `_NAVAID_*_MODE` vars are computed outputs that REVERT a calc-path write (verified) → they stay read-only. The LS button `A32NX_EFIS_{L,R}_LS_BUTTON_IS_ON` IS directly settable.
- **Anti-Ice:** the `XMLVAR_MOMENTARY_PUSH_OVHD_ANTIICE_*_PRESSED` vars are model press-animation flags that do NOT actuate (same as A380 #56). WING anti-ice = `A32NX_PNEU_WING_ANTI_ICE_SYSTEM_SELECTED` (calc path; reverts on the on-ground inhibit, holds in flight — `_SYSTEM_ON` is the read-only flowing status). ENG 1/2 anti-ice = the stock K-event `ANTI_ICE_SET_ENGn` (state from `ENG ANTI ICE:n`), routed in HandleUIVariableSet.
- **Thrust Levers (settable detents)** ported from the A380 (2 engines): synthetic `THROTTLE_ALL_DETENT` + `THROTTLE_{1,2}_DETENT` combos → `THROTTLEn_AXIS_SET_EX1` with the FBW default-calibration axis values (idle `-0.44` verified to snap to the A320 idle dead-zone). The synthetic `_DETENT` keys are EXCLUDED from the GetVariables auto-announce loop (no real L:var to monitor — the loop guard checks `!key.EndsWith("_DETENT")`); the live angle reads from `A32NX_AUTOTHRUST_TLA:n`.
- **Doors** = synthetic `A32NX_MSFSBA_DOOR_{0..5}` combos backed by `INTERACTIVE POINT OPEN:n` (the exit index maps 1:1 to the interactive point on the A32NX; 0-3 = passenger, 4-5 = cargo per `EXIT TYPE:n`), set toggles `TOGGLE_AIRCRAFT_EXIT:n`. The var is a 0..1 animation fraction → ProcessSimVarUpdate announces Open/Closed once per transition (>0.05) and TryGetDisplayOverride renders the state. **Ground Equipment** jetway/stairs are momentary Activate combos → stock `TOGGLE_JETWAY` / `TOGGLE_RAMPTRUCK` (the A320 has no clean state var like the A380's `A380X_GND_*`). The flyPad Ground page (Shift+T) stays the richer ground interface.
- **Audio Control Panel:** the A32NX models audio as per-channel reception VOLUME (`A32NX_RMP_{L,R}_VHF{1,2,3}_VOLUME`, 0..100) — surfaced as 5-step combos (Off/Low/Medium/High/Full). It has NO discrete RX/TX on-off switches like the A380 RMP.
- **Flaps-and-Brakes is intentionally NOT consolidated** on the A320 (it keeps the separate Flight Controls / Speed Brake / Parking Brake panels) — a deliberate divergence from the A380's "Flaps and Brakes".
- **COM standby-set is index-aware** in MainForm: COM1 = `COM_STBY_RADIO_SET_HZ`, COM2/3 = `COM{n}_STBY_RADIO_SET_HZ` (a prior bug fired the COM1 event for every index).
- **A380 Speed Brake** (added speculatively, NOT live-verified — no live A380 at the time): a pedestal "Speed Brake" panel with a Retracted/Half/Full combo → `SPOILERS_SET` (0/8192/16383) + a Disarm/Arm combo → `SPOILERS_ARM_OFF/_ON`, and the handle auto-announced by 10% band. Re-verify when an A380 is loaded.
- **EWD on-demand read** is on BOTH jets via `ReadDisplayUpperECAM` (output mode, Alt+E), each opening the `FbwEwdWindow` pop-out. **Both are now DECODE-based — NO Coherent scrape in the A320 path.** The A320's `BuildEwdWindowTextAsync(sim)` decodes the upper E/WD from SimVars: thrust rating + the single `A32NX_AUTOTHRUST_THRUST_LIMIT` number (the A320 has NO per-engine THR% gauge, unlike the A380), per-engine N1 / N1-command / EGT / N2 / FF / state (2 engines, no N3), reversers, IDLE, and FOB (`A32NX_TOTAL_FUEL_QUANTITY` via `WeightUser`) — plus the ECAM memo/warning lines decoded from the `A32NX_Ewd_LOWER_*` SimVar codes via **`SimConnectManager.GetEcamLineRaw`** (the batch handler decodes those codes to `ecamStringData` strings and `continue`s PAST the numeric cache, so `GetCachedVariableValue` returns null for them — `GetEcamLineRaw` exposes the decoded text). The schematic engine row scraped as garbage ("XX XX"), and the Coherent scrape socket is a documented native-crash risk, so the **A320 scrape was REMOVED entirely** (the old `ReadEwdAloud` method + `_ewdScrapeClient` field are gone); a text placeholder shows when nothing decodes (displays unpowered). The SD page-0 box uses the same decode. **The A380 KEEPS its always-on `CoherentEWDClient` (`EwdMonitor`)** — but only because its newer architecture moved the sensed abnormal/failure PROCEDURES onto an in-process EventBus with NO SimVar (so they can ONLY be scraped); its normal memos decode the same way, and its SD box-scrape fallback goes THROUGH the one always-on monitor socket (never a second client). The A32NX (older FBW) publishes ALL its EWD message content — warnings + actions + memos — as the `Ewd_LOWER` codes, so it needs no scrape. The continuous EWD-line monitor still auto-announces new memos/warnings on both.
- **Autoflight (A/THR / LOC / APPR) is wired via the FBW input events** `A32NX.FCU_{ATHR,LOC,APPR}_PUSH` + `A32NX.FCU_ATHR_DISCONNECT_PUSH` (A380 A/THR also via stock `AUTO_THROTTLE_ARM`). These are registered FBW **input events** (source: `fbw-a380x/.../SimConnectInterface.cpp`), reached by MSFSBA's `SendEvent`/TransmitClientEvent — NOT by the MCP/calc `K:` namespace (a calc `(>K:A32NX.FCU_ATHR_PUSH)` is a no-op) and they won't arm on the ground, so live MCP testing is inconclusive but the wiring is source-correct. Both FCU panels expose engage + disengage + LOC + APPR.
- **Air-conditioning ZONE TEMP selectors** (5th-audit gap): Cockpit / Fwd Cabin / Aft Cabin are numeric °C inputs (`COND_*_TEMP_SET`) → `HandleUIVariableSet` converts to the `A32NX_OVHD_COND_{CKPT,FWD,AFT}_SELECTOR_KNOB` (0..300 ↔ 18-30 °C, live-verified). Actual zone temps `A32NX_COND_*_TEMP` read in the display. **APU AVAIL** (`A32NX_OVHD_APU_START_PB_IS_AVAILABLE`) is a read-only APU-panel field (not auto-announced — the EWD memo speaks it).
- **EFIS baro (altimeter) is per-side + silent on first detect.** Captain = `A32NX_FCU_LEFT_EIS_BARO_HPA` + `_EFIS_L_DISPLAY_BARO_VALUE_MODE`; First Officer = the `RIGHT`/`_R_` equivalents (announced with a "First Officer" prefix). BOTH the HPA and the value-MODE handlers **seed their cache silently on the first read** — otherwise each announced on first detect, which was the "altimeter spoken twice on start" bug. Brake "triple indicator" (`A32NX_HYD_BRAKE_{NORM,ALTN}_{LEFT,RIGHT}_PRESS` + `_ALTN_ACC_PRESS`) + Brakes Hot + GPWS Test were added for A380 parity.
- **Feature parity with the A380:** the A32NX now also has **rudder trim** (FCC display reads `A32NX_FAC_1_RUDDER_TRIM_POS`, an ARINC degrees word, positive = nose-Left, + a Reset combo firing `RUDDER_TRIM_RESET`) and **metric/imperial weight units** (gross-weight + fuel-info readouts follow the EFB `A32NX_EFB_USING_METRIC_UNIT` setting via `WeightUser(kg)`; the EFB "US Units" toggle, Shift+T → Settings, is the control — no separate hotkey). NOT ported (the A320 genuinely can't): **metric ALTITUDE / MTRS** (the real A320 has no MTRS button — A330+/A380 only), and **BTV / OANS / ROW-ROP** (A380/A350 systems).
- **FCU value-entry windows ported (Ctrl+S/H/A/V/P/B).** The A32NX now has its own Fenix-style FCU windows (`Forms/FBWA320/FBWA320{Speed,Heading,Altitude,VS,Autopilot,Baro}Window` + shared `FBWA320FCUWindowBase`), mirroring the A380's — value entry + knob Push/Pull + mode toggles + spoken read-out, replacing the deleted single-field `ShowA320*InputDialog` / `ShowFBWBaroSetDialog`. Thin UI delegating to public `FlyByWireA320Definition` methods: `SetFCU{Heading,Speed,Altitude,VS}Value`, `FireFCUButton(evt)` (readback via an inline event→`RequestFCU*` switch — the A320 has no `OnPanelButtonFired` override), `RequestFCU{Heading,Speed,Altitude,VS}Readout` (wrappers over the existing private `RequestFCU*WithStatus`/`RequestFCUVerticalSpeedFPA`, which read the `A32NX_FCU_AFS_DISPLAY_*` combine vars), `RequestAutopilotStates`, `SetAltIncrement`. Differences from the A380: **no MTRS/metric toggle** in the Altitude window (real A320 has none); the **Baro** window sets QNH via the proven `A32NX.FCU_EFIS_L/R_BARO_SET` (hPa×16) events (not `CAPT_QNH_SET`/`KOHLSMAN_SET`), STD/QNH via `*_EIS_BARO_IS_STD` both sides, unit via `XMLVAR_Baro_Selector_HPA_{1,2}`. **Autopilot panel (Ctrl+P)** identical to the A380 (shared `A32NX.FCU_*` events + `A32NX_*` state vars). The A380 FCU windows are Gus's; these are the A32NX mirror.
- **Engine Mode selector display-sync.** The A32NX combo (MainForm special-case) already reads/writes the **stock ignition simvar** `TURB ENG IGNITION SWITCH EX1:1` (via `TURBINE_IGNITION_SWITCH_SET1/2`), so it never had the A380's read-`XMLVAR_ENG_MODE_SEL` staleness bug (fixed in 7b0f661); the A32NX now also nudges `XMLVAR_ENG_MODE_SEL` on set so the cockpit/EWD display matches (the events don't touch it) — the applicable half of that A380 fix.
- **A32NX System Display now has 12 pages (added ENG + F/CTL).** The `A32NX_MSFSBA_SD_PAGE` combo offers E/WD(0), ELEC, HYD, PRESS, APU, COND, WHEEL, BLEED, FUEL, DOOR, **Engine(10)**, **Flight Controls(11)**. Content is decoded SimVars per page (`SdSystemRows`), NOT scraped — the A32NX SD page index is read-only so a page can't be forced (only the E/WD, page 0, is scraped). ENG page reads the stock simvars the FBW SD ENG page uses (`GENERAL ENG OIL TEMPERATURE`/`ENG OIL PRESSURE`/`ENG OIL QUANTITY`/`TURB ENG VIBRATION :n` + `A32NX_FADEC_IGNITER_{A,B}_ACTIVE_ENGn`) — pre-declared as **SimVar** (key underscored, Name spaced, like `ENG_ANTI_ICE:1`) so the SD auto-register loop (now pages 1-11) doesn't mis-register them as L:vars. F/CTL page decodes surface positions from the **FCDC/FAC ARINC429** words (`A32NX_FCDC_1_{ELEVATOR,AILERON}_{LEFT,RIGHT}_POS`, `_ELEVATOR_TRIM_POS`, `A32NX_FAC_1_RUDDER_{TRIM,TRAVEL_LIMIT}*`) + rudder percent + spoiler handle. **STS** (computed inop text — not SimVar-decodable, unscrapable) and **CRUISE** (redundant) are intentionally absent. Source-parity adds to existing pages: ELEC gained DC ESS / AC ESS shed / DC ESS shed buses + APU/emer gen frequency; COND gained pack 1/2 flow valves.
- **A32NX autobrake set uses the calc path.** The Autobrake combo (`AUTOBRAKE_MODE` → `HandleUIVariableSet`) writes `A32NX_AUTOBRAKES_ARMED_MODE_SET` via the MobiFlight **calculator** path (`ExecuteCalculatorCode`), NOT `SetLVar` (the data-def write is unreliable for FBW L:vars). Verified live: `2 (>L:A32NX_AUTOBRAKES_ARMED_MODE_SET)` arms MED, the SET var auto-resets to -1. The FCU value-set events likewise work via the calc-K path (live-verified: HDG/SPD/ALT/VS incl. signed VS round-trip through the `A32NX_FCU_AFS_DISPLAY_*` readback vars).
- **Phase 4 — the two FBW jets share identical keyboard shortcuts.** Both handle the same `HotkeyAction`s: the A320 gained **ReadFlaps (L)** + **ReadGear (Shift+G)**; the A380 gained **ReadFuelInfo** + a **Ctrl+B Set-Altimeter** dialog (`ShowA380BaroSetDialog`, which uses the stock `KOHLSMAN_SET` = hPa×16, NOT the A320's `A32NX.FCU_EFIS_*_BARO_SET`). NEW on both: **A/THR = Ctrl+J** and **LOC = Ctrl+L** (input mode) → `A32NX.FCU_ATHR_PUSH` / `A32NX.FCU_LOC_PUSH`. **D / Shift+D (distance + time to destination / Top of Descent) now works on BOTH jets** — the A320 reads its FMS `guidanceController` via a one-shot `CoherentEvalClient` eval of `coherent-a32nx-flightinfo.js` (it uses the SimBridge MCDU relay, not the Coherent MCDU bridge, so it can't reuse the A380's persistent client). Both share `MainForm.AnnounceFlightInfoJson` for an identical readout. See the "D / Shift+D distance hotkeys" section above.

- **Status-display boxes refresh live — F5 AND auto, without yanking the reader cursor (both jets).** The SD-page status box renders the aircraft def's `_sdPageContent`, an OnRequest **snapshot** that is ONLY rebuilt by `OnDisplayPanelShown → RefreshSdPageDisplayAsync` (which re-reads the underlying SimVars: FOB, engine N1/N2/N3, per-tank fuel, …). **The core bug:** the F5 / Refresh handler re-requested the panel's display vars and re-rendered, but never rebuilt `_sdPageContent` — so it re-printed the SAME stale snapshot and "FOB 13400 KG" never moved, *even on a manual F5*. Fix: the refresh handler now calls `OnDisplayPanelShown(currentPanel)` (fire-and-forget; it pushes its own `UpdateDisplayText` ~0.6 s later when the fresh read completes). Non-SD panels no-op (each def gates on its own panel key: A380 = "ECAM Control Panel", A32NX = "System Display").
  - **No-cursor-jump update.** Every write to a status box now goes through `MainForm.SetDisplayTextPreserveCaret(box, text)`: if the text is unchanged it leaves the box **completely untouched** (no `TextChanged`, no caret move — an idle auto-refresh is invisible); if it changed it captures `SelectionStart` before assigning and restores it (clamped) after, so a blind user reading line-by-line stays put instead of being thrown back to line 1 the instant a value ticks. This was why auto-refresh "moved the screen-reader cursor to the top."
  - **Auto-refresh** (`MainForm._sdAutoRefreshTimer`, 3 s, started/stopped by `StartOrStopSdAutoRefresh()` per panel — only panels with a `_REFRESH_` button) ticks the same rebuild + display-var re-pull; combined with the caret-preserving, change-only write it now updates "on whatever change" without disturbing the reader. The Refresh handler shows "Loading..." only on the first **empty** populate.
- **Master Warning / Caution acknowledge — clears the aural too (live on the A380, source-correct on the A32NX).** The acknowledge PBs are momentary **buttons** (`Btn`, pulse L:var 1→0) not combos. **A380:** the FBW glareshield.xml HOLD_SIMVAR is misspelled `PUSH_AUTOPILOT_MASTER`**`A`**`WARN_L/R` (extra A) — verified live: pulsing that clears `A32NX_MASTER_WARNING` + cancels the aural, while the no-A name is a no-op; caution `PUSH_AUTOPILOT_MASTERCAUT_L/R` is spelled normally. **A32NX:** `CLEAR_MASTER_WARNING`/`CLEAR_MASTER_CAUTION` already pulse `PUSH_AUTOPILOT_MASTERWARN_L`/`MASTERCAUT_L` (no extra A — the conventional A32NX name; the typo was A380-specific) 1→0 via `HandleUIVariableSet`, rendered as buttons. **DEFERRED (verify when loaded in the A32NX):** confirm the A32NX master-warn/caut pulse actually clears `A32NX_MASTER_WARNING`/`_CAUTION` + the aural live (source XMLs aren't in this fbw-aircraft checkout to cross-check the var name); if the no-A name no-ops like the A380 did, switch to `MASTERAWARN`.

- **A380 button-vs-combo audit (2026-06, 3 parallel source-cross-referenced agents).** Policy: a MOMENTARY cockpit action with no readable resting state → `Btn` (push-button, pulses L:var 1→0, announces "<name> pressed"); a multi-state selector or a LATCHING pushbutton whose pushed-in state matters → COMBO (state stays visible); fire/smoke TESTS stay combos. Changes made:
  - **`Press`→`Btn`** for the momentary push vars whose *latched result* is already a separate readout, so the Released/Pressed combo never settled: `A32NX_OVHD_AUTOBRK_RTO_ARM_IS_PRESSED` (RTO arm; state = `A32NX_AUTOBRAKES_RTO_ARMED` — **live-verified** the 1→0 pulse arms it), `A32NX_OVHD_ELEC_IDG_n_PB_IS_RELEASED` (IDG disconnect; state = `A32NX_OVHD_ELEC_IDG_n_PB_IS_DISC`), `A32NX_OVHD_HYD_RAT_MAN_ON_IS_PRESSED` and `A32NX_OVHD_EMER_ELEC_RAT_AND_EMER_GEN_IS_PRESSED` (RAT manual deploys; momentary `<MOMENTARY/>` in source, Inop in current FBW build but now the correct shape).
  - **`A32NX_TILLER_PEDAL_DISCONNECT` was BROKEN** — the public L:var is NOT consumed by the FBW NWS systems (live-verified: writing 1 sticks and is never read/reset), so the old `Btn` pulse did nothing. FBW maps the disconnect to the stock event `TOGGLE_WATER_RUDDER` → internal aspect (`nose_wheel_steering.rs`). Now a `HandleUIVariableSet` branch fires `(>K:TOGGLE_WATER_RUDDER)` (announces "Nosewheel steering pedal disconnect"). The held-vs-momentary effect should be confirmed on a taxi test.
  - **Jet bridge / passenger stairs showed FAKE state.** They were `Retracted/Extended` combos but there is no readable jetway position var and the stock event only TOGGLES — so the combo announced a position it couldn't know, and either option fired the toggle. Relabelled to an honest `Idle/Toggle` action, and the `HandleUIVariableSet` branch now fires `TOGGLE_JETWAY`/`TOGGLE_RAMPTRUCK` only on the active (`>0.5`) value.
  - **Verified correct, left as-is:** master warn/caut acks (`Btn`, incl. the A380 `MASTERAWARN` extra-A spelling), CALLS All/Fwd/Aft/Mech (`Btn`, momentary `FBW_Push_Held`), DCDU ATC ack (`Btn`), ECAM-CP keys (`PressSilent` — all 11 `A32NX_BTN_*` exist; no `STS` key on the A380 ecam-cp), chrono start-stop/reset (buttons firing H-events; fixed a stale "rendered as combos" comment), transponder ident (`Evt`), rudder-trim reset (`Act`→stock `RUDDER_TRIM_RESET`), ground-vehicle requests (`Act` Idle/Request, fire on >0.5). LATCHING state pushbuttons kept as combos: ENG/APU **fire handles** (guarded, pushed-in = armed), APU Start (`_PB_IS_ON` latches), Oxygen Timer Reset (latching toggle), speed-brake/spoiler-arm (multi-state). **FCU not touched** (per user). No stateful toggle is rendered as a momentary `Btn` (the label-staleness trap).

- **A32NX feature-parity TODO → moved to [`docs/a32nx-feature-parity-todo.md`](docs/a32nx-feature-parity-todo.md).** Every A380→A320 port step (doors→read-only status, pushback/preset panel removal + preset-progress announce, Ground-Equipment buttons, PFD/ND display adds, EWD THR%, the full A32NX display audit, etc.) lives there as a checklist — do ONE at a time and DELETE it from that file when done. The one general rule that came out of this work, kept here: **never register a name containing a space or colon as an L:var — those are stock SimVars** (the A380 "not connected" bug was the stock SimVar `INTERACTIVE POINT OPEN:n` force-registered through the L:var path; the `A380SdRows` registration loop is now hardened to register any space/colon name as `Type = SimVar`, fixed commit 8af7e47).

**⚠️ THE SIMCONNECT DATA-DEFINITION CEILING — root-caused AND STRENGTHENED 2026-06 (commit 72ced22; see `tools/a380-finish-pass-2026-06.md`).** SimConnect caps a client connection at **1000 data definitions ("objects") + 1000 requests** ([MSFS SDK: "TOO_MANY_OBJECTS… maximum is 1000"](https://docs.flightsimulator.com/html/Programming_Tools/SimConnect/API_Reference/Structures_And_Enumerations/SIMCONNECT_EXCEPTION.htm)). MSFSBA registers each var as a native SimConnect data def (`AddToDataDefinition("L:NAME"…)`), so each counts against that budget — it is NOT MobiFlight's 64-string cap (MSFSBA bypasses that channel for bulk reads). The limit is **per SimConnect connection but RESETS on aircraft switch** (MSFSBA clears + re-registers, `nextDataDefinitionId`→1000), so effectively **per-aircraft**. It is identical on **MSFS 2020 and 2024**.
  - **The bug it caused:** `StartContinuousMonitoring` registered every Continuous+IsAnnounced var a SECOND time into a `CONTINUOUS_BATCH_n` def (on top of its individual def). The A380 used ~1083 defs and overflowed; the 2nd batch's `AddToDataDefinition` failed wholesale (`TOO_MANY_OBJECTS`/`UNRECOGNIZED_ID`), and that async exception disrupted the AIRCRAFT_INFO/ATC one-shot → `IsFullyConnected` never set → "MSFS detected but every hotkey says not connected".
  - **THE FIX (3 layers, all in `SimConnectManager.cs`):** (1) **Headroom** — `RegisterAllVariables` now SKIPS the individual def for batch-covered vars (Continuous+IsAnnounced+!ExcludeFromBatch); they read their on-demand value from `lastVariableValues`, the SAME cache the batch fills (panels fall back to `GetCachedVariableValue`, forms use `GetCachedVariableSnapshot`). **A380: ~1083 → ~530 defs, verified live, fully connects.** (2) **Resilience** — `SetupDataDefinitions` registers the bulk vars LAST, after the fixed/critical defs (AIRCRAFT_INFO/ATC/position/…), so detection can NEVER again be stranded by an overflow (it degrades gracefully). (3) **Guard + observability** — a 900 individual-def cap (skip+log past it) and a persistent `%LOCALAPPDATA%/MSFSBlindAssist/logs/registration.log` recording the per-connect footprint, "[Detection] FULLY CONNECTED", and any "[CEILING]" `TOO_MANY_OBJECTS`.
  - **PRACTICAL RULE NOW:** the A380 has ~470 vars of headroom; net additions are fine again. Adding a var as **Continuous+IsAnnounced costs 1 batch slot** (no individual def); **OnRequest costs 1 individual def**. Keep an eye on `registration.log` (`approxTotalDefs` should stay well under 1000). **Diagnostic if it ever recurs:** read `registration.log`; if `[CEILING]` appears, the connection hit 1000 — reduce continuous/OnRequest count or split to a second SimConnect connection (each gets its own 1000 budget). (Dead-end theories ruled out: it was NOT MobiFlight's cap, NOT bad stock-SimVar names like `INDICATED ALTITUDE:3` — those `NAME_UNRECOGNIZED` are pre-existing/harmless — and NOT the continuous-vs-OnRequest split; it was the raw TOTAL def count vs 1000.)
  - **A32NX:** the strengthening is in the SHARED `SimConnectManager` — needs a live A32NX connect check (architecturally identical: fewer continuous vars, same cache reads).

### FlyByWire A32NX Accessible MCDU

**Feature:** Screen-reader-accessible MCDU for the FlyByWire A32NX, opened with **Shift+M** (input mode) — same shared `HotkeyAction.ShowFenixMCDU` dispatch as the other CDUs, selected by `AircraftCode == "A320"` (Fenix is `FENIX_A320CEO`, so no collision). **Single MCDU (Captain) by design** — see the protocol limitation below.

**CRITICAL — the protocol cannot separate Captain and First Officer MCDUs.** FBW's `A320_Neo_CDU_MainDisplay.sendUpdate()` builds ONE `screenState` from its own display and assigns that same object to BOTH the `left` and `right` keys of every `update` message (only `annunciators`/brightness differ). Each MCDU instrument (Captain + FO) runs its own broadcaster that writes its own screen into both keys, with no sender tag, interleaved. So `content.left == content.right` in every message (verified live: 100% of messages) and a Left/Right selector cannot pick one MCDU — it only drives the two physical MCDUs apart, making the shared display flip last-writer-wins (this was the "adjusting one side changes the other" bug). We therefore mirror FBW's own web remote MCDU exactly: **only ever control the Captain MCDU (`event:left`) and read the single shared screen (`content.left`).** Do NOT reintroduce a side selector or `event:right` — the separation is not achievable over this transport.

**Transport:** Connects to SimBridge's existing MCDU relay websocket `ws://localhost:8380/interfaces/v1/mcdu` — the same socket the FBW remote-MCDU web UI uses. Sim → client: `update:{left:{...},right:{...}}` (each side carries `lines` 12×3 `[left,right,center]` even=label/odd=value, `scratchpad`, `title`, `page`, `arrows` `[up,down,left,right]`, `annunciators`; **left and right are identical** per above — read `left`). Client → sim: `event:left:<KEY>` and `requestUpdate`. The gateway echoes every message to all clients, so the receive loop filters on `type == "update"`. No press/release pair is needed — the sim sets the `H:A320_Neo_CDU_1_BTN_{KEY}` event directly.

**Key components:**
- **`Services/FlyByWireMCDUService.cs`** — `ClientWebSocket` client with connect-loop + backoff + `SynchronizationContext` UI marshalling (mirrors `FenixMCDUService`'s shape, single socket for both directions). Reads/controls the Captain MCDU only (`const CaptainSide = "left"`); the class remarks document the no-separation protocol limitation.
- **`Services/FbwMcduFormat.cs`** — decodes curly-brace cell markup (`{green}`/`{small}`/`{sp}`/`{end}`/…) into accessible text and builds the shared `MCDUDisplayData` (extended with `Page`/`Arrows`/`Annunciators`). Mixed-color lines with a green segment mark the active option with `*` (same convention as Fenix). **This is a 1:1 mirror of `tools/fbw-mcdu-probe/mcdu-format.js` — keep both in sync.** **CRITICAL — `{`/`}` are overloaded.** They delimit `{tag}` markup AND are the MCDU's literal LSK arrow/bracket glyphs (e.g. a selectable runway renders as `{08L{small}-ILS{end}`). `parseSegments` therefore consumes ONLY *known* tags (colors / `small` / `big` / `left` / `right` / `sp` / `end`); a lone `{` or `}` is the glyph — dropped, with its content kept. Do NOT revert to greedy "`{`…`}` is always a tag" parsing — it ate the runway designators (`08L` etc.) and broke the DEP/ARR pages. **Lines are reconstructed positionally** via `PositionLine` (24 cols: left-aligned left, right-aligned right, centred centre), NOT by dropping blank columns — otherwise a right-only cell collapses onto the left of the line.
- **`Forms/FlyByWireA320/FlyByWireMCDUForm.cs`** — ListBox display + scratchpad TextBox + page buttons (no side selector — single Captain MCDU). LSK keys use the shared `MCDUUseAlternateLSKKeys` setting (Ctrl/Alt+1–6 or F1–F12). Annunciators + page number + ▲▼◄► page-arrow hints render into the display. Only background-state changes are announced (title change, scratchpad debounce, connection) per the screen-reader rule.
- **`tools/fbw-mcdu-probe/`** — standalone Node CLI (`watch`/`press`/`type`/`replay`, `--export` JSONL captures) to inspect, drive, and capture the MCDU over the websocket without the C# app. `replay` re-renders an exported capture offline (shareable for diagnosing a page without a live sim). `mcdu-format.js` is the authoritative decode reference; `node --test` covers it.

## Detailed Documentation

**Claude: Read these docs only when the task specifically requires them.**

**When to read detailed docs:**
- **Debugging the A380/A32NX live (Coherent debugger, probes, scrapers, crash diagnosis)** → [Developer Tooling Guide](docs/tooling.md)
- **Working on the HS787 FMC bridge (FS2024 compatibility)** → [HS787 Bridge Debugging](docs/hs787-bridge-debugging.md)
- **Adding complex features or workflows** → [Adding Features](docs/adding-features.md), [Quick Reference](docs/QUICK-REFERENCE.md)
- **Implementing new aircraft** → [Architecture](docs/architecture.md), [Adding Features](docs/adding-features.md)
- **Working with FCU/MCP/display systems** → [Architecture](docs/architecture.md)
- **Adding or modifying hotkeys** → [Hotkey System](docs/hotkey-system.md)
- **Fenix rotary encoders (RMP, FCU)** → [Fenix Increment/Decrement](docs/fenix-increment-decrement.md)
- **Tuning visual guidance PID controller** → [Visual Guidance](docs/visual-guidance.md)
- **Working on taxi guidance (graph, router, tone, form)** → [Taxi Guidance](docs/taxi-guidance.md)
- **Working on PMDG 737-800 panels, CDU, NG3 data struct** → [PMDG 737-800](docs/pmdg-737.md)
- **Understanding variable patterns** → [Variable System](docs/variable-system.md)
- **API reference** → [Aircraft Definitions](docs/aircraft-definitions.md)
- **Dependencies and key files** → [Development](docs/development.md)

**Available documentation:**
- **[Quick Reference](docs/QUICK-REFERENCE.md)** - Common patterns and workflows (read first for most tasks)
- **[Architecture](docs/architecture.md)** - Core components, multi-aircraft system, FCU architecture
- **[Adding Features](docs/adding-features.md)** - Step-by-step workflows for common development tasks
- **[Variable System](docs/variable-system.md)** - Three patterns for managing variables (Panel, Monitoring, Hotkey)
- **[Fenix Increment/Decrement](docs/fenix-increment-decrement.md)** - Counter-based pattern for Fenix rotary encoders
- **[Visual Guidance](docs/visual-guidance.md)** - PID controller tuning and ground track monitoring
- **[Taxi Guidance](docs/taxi-guidance.md)** - Turn-by-turn taxi assistance, steering tone, ATC-constrained routing
- **[PMDG 737-800](docs/pmdg-737.md)** - NG3 SDK patterns, two-CDU convention, FIRE_HandlePos ordering, EFB gating
- **[Aircraft Definitions](docs/aircraft-definitions.md)** - Multi-aircraft dictionary system API reference
- **[Hotkey System](docs/hotkey-system.md)** - Dual-mode hotkeys and multi-aircraft routing
- **[Development](docs/development.md)** - Dependencies, key files, development notes
- **[Developer Tooling Guide](docs/tooling.md)** - Coherent debugger (`:19999`) probes/scrapers/drivers in `tools/`, how to run each, and crash diagnosis

## Technology Stack

.NET 9 (C# 13), Windows Forms, SimConnect SDK (MSFS), SQLite, NVDA/Tolk (screen readers)
