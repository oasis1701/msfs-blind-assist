# A32NX Live Pass-2 Session Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans (inline). One live sim, Coherent one-socket-per-view; the user's aircraft is IN FLIGHT — every probe must be state-restoring and must never touch thrust, engines, gear, generators, fire systems, or AP-engaged modes.

**Goal:** Work through the A32NX items from `docs/fbw-dev-pass2-live-verification.md` against the live in-flight A32NX: verify the bench-built fixes via the SimConnect MCP, tour + fix the flyPad performance calculators, and expose the metric-alt toggle if the probe proves it.

**Architecture:** MCP calc/L:var probes (read → write → read-back → RESTORE) for the panel fixes; Coherent debugger tours for the flyPad (the A32NX MCDU uses the SimBridge websocket, so Coherent views are freer than on the A380 — MSFSBA's A320 path has no always-on EWD scrape).

**EXCLUDED IN FLIGHT (defer to a ground session, listed in the checklist):** thrust-detent axis writes (would command real thrust), fire-handle pull + agent interlock, gravity-gear vars (could drop the gear), GPWS TEST (aural + inhibits), GEN/APU-GEN toggling (ELEC transient + master caution mid-cruise).

**Tech Stack:** simconnect MCP, tools/coherent-eval.ps1, C#/.NET 9, ES5 agents, jsdom tests.

---

### Task 0: Session health — transport.log probe verdict

- [ ] Read `%LOCALAPPDATA%\MSFSBlindAssist\logs\transport.log` tail: confirm the new "[Probe] calc path VERIFIED end-to-end" line fired on the A320 session connect (the MobiFlight end-to-end probe's first real-world run), and check the MF registration line. This also answers the checklist's ECP-HVar-channel question half-way: if registration timed out again, the per-client FBWBA channel is closed on this install and the A32NX ECP HVar buttons need the calc-path check below.

### Task 1: MCP verification sweep (safe, state-restoring)

- [ ] Baro unit: read `A32NX_FCU_EFIS_L_BARO_IS_INHG` → calc-write the opposite → re-read + read `A32NX_FCU_LEFT_EIS_BARO` (displayed value should switch hPa↔inHg scale) → restore.
- [ ] LS: read `A32NX_FCU_EFIS_L_LS_LIGHT_ON` → fire `(>K:A32NX.FCU_EFIS_L_LS_PUSH)` via calc → light flips → fire again to restore.
- [ ] FPA ×10: read `A32NX_TRK_FPA_MODE_ACTIVE`; toggle TRK/FPA if needed (display-only), `25 (>K:A32NX.FCU_VS_SET)` → expect FPA selected 2.5 (read `A32NX_AUTOPILOT_FPA_SELECTED`) → restore TRK/FPA mode. Do NOT pull/engage.
- [ ] Metric alt probe: read `A32NX_METRIC_ALT_TOGGLE` (or search the live catalog for the actual state var) → fire `(>K:A32NX.FCU_METRIC_ALT_TOGGLE_PUSH)` → state flips? → fire again to restore. Record verdict for Task 3.
- [ ] FCU dashes semantics (read-only): read `A32NX_FCU_AFS_DISPLAY_SPD_DASHES` / `_HDG_DASHES` / `_VS_DASHES` in the current managed/selected state and note which modes show dashes — no knob pulls.
- [ ] NAVAID selectors: read `A32NX_EFIS_L_NAVAID_1_MODE` (computed output per the audit — verify the CONTROL var the def uses actually sticks: write → read → restore).
- [ ] FO EFIS filter push + light: fire the FO CSTR push event the def uses, read `A32NX_FCU_EFIS_R_CSTR_LIGHT_ON` flip, fire again to restore.
- [ ] LDG ELEV: read the knob var → write -4000 (Auto) → read → write a real elevation → read → RESTORE the original value immediately.
- [ ] COM3: `COM3_STBY_RADIO_SET_HZ` set a standby freq → read `COM STANDBY FREQUENCY:3` → `COM3_RADIO_SWAP` → read active/standby → swap back + restore the original standby.
- [ ] Brightness: write one `BRIGHT_*` pot the def maps (e.g. pedestal) ±20% → read the `LIGHT POTENTIOMETER:n` follow → restore.
- [ ] Wipers: circuit 77 (capt) — `77 (>K:ELECTRICAL_CIRCUIT_TOGGLE)` on, `75 77 (>K:2:ELECTRICAL_CIRCUIT_POWER_SETTING_SET)` slow, read `A:CIRCUIT SWITCH ON:77` + power setting, then off (restore).
- [ ] Dome light: `50 7 (>K:2:LIGHT_POTENTIOMETER_SET)`? — use the EXACT RPN from `FlyByWireA320Definition.HandleUIVariableSet` (read the code first), verify `LIGHT POTENTIOMETER:7` moves, restore 0.
- [ ] Runway turn-off: the def's indexed `TAXI_LIGHTS_SET` RPN → `LIGHT TAXI:2`/`:3` follow → restore off.
- [ ] Read-only existence: `A32NX_TCAS_STATE`, GPWS switch vars (`A32NX_GPWS_SYS_OFF` etc. — labels: var=1 reads Off), `A32NX_GPWS_WARNING_LIGHT_ON`/`_ALERT_LIGHT_ON` baselines.
- [ ] ECP HVar half-check: fire ONE harmless ECP HVar via the calc path (`(>H:A32NX_EFIS_L_CHRONO_PUSHED)`? no — use the ECAM ALL/RCL key the def maps, read any observable) — goal: prove H-events reach the A32NX through the default channel (they should, same as the A380).
- [ ] Anything that FAILS: fix in `FlyByWireA320Definition.cs` immediately, build, re-verify.

### Task 2: flyPad tours + Performance calculator dropdowns (the main implementation item)

PRECONDITION: MSFSBA's flyPad window (Shift+T) closed; back off on socket rejection.

- [ ] Connect `tools/coherent-eval.ps1 -Title "- EFB" -PreFile MSFSBlindAssist\Resources\coherent-flypad-agent.js`; tour to Performance. Scrape the TAKEOFF calculator: the 13 SelectInput dropdowns (runway condition, anti-ice, packs, etc.) must each appear as an interactive element with its current value; open one and check the option list surfaces. Repeat for LANDING (11 dropdowns).
- [ ] For each gap: extend `coherent-flypad-agent.js` (ES5) — SelectInput classification (closed control + open options), with live-captured jsdom fixtures in `tools\flypad-settings-test\`. `node --test` green. Build, commit.
- [ ] Checklists page: tour with `EFB_AUTOFILL_CHECKLISTS` current state; verify item check-state reads (`text-utility-green` path) and that disabled tiles read "(dimmed)" and refuse clicks.
- [ ] Return the flyPad to the Dashboard.

### Task 3: Metric-alt exposure (only if the Task-1 probe proved it)

- [ ] If `A32NX.FCU_METRIC_ALT_TOGGLE_PUSH` flips a readable state var: add to `FlyByWireA320Definition.cs` a settable combo (Off/On, state = the state var, set fires the event via `HandleUIVariableSet`) in the FCU/Autoflight panel + extend the FCU Altitude window if the A380 pattern applies (A32NX has no MTRS button physically — note that dev FBW added the event anyway; expose it as the combo only, no hotkey). If the state var does NOT flip: record "registered but inert in dev FBW" in the checklist and change nothing.

### Task 4: Docs + final build

- [ ] Update `docs/fbw-dev-pass2-live-verification.md`: tick every verified item with results; annotate the in-flight-excluded items as "deferred to a ground session" explicitly.
- [ ] `dotnet build MSFSBlindAssist.sln -c Release`; verify the exe + Resources timestamps; commit (individual file adds, never `git add -A`).

## Execution progress

(ticked during the session)
