FLYBYWIRE A380X — HOTKEY REFERENCE

Quick key list only. For what each feature does and step-by-step how to use it
(the MCDU, flyPad, checklists, displays, flight planning, systems, a full-flight
walkthrough), see the full manual: docs/a380-manual.html.

Modes: press ] (right bracket) for OUTPUT mode, [ (left bracket) for INPUT mode,
Escape to exit. A few chords are direct (no mode). Press the same bracket again
to cancel a mode activated by accident.


DIRECT (no mode)
  Ctrl+1     Focus Sections list
  Ctrl+2     Focus Panels list
  Ctrl+3     Focus Status Display field of the current panel (refreshes it if empty)
  Ctrl+M     Monitor Manager (choose what auto-announces)
  Ctrl+E     Toggle ECAM E/WD call-outs
  Ctrl+Shift+B  Airport Map and BTV (OANS) window


OUTPUT MODE  (press ] first)

  FCU read
  Shift+H    FCU Heading
  Shift+S    FCU Speed (knots or Mach)
  Shift+A    FCU Altitude
  Shift+V    FCU Vertical Speed / FPA

  Flight data
  Q          Altitude AGL
  A          Altitude MSL
  S          IAS
  T          TAS
  G          Ground Speed
  M          Mach Number
  H          Magnetic Heading
  U          True Heading
  V          Vertical Speed
  D          Distance to destination (from the FMS)
  Shift+D    Distance to Top of Descent (once computed in flight)
             (Q / A / Shift+A read in metres when the FCU MTRS button is on)
  Ctrl+W     ND active (TO) waypoint: name, distance, and bearing
  Ctrl+Shift+R  Last landing rate (touchdown vertical speed, feet per minute)
  Ctrl+Shift+G  Last landing peak g-force

  Characteristic speeds
  Shift+1    Green Dot speed
  Shift+2    S speed (slat retraction)
  Shift+3    F speed (flap retraction)
  Shift+4    VLS (minimum selectable)
  Shift+5    VS (stall)
  Shift+6    VFE next (next flap limit)

  Weather / time / misc
  I          Wind info
  O          Outside Temperature (Celsius)
  Z          Local Time
  Shift+Z    Zulu / UTC Time
  X          Squawk Code
  C          Nearest City
  Shift+T    Toggle trim announcements

  Traffic / weather windows
  R          Announce TCAS traffic
  Ctrl+R     Show TCAS window
  Shift+W    Weather Radar
  Shift+M    METAR report

  Display windows
  Alt+E      E/WD (Engine/Warning Display) window
  Alt+I      Standby Instruments (ISIS) window
  Alt+D      Describe Scene (AI)

  Checklists
  Shift+C    Static text checklist (cold-and-dark to shutdown)
  Ctrl+Shift+C  Live Electronic Checklist (ECL) window -- the real cockpit
                checklists + active ECAM procedures (see below)

  Taxi
  Y          Taxi status
  Ctrl+Y     Repeat taxi instruction
  Alt+Y      Where am I (ground)

  Tools
  Ctrl+H     Hand-fly mode
  Ctrl+V     Visual guidance (needs hand-fly)
  Ctrl+T     Takeoff Assistance
  Ctrl+D     Distance to selected runway
  Ctrl+I     ILS guidance
  Ctrl+G     Read latest GSX tooltip


INPUT MODE  (press [ first)

  Windows
  Shift+M    Open MCDU / MFD window
  Shift+T    Open flyPad EFB window
  Ctrl+Shift+R  Open Radio Management Panel (RMP) window — Captain / First Officer,
             type a frequency + Enter, page / line / swap / clear keys
  Alt+G      Open GSX window

  Teleport / flight planning
  Shift+R    Runway teleport
  Shift+G    Gate teleport
  Shift+F    Fix / waypoint tracking
  Shift+D    Select destination runway

  Autopilot
  Shift+A    Autopilot 1
  Ctrl+O     Autopilot 2
  Shift+P    Approach mode
  Ctrl+J     Autothrust (A/THR arm / disconnect)
  Ctrl+L     Localizer (LOC arm)

  FCU push / pull
  Shift+1 / Ctrl+1  Push / Pull Heading knob
  Shift+2 / Ctrl+2  Push / Pull Altitude knob
  Shift+3 / Ctrl+3  Push / Pull Speed knob
  Shift+4 / Ctrl+4  Push / Pull VS knob

  FCU windows (value entry + knob buttons + spoken readout)
  Ctrl+S     Speed window (set value; Push/Pull; SPD/MACH toggle)
  Ctrl+H     Heading window (set value; Push/Pull; HDG-V/S / TRK-FPA toggle)
  Ctrl+A     Altitude window (set value; Push/Pull; 100/1000; Metric toggle)
  Ctrl+V     Vertical Speed / FPA window (set value; Push/Pull)
  Ctrl+P     Autopilot panel (AP1/AP2; A/THR; AP + A/THR disconnect;
             APPR/LOC/EXPED; Flight Director status)
  Ctrl+B     Altimeter window (enter QNH once -> both sides; STD/QNH; hPa/inHg)

  Taxi / landing
  Shift+Y    Open Taxi Assist form
  Y          Continue past hold-short
  Ctrl+Y     Stop taxi guidance
  Shift+X    Open Landing Exit Planner


INSIDE THE MCDU / MFD WINDOW
  Up / Down  Move through lines
  Enter      Activate the line (send scratchpad to field; open a combo box)
  (type)     Type into the scratchpad, then Enter
  Backspace  CLR (scratchpad first, then the selected field)
  Delete     Clear the selected field
  Ctrl+Up    Previous page (PageUp also)
  Ctrl+Down  Next page (PageDown also; scrolls the F-PLN window)
  F5         Refresh

  Page buttons (Alt+letter)
  Alt+I  INIT     Alt+F  F-PLN    Alt+P  PERF
  Alt+R  RAD NAV  Alt+S  SEC FPL  Alt+A  ATC COM
  Alt+D  DIR      Alt+H  Refresh
  Alt+U  Toggle weight units (kilograms / pounds) for MSFSBA read-outs


INSIDE THE RADIO MANAGEMENT PANEL (RMP) WINDOW (Ctrl+Shift+R opens it)
  The RMP screen is read-only text — read it with the arrow keys. It refreshes
  automatically and announces the standby and any message (e.g. FREQ NOT VALID).
  Soft keys (same scheme as the MCDU: left = Ctrl, right = Alt):
  Ctrl+1 / 2 / 3   Select radio 1 / 2 / 3 (the left line keys; again = load standby)
  Alt+1 / 2 / 3    Swap that radio's active and standby (the right keys; manual)
                   (or F1/2/3 = select, F7/8/9 = swap, with alternate MCDU keys on)
  Pages (also buttons in the Tab order):
  Alt+V            VHF page          Alt+Q   Transponder (SQWK) page
                   (HF / TEL / NAV / MENU are not modelled by FBW — those do nothing)
  Alt+C            Fully clear the entry (resets a stuck entry)
  Alt+Home         Jump back to the screen (e.g. from a page button)
  You TYPE the digits right on the screen — there is no separate entry box (the
  screen is read-only, so the digits go to the radio, not the text):
  VHF page  — type the digits (keyed live, no decimal), Enter loads the standby,
             Backspace deletes a digit; Alt+n swaps it active.
  SQWK page — type the 4 squawk digits, Enter validates, Alt+3 = ident.
  Side combo (Captain / First Officer) switches which RMP you control — the screen,
  the announcements and the keys all follow it.


INSIDE THE LIVE CHECKLIST (ECL) WINDOW (Ctrl+Shift+C opens it)
  The checklist is shown automatically when the window opens and hidden when it
  closes -- no need to toggle it.
  Up / Down  Move the checklist cursor and read each line
  Enter      Open the highlighted checklist, or tick the highlighted item
  Backspace  Step back to the menu
  F5         Refresh
  (the Abnormal procedures button lists active ECAM procedures)

Thrust levers auto-announce the detent reached (Idle / Climb / Flex MCT / TOGA /
Reverse). Most cockpit controls are in the Sections/Panels lists (Ctrl+1/Ctrl+2).
Full explanations: docs/a380-manual.html.
