Runway geometry internals from the PR #238 deferred-findings pass: one memoised runway shape per centreline instead of two allocations per call (171,254 calls in an on-runway sweep fell from 48 ms to 9 ms), thread-safe gate-source caches, the Landing Exit planner's graph load moved entirely off the UI thread, and the ProgressiveTaxiProbe link set repaired so the probe builds again.
