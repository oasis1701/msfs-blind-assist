The PMDG 777's TCAS Test button (Pedestal, Transponder/TCAS) now actually runs the test.
The press it sent was one the aircraft ignores for this button, so nothing happened.
You now hear "TCAS test", then "TCAS test pass" about eight seconds later.
