GSX stand identifiers are logged exactly as sent, leading and trailing spaces
included — trimming them for display is what hid a two-week-old gate-selection
failure. Every built taxi route now logs which runways it crosses, so a report about
an odd route can be answered from the log instead of reconstructed by hand.
