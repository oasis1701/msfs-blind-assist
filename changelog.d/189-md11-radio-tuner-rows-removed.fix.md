The six "MHz Tuner" and "KHz Tuner" rows are gone from the MD-11's Radios panel. They were read-only fields that always read 0, because the tuner knobs are rotary encoders whose variable counts clicks rather than holding a position — there was nothing useful to show and no way to operate them. Tune with the standby frequency field and the transfer button, which were already there.
