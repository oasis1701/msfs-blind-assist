The A380 navigation display mode and range knobs now actually move. Setting either one from a panel wrote a value the aircraft overwrites every frame, so the display never changed and the control simply snapped back with nothing said. Both knobs are now turned the way the cockpit turns them. The range list also had the wrong numbers behind it, which would have selected an airport-zoom range instead of the one you picked — 40 now gives you 40.
