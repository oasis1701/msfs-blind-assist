The control-map generator no longer invents an on/off map for a cockpit tooltip whose expression is a comparison rather than a plain variable read. It records the threshold instead, refuses and counts the shapes it cannot read, and reports both in the map's statistics — previously that class left no trace at all. Measured against the installed package: exactly one control (the gear lever) was affected, and it already carries a hand-written fix.
