The A32NX/A330 CONF 3 landing L:var is now registered `OnRequest`, matching its `A32NX_SPEEDS_*` siblings, the A380's copy of the same variable, and the First Officer poll that is its only delivery path. It had been declared `Continuous` without `IsAnnounced` — a frequency no continuous delivery path in `SimConnectManager` admits. A new delivery-contract test pins the pairing between the registration's frequency and the evaluator poll list for both airframes, so a repeat cannot pass by merely being present in `GetVariables()`.
