The developer tool that builds the MD-11's control list now finds the aircraft on any PC —
Microsoft Store or Steam, MSFS 2020 or 2024, and custom or external add-on folders — instead
of only on one developer's machine. It also refuses to write an incomplete control list rather
than quietly leaving out the cockpit read-outs.
