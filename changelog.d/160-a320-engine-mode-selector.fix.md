The First Officer now actually turns the ENG MODE knob on the A32NX and the Headwind
A330. It never did: the write landed on a variable the aircraft does not have, and the
step still announced "Engine mode selector: IGN START" and ticked its checklist item, so
you reached the start with the knob still in NORM and nothing to tell you. It also reads
the knob's real position now, so a step that is already set is skipped quietly instead of
being fired every time, the engine mode panel tracks the knob when you turn it in the
cockpit, and a change announces on its own.
